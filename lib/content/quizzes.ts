import type { Quiz } from "./types";

export const quizzes: Quiz[] = [
  {
    id: "swift-basics-quiz",
    title: "Swift Basics Quiz",
    description: "20 questions on variables and constants, type inference, integers and floating point, booleans, strings, tuples, type conversion, and comments.",
    relatedArticleSlug: "swift-basics",
    questions: [
      {
        id: "q1",
        prompt: "What happens if you try to reassign a new value to a constant declared with `let`?",
        options: [
          { id: "a", text: "It compiles but Xcode shows a warning" },
          { id: "b", text: "It compiles with no issues, since `let` only applies at declaration" },
          { id: "c", text: "It's a compile-time error" },
          { id: "d", text: "It's a runtime crash" },
        ],
        correctOptionId: "c",
        explanation: "`let` constants must be assigned exactly once. Attempting a second assignment — `maxAttempts = 4` after `let maxAttempts = 3` — produces the compile-time error \"cannot assign to value: '...' is a 'let' constant.\" This is caught before the code ever runs, unlike a runtime crash.",
      },
      {
        id: "q2",
        prompt: "Which statement about deferred initialization of a `let` constant is true?",
        options: [
          { id: "a", text: "It's not allowed — `let` requires a value at the declaration line" },
          { id: "b", text: "It's allowed only inside a `class`" },
          { id: "c", text: "It's allowed as long as the compiler can prove the constant is assigned exactly once on every possible code path before its first use" },
          { id: "d", text: "It's allowed, but only if you also mark it `lazy`" },
        ],
        correctOptionId: "c",
        explanation: "Swift permits declaring a `let` without an initial value (`let temperature: Double`) and assigning it later, as long as every branch (e.g. both sides of an `if`/`else`) guarantees it's set exactly once before it's read. The compiler tracks this at compile time — no `lazy` keyword involved, and it works in functions generally, not just classes.",
      },
      {
        id: "q3",
        prompt: "What type is inferred for `let price = 19.99`?",
        options: [
          { id: "a", text: "`Float`" },
          { id: "b", text: "`Double`" },
          { id: "c", text: "`Decimal`" },
          { id: "d", text: "`Int`" },
        ],
        correctOptionId: "b",
        explanation: "Swift infers `Double` for any floating-point literal by default. To get `Float` instead, you'd need an explicit annotation: `let price: Float = 19.99`.",
      },
      {
        id: "q4",
        prompt: "Why does `var buffer: [UInt8]` require an explicit type annotation?",
        options: [
          { id: "a", text: "Arrays can never have their type inferred" },
          { id: "b", text: "`UInt8` cannot be used inside array literals" },
          { id: "c", text: "There's no initial value/literal for the compiler to infer a type from" },
          { id: "d", text: "It's a stylistic convention only, not a requirement" },
        ],
        correctOptionId: "c",
        explanation: "Type inference works by examining the literal or expression on the right-hand side of `=`. With no initial value at all, there's nothing to infer from, so an explicit annotation becomes mandatory.",
      },
      {
        id: "q5",
        prompt: "On modern Apple platforms, what is the size of the default `Int` type?",
        options: [
          { id: "a", text: "Always 32-bit, regardless of platform" },
          { id: "b", text: "Matches the platform's native word size — 64-bit on modern devices" },
          { id: "c", text: "16-bit" },
          { id: "d", text: "Arbitrary precision (grows as needed)" },
        ],
        correctOptionId: "b",
        explanation: "`Int` is platform-dependent and matches the native word size, which is 64-bit on all current Apple platforms. Fixed-width alternatives like `Int8`, `Int16`, `Int32`, `Int64` exist for when you need a guaranteed specific width.",
      },
      {
        id: "q6",
        prompt: "What happens when you compute `UInt8.max + 1` using the regular `+` operator?",
        options: [
          { id: "a", text: "It silently wraps around to `0`" },
          { id: "b", text: "It traps with a runtime \"Arithmetic overflow\" crash" },
          { id: "c", text: "It's automatically promoted to `Int` and returns `256`" },
          { id: "d", text: "It's a compile-time error" },
        ],
        correctOptionId: "b",
        explanation: "Swift integers trap on overflow by default — a deliberate safety choice, unlike C's silent wraparound. To get wraparound behavior deliberately, you must opt in explicitly with the overflow operator `&+` (`UInt8.max &+ 1` gives `0`).",
      },
      {
        id: "q7",
        prompt: "What does `7 / 2` evaluate to when both operands are `Int`?",
        options: [
          { id: "a", text: "`3.5`" },
          { id: "b", text: "`3`" },
          { id: "c", text: "`4`" },
          { id: "d", text: "A runtime crash" },
        ],
        correctOptionId: "b",
        explanation: "Integer division truncates toward zero and never auto-promotes to a floating-point result. `7 / 2` is `3`; to get `3.5` you must convert both operands to `Double` first: `Double(7) / Double(2)`.",
      },
      {
        id: "q8",
        prompt: "Why does `0.1 + 0.2 == 0.3` evaluate to `false` in Swift?",
        options: [
          { id: "a", text: "It's a bug specific to the Swift compiler" },
          { id: "b", text: "Binary floating-point representation can't exactly represent most decimal fractions" },
          { id: "c", text: "`Double` values are always compared by reference, not value" },
          { id: "d", text: "Operator precedence causes `0.2 == 0.3` to be evaluated first" },
        ],
        correctOptionId: "b",
        explanation: "This is a fundamental property of IEEE 754 binary floating-point, not a Swift-specific bug — the same result occurs in virtually every language using binary floats. `0.1 + 0.2` actually equals `0.30000000000000004`. The takeaway is to never compare floating-point values with `==`; use a tolerance-based comparison instead.",
      },
      {
        id: "q9",
        prompt: "Which type should you use for exact decimal arithmetic, such as currency calculations?",
        options: [
          { id: "a", text: "`Double`" },
          { id: "b", text: "`Float`" },
          { id: "c", text: "`Decimal`" },
          { id: "d", text: "`Int` with manual rounding" },
        ],
        correctOptionId: "c",
        explanation: "`Decimal` performs exact decimal arithmetic without the binary rounding errors inherent to `Double`/`Float`. For money specifically, `Decimal` (or integer cents) avoids surprises like `0.1 + 0.2 != 0.3`.",
      },
      {
        id: "q10",
        prompt: "Which operators in Swift are short-circuiting?",
        options: [
          { id: "a", text: "`&` and `|`" },
          { id: "b", text: "`&&` and `||`" },
          { id: "c", text: "Only `||`" },
          { id: "d", text: "Neither — Swift always evaluates both sides" },
        ],
        correctOptionId: "b",
        explanation: "`&&` and `||` stop evaluating as soon as the result is already determined by the left-hand side — e.g. `false && expensiveCheck()` never calls `expensiveCheck()`. This is also why patterns like `!array.isEmpty && array[0] == 5` are safe: the subscript is only reached if the array is confirmed non-empty first.",
      },
      {
        id: "q11",
        prompt: "What is the purpose of a `// MARK: -` comment, and does it affect compiled behavior?",
        options: [
          { id: "a", text: "It changes compiler optimization level for the following code" },
          { id: "b", text: "It's purely a navigation aid for Xcode's jump bar and has zero effect on compilation or runtime behavior" },
          { id: "c", text: "It automatically generates unit tests for the marked section" },
          { id: "d", text: "It suppresses warnings for the rest of the file" },
        ],
        correctOptionId: "b",
        explanation: "`// MARK:` comments (along with `// TODO:` and `// FIXME:`) exist purely to help Xcode organize a file's jump bar into navigable sections — the leading `-` adds a visual divider line. None of these affect compilation or runtime behavior at all; they're purely organizational, distinct from `///` documentation comments, which Xcode renders as Quick Help for a symbol.",
      },
      {
        id: "q12",
        prompt: "What is the idiomatic way to embed a variable's value inside a string?",
        options: [
          { id: "a", text: "String concatenation using `+`" },
          { id: "b", text: "String interpolation using `\\(...)`" },
          { id: "c", text: "`String(format:)` exclusively" },
          { id: "d", text: "Manual character-by-character construction" },
        ],
        correctOptionId: "b",
        explanation: "Interpolation (`\"\\(name) is \\(age) years old.\"`) is the idiomatic, readable approach in Swift, and it accepts any expression — not just simple variables, but full expressions like `\\(a + b)`.",
      },
      {
        id: "q13",
        prompt: "Why does `word[0]` fail to compile for a Swift `String`?",
        options: [
          { id: "a", text: "Strings in Swift are always immutable" },
          { id: "b", text: "Swift strings are collections of extended grapheme clusters with variable byte-width, so there's no way to jump to \"the Nth character\" in constant time — Swift refuses to hide that cost behind `Int` subscripting" },
          { id: "c", text: "It's simply a missing feature that will be added in a future release" },
          { id: "d", text: "It actually does compile and work correctly" },
        ],
        correctOptionId: "b",
        explanation: "Because characters can be composed of multiple Unicode scalars, integer indexing would silently be an O(n) operation disguised as O(1). Swift's design intentionally forces you to use `String.Index` (or convert to `[Character]`) instead of hiding that cost.",
      },
      {
        id: "q14",
        prompt: "What type do you get back when subscripting a `String` with a `Range<String.Index>`?",
        options: [
          { id: "a", text: "`String`" },
          { id: "b", text: "`Substring`" },
          { id: "c", text: "`Character`" },
          { id: "d", text: "`[Character]`" },
        ],
        correctOptionId: "b",
        explanation: "Range-subscripting a string produces a `Substring`, a lightweight view that shares storage with the original string. Convert it to a full `String` with `String(subSequence)` before storing it long-term, since a `Substring` keeps the original string's storage alive.",
      },
      {
        id: "q15",
        prompt: "In a multiline string literal (`\"\"\"...\"\"\"`), what determines how much leading whitespace is stripped from each line?",
        options: [
          { id: "a", text: "The indentation of the first line of content" },
          { id: "b", text: "The indentation of the closing `\"\"\"`" },
          { id: "c", text: "Tabs are stripped automatically; spaces never are" },
          { id: "d", text: "Nothing is stripped — all whitespace is preserved literally" },
        ],
        correctOptionId: "b",
        explanation: "The closing `\"\"\"`'s indentation level acts as the baseline; any matching leading whitespace on each content line is stripped, letting you indent a multiline string naturally within your code without that indentation leaking into the actual string value.",
      },
      {
        id: "q16",
        prompt: "What does wrapping a string in `#\"...\"#` (a raw string) do?",
        options: [
          { id: "a", text: "Enables interpolation everywhere automatically" },
          { id: "b", text: "Disables standard escape-sequence processing and interpolation, treating backslashes and quotes literally" },
          { id: "c", text: "Is purely cosmetic and behaves identically to a normal string" },
          { id: "d", text: "Converts the string to Unicode scalar view automatically" },
        ],
        correctOptionId: "b",
        explanation: "Raw strings turn off `\\n`-style escapes and `\\(...)` interpolation by default, which is why they're ideal for regex patterns or file paths full of backslashes (`#\"C:\\Users\\Nahidul\\Documents\"#`). To interpolate inside a raw string, you must match the `#` count: `#\"\\#(value)\"#`.",
      },
      {
        id: "q17",
        prompt: "What does a Swift `Character` represent?",
        options: [
          { id: "a", text: "A single raw byte" },
          { id: "b", text: "A single UTF-8 code unit" },
          { id: "c", text: "An extended grapheme cluster — a user-perceived character, which may be composed of multiple Unicode scalars" },
          { id: "d", text: "A fixed 2-byte UTF-16 code unit" },
        ],
        correctOptionId: "c",
        explanation: "Swift's `Character` type models what a human perceives as \"one character\" on screen, regardless of how many underlying Unicode scalars are combined to produce it (e.g. a letter plus a combining accent, or a flag emoji made of two regional indicator scalars).",
      },
      {
        id: "q18",
        prompt: "For the string `\"👨‍👩‍👧‍👦\"` (a family emoji made of four people joined with zero-width joiners), which statement is true?",
        options: [
          { id: "a", text: "`.count` and `.unicodeScalars.count` are equal" },
          { id: "b", text: "`.count` is `1`, while `.unicodeScalars.count` is much greater than `1`" },
          { id: "c", text: "`.count` is greater than `.utf8.count`" },
          { id: "d", text: "It's impossible to store this as a single `Character`" },
        ],
        correctOptionId: "b",
        explanation: "`.count` operates at the grapheme-cluster level, so a visually single emoji counts as `1` `Character`, even though it's built from 7 Unicode scalars joined together — and 11 UTF-16 code units, and 25 UTF-8 bytes. These different \"views\" of a string can report very different lengths.",
      },
      {
        id: "q19",
        prompt: "What is the type of `let coordinate = (2, 3)`?",
        options: [
          { id: "a", text: "`[Int]`, a two-element array" },
          { id: "b", text: "A tuple of type `(Int, Int)`" },
          { id: "c", text: "A custom `Point` struct" },
          { id: "d", text: "`Dictionary<Int, Int>`" },
        ],
        correctOptionId: "b",
        explanation: "Parentheses with comma-separated values create a tuple — a lightweight compound value with no need to declare a named type. Elements can be accessed positionally (`.0`, `.1`) or, if named (`(x: 2, y: 3)`), by name (`.x`, `.y`).",
      },
      {
        id: "q20",
        prompt: "What does `Int(\"abc\")` return?",
        options: [
          { id: "a", text: "A runtime crash" },
          { id: "b", text: "`0`" },
          { id: "c", text: "`nil`, because `Int.init?(String)` is a failable initializer" },
          { id: "d", text: "A compile-time error" },
        ],
        correctOptionId: "c",
        explanation: "Converting a `String` to a numeric type is inherently fallible — the string might not represent a valid number — so Swift's `Int(String)` initializer returns an optional (`Int?`). `Int(\"abc\")` returns `nil`, while `Int(\"42\")` returns `Optional(42)`. This is also why `// MARK:`/`///` comments matter for documenting such failable APIs clearly for other developers — the doc comment can call out that the initializer may return `nil`.",
      },
    ],
  },
  {
    id: "control-flow-quiz",
    title: "Control Flow Quiz",
    description: "20 questions on if/switch branching, every loop form, guard, break/continue, and defer.",
    relatedArticleSlug: "control-flow",
    questions: [
      {
        id: "q1",
        prompt: "What type must the condition in an `if` statement be in Swift?",
        options: [
          { id: "a", text: "Any type — Swift auto-converts to boolean" },
          { id: "b", text: "`Int`, where `0` is false and anything else is true" },
          { id: "c", text: "`Bool`, with no implicit conversion from any other type" },
          { id: "d", text: "`String`, where empty strings are false" },
        ],
        correctOptionId: "c",
        explanation: "Swift enforces that `if` conditions are genuinely `Bool` — there is no implicit truthiness conversion from `Int`, `String`, or any other type, unlike C or JavaScript.",
      },
      {
        id: "q2",
        prompt: "When using the single-expression `if` form to assign a value (`let x = if cond { a } else { b }`), what's required?",
        options: [
          { id: "a", text: "Nothing special — any mismatched branch types are automatically unified" },
          { id: "b", text: "Every branch must produce the same type" },
          { id: "c", text: "An `else` branch is optional" },
          { id: "d", text: "This syntax only works with `Bool` return values" },
        ],
        correctOptionId: "b",
        explanation: "When `if` is used as an expression to produce a value, all branches must evaluate to the same type so the compiler can determine a single result type for the whole expression, and every branch (including `else`) must be present.",
      },
      {
        id: "q3",
        prompt: "What is required for a `switch` statement over a non-enum type like `Int` to compile?",
        options: [
          { id: "a", text: "Nothing — Swift `switch` statements never require exhaustiveness" },
          { id: "b", text: "It must cover every case explicitly, one per line" },
          { id: "c", text: "It must either cover all possible values or include a `default:` case" },
          { id: "d", text: "It must always end in `break`" },
        ],
        correctOptionId: "c",
        explanation: "Swift `switch` statements must be exhaustive. For a type like `Int` with effectively infinite possible values, you satisfy exhaustiveness with a `default:` case; for enums, listing every case can satisfy exhaustiveness without `default`.",
      },
      {
        id: "q4",
        prompt: "By default, does a matched `case` in a Swift `switch` fall through into the next case?",
        options: [
          { id: "a", text: "Yes, exactly like C or Java, unless you add `break`" },
          { id: "b", text: "No — each case implicitly breaks after its body runs; `fallthrough` must be added explicitly to opt in to fall-through behavior" },
          { id: "c", text: "Only if the case doesn't include a comma-separated value list" },
          { id: "d", text: "Only for numeric switches, not for strings" },
        ],
        correctOptionId: "b",
        explanation: "Unlike C-family languages, Swift's `switch` cases do not fall through automatically — this removes a common class of forgotten-`break` bugs. If you genuinely want fall-through behavior, you opt in with the explicit `fallthrough` keyword.",
      },
      {
        id: "q5",
        prompt: "In `case 80..<90:` inside a switch over an `Int` score, which values match?",
        options: [
          { id: "a", text: "80 through 90, inclusive of both" },
          { id: "b", text: "80 up to but not including 90" },
          { id: "c", text: "Only exactly 80 or exactly 90" },
          { id: "d", text: "Any value greater than 80" },
        ],
        correctOptionId: "b",
        explanation: "`..<` is Swift's half-open range operator, excluding the upper bound. So `80..<90` matches 80 through 89. The closed range operator `...` would include 90 as well.",
      },
      {
        id: "q6",
        prompt: "In a tuple switch pattern like `case (_, 0):`, what does the underscore `_` mean?",
        options: [
          { id: "a", text: "It matches only the literal value `0`" },
          { id: "b", text: "It's a wildcard that matches any value in that position, ignoring it" },
          { id: "c", text: "It causes a compile-time error unless bound with `let`" },
          { id: "d", text: "It represents `nil`" },
        ],
        correctOptionId: "b",
        explanation: "`_` is Swift's wildcard pattern — it matches anything in that position without binding it to a name. `case (_, 0)` matches any first element, as long as the second element is exactly `0`.",
      },
      {
        id: "q7",
        prompt: "What does a `where` clause do when attached to a `switch` case, e.g. `case let (x, y) where x == y:`?",
        options: [
          { id: "a", text: "It replaces the need for a pattern entirely" },
          { id: "b", text: "It adds an additional runtime boolean condition that must also be true for the case to match, on top of the pattern itself matching" },
          { id: "c", text: "It only works with `default` cases" },
          { id: "d", text: "It forces the case to always match, ignoring the pattern" },
        ],
        correctOptionId: "b",
        explanation: "`where` lets you layer an extra condition onto a pattern match — the case only fires if the pattern matches *and* the `where` condition evaluates to `true`. This is commonly combined with value bindings, as in `case let (x, y) where x == y`.",
      },
      {
        id: "q8",
        prompt: "What does `case let (x, y):` do when matching against a tuple in a switch statement?",
        options: [
          { id: "a", text: "It's a syntax error — tuples cannot be destructured in switch cases" },
          { id: "b", text: "It binds the tuple's elements to new local constants `x` and `y` for use within that case's body" },
          { id: "c", text: "It only works if `x` and `y` were already declared elsewhere" },
          { id: "d", text: "It requires `var` instead of `let` to work at all" },
        ],
        correctOptionId: "b",
        explanation: "`case let x` (or `case var x`) is value binding — it extracts values out of the matched pattern (a tuple here, but also enum associated values or optional patterns) and binds them to new local names usable inside that case's body.",
      },
      {
        id: "q9",
        prompt: "What is the difference between `for i in 1...5` and `for i in 0..<5`?",
        options: [
          { id: "a", text: "They are identical" },
          { id: "b", text: "`1...5` includes both 1 and 5 (5 iterations: 1,2,3,4,5); `0..<5` excludes 5 (5 iterations: 0,1,2,3,4)" },
          { id: "c", text: "`0..<5` includes 5 as the final value" },
          { id: "d", text: "`1...5` only works with floating-point ranges" },
        ],
        correctOptionId: "b",
        explanation: "`...` is a closed range including both endpoints, while `..<` is half-open, excluding the upper bound. Both examples happen to iterate 5 times, but over different actual values (1–5 vs. 0–4).",
      },
      {
        id: "q10",
        prompt: "What is the key behavioral difference between `while` and `repeat-while`?",
        options: [
          { id: "a", text: "`repeat-while` is deprecated in modern Swift" },
          { id: "b", text: "`while` checks its condition before each iteration (may run zero times); `repeat-while` checks after each iteration (always runs at least once)" },
          { id: "c", text: "`while` always runs exactly once" },
          { id: "d", text: "There is no difference — they're interchangeable syntax for the same behavior" },
        ],
        correctOptionId: "b",
        explanation: "`while` evaluates its condition up front, so the loop body might never execute if the condition starts `false`. `repeat-while` (Swift's `do-while` equivalent) always executes the body at least once before checking the condition, useful for things like retry loops.",
      },
      {
        id: "q11",
        prompt: "What is the difference between `break` and `continue` inside a loop?",
        options: [
          { id: "a", text: "They are synonyms" },
          { id: "b", text: "`break` exits the loop entirely; `continue` skips the rest of the current iteration and moves to the next one" },
          { id: "c", text: "`break` skips one iteration; `continue` exits the loop entirely" },
          { id: "d", text: "`continue` can only be used inside a `switch`" },
        ],
        correctOptionId: "b",
        explanation: "`break` terminates the nearest enclosing loop (or switch case) immediately. `continue` stops the current iteration's remaining code but lets the loop proceed to its next iteration, re-evaluating the loop condition as normal.",
      },
      {
        id: "q12",
        prompt: "In nested loops, how do you make `break` or `continue` target an outer loop instead of the innermost one?",
        options: [
          { id: "a", text: "It's not possible in Swift — you'd need a boolean flag variable instead" },
          { id: "b", text: "By using a labeled statement, e.g. `outerLoop: for ... { ... break outerLoop }`" },
          { id: "c", text: "By calling `break` twice in a row" },
          { id: "d", text: "By wrapping the outer loop in a function and using `return`" },
        ],
        correctOptionId: "b",
        explanation: "A label placed before a loop (`outerLoop: for i in ...`) lets you write `break outerLoop` or `continue outerLoop` from within a nested loop, explicitly targeting that labeled loop instead of only affecting the innermost one.",
      },
      {
        id: "q13",
        prompt: "What values does `stride(from: 0, to: 20, by: 5)` produce?",
        options: [
          { id: "a", text: "0, 5, 10, 15, 20" },
          { id: "b", text: "0, 5, 10, 15 (20 excluded)" },
          { id: "c", text: "5, 10, 15, 20" },
          { id: "d", text: "An error, since `stride` requires `through:` not `to:`" },
        ],
        correctOptionId: "b",
        explanation: "`stride(from:to:by:)` is half-open, like `..<` — it excludes the final \"to\" value. To include the endpoint, use `stride(from: 0, through: 20, by: 5)`, which would produce 0, 5, 10, 15, 20.",
      },
      {
        id: "q14",
        prompt: "What is the main caution around the ternary operator (`condition ? a : b`)?",
        options: [
          { id: "a", text: "It cannot be used with `String` types" },
          { id: "b", text: "It's slower at runtime than `if`/`else`" },
          { id: "c", text: "It reads fine for one simple condition, but becomes hard to read once nested or combined with complex conditions — an `if`/`else` or `switch` is clearer at that point" },
          { id: "d", text: "It requires both branches to be the same literal value" },
        ],
        correctOptionId: "c",
        explanation: "The ternary operator is fine for compact, single-condition choices (like `age >= 18 ? \"Adult\" : \"Minor\"`), but nesting ternaries (`a > b ? (a > c ? a : c) : ...`) quickly becomes difficult to parse visually — that logic reads far more clearly as a plain `if`/`else` or a small helper function.",
      },
      {
        id: "q15",
        prompt: "What must the `else` branch of a `guard` statement do?",
        options: [
          { id: "a", text: "Nothing in particular — it's optional" },
          { id: "b", text: "It must exit the current scope, e.g. via `return`, `break`, `continue`, or `throw`" },
          { id: "c", text: "It must always call `fatalError()`" },
          { id: "d", text: "It must reassign the bound variable to a default value" },
        ],
        correctOptionId: "b",
        explanation: "`guard`'s `else` clause is mandatory and must transfer control out of the current scope. This is what guarantees that any values bound in the `guard`'s condition are safe to use for the rest of the enclosing scope — if the condition fails, control never reaches that code.",
      },
      {
        id: "q16",
        prompt: "What is the key structural advantage of `guard let x = ...` over `if let x = ...` for validating multiple inputs at the top of a function?",
        options: [
          { id: "a", text: "`guard let` runs faster at runtime" },
          { id: "b", text: "Bindings from `guard let` remain available for the rest of the enclosing scope, avoiding the deep nesting that repeated `if let` blocks would require" },
          { id: "c", text: "`guard let` can bind more variables at once than `if let`" },
          { id: "d", text: "There is no actual difference between them" },
        ],
        correctOptionId: "b",
        explanation: "With `if let`, the unwrapped binding is only visible inside that `if`'s indented block, so validating several optionals in sequence nests deeper and deeper. `guard let`'s binding escapes into the rest of the function, keeping the \"happy path\" flat and unindented.",
      },
      {
        id: "q17",
        prompt: "When does a `defer` block execute?",
        options: [
          { id: "a", text: "Immediately, at the point it's written in the code" },
          { id: "b", text: "Only if the function throws an error" },
          { id: "c", text: "When the current scope exits, whether through normal completion, an early `return`, a thrown error, or a `break`" },
          { id: "d", text: "Only once, the first time the containing function is ever called" },
        ],
        correctOptionId: "c",
        explanation: "`defer` schedules code to run whenever the enclosing scope exits, regardless of the exit path — normal fall-through completion, an early return, a thrown error, or a break out of a loop. It's Swift's mechanism for guaranteed cleanup.",
      },
      {
        id: "q18",
        prompt: "If a function contains three `defer` blocks in sequence, in what order do they execute when the function exits?",
        options: [
          { id: "a", text: "In the same order they were written (first-in-first-out)" },
          { id: "b", text: "In reverse order — the last `defer` written runs first (last-in-first-out)" },
          { id: "c", text: "In random order" },
          { id: "d", text: "Only the last `defer` block actually runs; the others are ignored" },
        ],
        correctOptionId: "b",
        explanation: "Multiple `defer` blocks in the same scope execute in reverse order, like a stack (LIFO). This matters for symmetric setup/teardown — e.g., releasing a lock before closing the resource it was protecting, in the exact reverse order they were acquired.",
      },
      {
        id: "q19",
        prompt: "What does the `fallthrough` keyword do inside a `switch` case?",
        options: [
          { id: "a", text: "Nothing — it's not valid Swift syntax" },
          { id: "b", text: "It causes execution to continue into the next case's body, regardless of whether that next case's pattern matches" },
          { id: "c", text: "It exits the switch statement immediately" },
          { id: "d", text: "It's required at the end of every case for the switch to compile" },
        ],
        correctOptionId: "b",
        explanation: "`fallthrough` is Swift's explicit opt-in to C-style fall-through behavior — after `fallthrough`, execution moves directly into the very next case's body without evaluating whether that case's own pattern matches.",
      },
      {
        id: "q20",
        prompt: "Consider a `switch` over `(Any) ` values using `case let n as Int where n < 0:`. What does this pattern require to match?",
        options: [
          { id: "a", text: "The value must simply be non-nil" },
          { id: "b", text: "The value must successfully cast to `Int` (via `as`), AND the `where` condition (`n < 0`) must also be true" },
          { id: "c", text: "The value must be a `String` representation of a negative number" },
          { id: "d", text: "The `where` clause is ignored when combined with `as` casting" },
        ],
        correctOptionId: "b",
        explanation: "This combines a type-casting pattern (`as Int`) with a `where` clause — Swift first attempts to bind the value as an `Int`; only if that succeeds does it then evaluate the `where` condition. Both must hold for the case to be selected, which is a common pattern when switching over heterogeneous `Any` collections.",
      },
    ],
  },
  {
    id: "collections-quiz",
    title: "Collections Quiz",
    description: "20 questions on Array, Dictionary, Set, and the map/filter/reduce family.",
    relatedArticleSlug: "collections",
    questions: [
      {
        id: "q1",
        prompt: "What happens when you access an out-of-bounds index on an `Array`, e.g. `[1, 2, 3][5]`?",
        options: [
          { id: "a", text: "It returns `nil`" },
          { id: "b", text: "It returns the last valid element instead" },
          { id: "c", text: "It crashes at runtime with \"Index out of range\"" },
          { id: "d", text: "It's a compile-time error" },
        ],
        correctOptionId: "c",
        explanation: "Unlike dictionary subscripting, `Array` subscripting traps at runtime on an invalid index rather than returning an optional or clamping to a valid value. This is why safe access patterns (`indices.contains`, `.first`/`.last`, or a custom `[safe:]` extension) matter for untrusted indices.",
      },
      {
        id: "q2",
        prompt: "Why can't you call `.append(_:)` on an array declared with `let`?",
        options: [
          { id: "a", text: "`append` doesn't exist on `Array` — only `insert` does" },
          { id: "b", text: "Mutability is a property of the binding; a `let` array is immutable, so all mutating methods are disallowed" },
          { id: "c", text: "`append` only works on `Set`, not `Array`" },
          { id: "d", text: "It's allowed, but only for arrays of value types" },
        ],
        correctOptionId: "b",
        explanation: "As with all value types in Swift, whether a variable can be mutated depends on whether it's declared with `var` or `let`. A `let`-declared array cannot use `append`, `insert`, `remove`, `sort()`, or any other mutating member — you'd need to redeclare it with `var`.",
      },
      {
        id: "q3",
        prompt: "What is the benefit of a custom `subscript(safe:)` extension on `Array`, such as `numbers[safe: 5]`?",
        options: [
          { id: "a", text: "It makes the array sort automatically" },
          { id: "b", text: "It returns an optional (`nil` for an invalid index) instead of crashing, unlike the standard subscript" },
          { id: "c", text: "It changes the array's element type to `Any`" },
          { id: "d", text: "It's identical in behavior to the standard subscript, just slower" },
        ],
        correctOptionId: "b",
        explanation: "A safe-subscript extension checks `indices.contains(index)` internally and returns `Element?`, gracefully returning `nil` for an invalid index rather than trapping — useful when working with indices that aren't guaranteed to be valid.",
      },
      {
        id: "q4",
        prompt: "What does subscripting a `Dictionary` with a key that doesn't exist return, e.g. `ages[\"Dave\"]` when `\"Dave\"` isn't a key?",
        options: [
          { id: "a", text: "A runtime crash, just like an out-of-bounds array access" },
          { id: "b", text: "`nil`, because dictionary subscripting always returns an optional" },
          { id: "c", text: "`0` by default" },
          { id: "d", text: "A compile-time error" },
        ],
        correctOptionId: "b",
        explanation: "Dictionary subscripting always returns an optional (`Value?`) because a missing key is treated as a normal, expected outcome rather than a programmer error — a deliberate contrast to `Array`'s crash-on-invalid-index behavior.",
      },
      {
        id: "q5",
        prompt: "What does `visitCounts[\"profile\", default: 0]` do if `\"profile\"` isn't currently a key in `visitCounts`?",
        options: [
          { id: "a", text: "It crashes" },
          { id: "b", text: "It inserts `\"profile\": 0` into the dictionary permanently" },
          { id: "c", text: "It returns `0` as a fallback value without modifying the dictionary (unless you use it as the target of a compound assignment like `+=`)" },
          { id: "d", text: "It throws a runtime error requiring `try?`" },
        ],
        correctOptionId: "c",
        explanation: "The `default:` subscript parameter supplies a fallback value to read when the key is missing, avoiding manual `if let`/`??` boilerplate. It becomes especially useful in a compound assignment like `wordCounts[word, default: 0] += 1`, which both reads the fallback and writes the incremented value back.",
      },
      {
        id: "q6",
        prompt: "Why is `Set.contains(_:)` generally faster than `Array.contains(_:)` for membership testing on large collections?",
        options: [
          { id: "a", text: "`Set` stores its elements in sorted order, enabling binary search" },
          { id: "b", text: "`Set` membership testing is average O(1), backed by hashing, versus `Array`'s O(n) linear scan" },
          { id: "c", text: "There is no actual performance difference between them" },
          { id: "d", text: "`Array.contains` is deprecated in modern Swift" },
        ],
        correctOptionId: "b",
        explanation: "`Set` requires elements to conform to `Hashable` and uses that hash to locate elements directly, giving average O(1) membership checks. `Array.contains` must scan linearly through elements, which is O(n) — this difference becomes significant as collections grow.",
      },
      {
        id: "q7",
        prompt: "Given `let a: Set = [1, 2, 3, 4]` and `let b: Set = [3, 4, 5, 6]`, what does `a.subtracting(b)` return?",
        options: [
          { id: "a", text: "`{5, 6}`" },
          { id: "b", text: "`{3, 4}`" },
          { id: "c", text: "`{1, 2}`" },
          { id: "d", text: "`{1, 2, 3, 4, 5, 6}`" },
        ],
        correctOptionId: "c",
        explanation: "`subtracting` returns the elements present in `a` but not in `b`. Since `3` and `4` are shared, they're removed, leaving `{1, 2}`. (`a.intersection(b)` would give `{3, 4}`, and `a.union(b)` would give all six elements.)",
      },
      {
        id: "q8",
        prompt: "What is the difference between `sorted()` and `sort()` on an array?",
        options: [
          { id: "a", text: "They are identical in every way" },
          { id: "b", text: "`sorted()` returns a new sorted array without mutating the original; `sort()` mutates the array in place and returns nothing" },
          { id: "c", text: "`sort()` only works on arrays of `String`" },
          { id: "d", text: "`sorted()` requires a `var` array; `sort()` works on `let`" },
        ],
        correctOptionId: "b",
        explanation: "`sorted()`/`sorted(by:)` produce a new array, leaving the original untouched (and can be called on a `let` array). `sort()`/`sort(by:)` mutate the array directly, requiring a `var`-declared array, and don't return a value.",
      },
      {
        id: "q9",
        prompt: "What does `numbers.map { $0 * 2 }` produce, given `numbers = [1, 2, 3]`?",
        options: [
          { id: "a", text: "`[1, 2, 3]` unchanged" },
          { id: "b", text: "`[2, 4]` — one element dropped" },
          { id: "c", text: "`[2, 4, 6]` — the same element count, each transformed" },
          { id: "d", text: "A single value: `12`" },
        ],
        correctOptionId: "c",
        explanation: "`map` transforms every element 1:1 — the result always has the same number of elements as the input, just with each one passed through the closure. It never filters or drops elements; that's the job of `filter`.",
      },
      {
        id: "q10",
        prompt: "What does `numbers.filter { $0 % 2 == 0 }` do?",
        options: [
          { id: "a", text: "Transforms every element into a boolean" },
          { id: "b", text: "Returns a new array containing only the elements for which the closure returned `true`" },
          { id: "c", text: "Removes all elements and returns an empty array" },
          { id: "d", text: "Sorts the array by evenness" },
        ],
        correctOptionId: "b",
        explanation: "`filter` keeps only elements matching a predicate closure, discarding the rest, and returns a new collection — the original is untouched (for `Array`; the mutating in-place variant is `removeAll(where:)`, not `filter`).",
      },
      {
        id: "q11",
        prompt: "What does `numbers.reduce(0) { $0 + $1 }` compute, given `numbers = [1, 2, 3, 4, 5]`?",
        options: [
          { id: "a", text: "`[1, 2, 3, 4, 5]` unchanged" },
          { id: "b", text: "`120` (the product)" },
          { id: "c", text: "`15` (the sum, starting the accumulator at `0`)" },
          { id: "d", text: "`0`, since the initial value overrides everything" },
        ],
        correctOptionId: "c",
        explanation: "`reduce` combines every element into a single accumulated value, starting from the given initial value (`0` here) and applying the closure cumulatively: `0+1=1`, `1+2=3`, `3+3=6`, `6+4=10`, `10+5=15`.",
      },
      {
        id: "q12",
        prompt: "What is the key difference between `map` and `compactMap` when converting `[\"1\", \"2\", \"abc\"]` using `{ Int($0) }`?",
        options: [
          { id: "a", text: "They produce identical results" },
          { id: "b", text: "`map` produces `[Optional(1), Optional(2), nil]`; `compactMap` produces `[1, 2]`, automatically dropping the failed conversion" },
          { id: "c", text: "`compactMap` crashes on the invalid string `\"abc\"`" },
          { id: "d", text: "`map` automatically sorts the results; `compactMap` does not" },
        ],
        correctOptionId: "b",
        explanation: "`map` preserves every element's transformed result, including `nil`s, giving you an array of optionals. `compactMap` performs the same transformation but then automatically unwraps and discards any `nil` results, giving you a clean array of only the successful conversions.",
      },
      {
        id: "q13",
        prompt: "What does `flatMap` do differently from `map` when working with `[[1, 2, 3], [4, 5]]`?",
        options: [
          { id: "a", text: "They behave identically for nested arrays" },
          { id: "b", text: "`flatMap` maps each inner array through the closure and then flattens all the resulting sequences into one single-level array" },
          { id: "c", text: "`flatMap` only works on dictionaries" },
          { id: "d", text: "`flatMap` sorts the flattened result automatically" },
        ],
        correctOptionId: "b",
        explanation: "`flatMap` is designed for when each element maps to a *sequence* — it flattens those sequences into one combined result. `nested.flatMap { $0 }` on `[[1, 2, 3], [4, 5]]` gives `[1, 2, 3, 4, 5]`, whereas plain `map { $0 }` would just return the original nested array unchanged.",
      },
      {
        id: "q14",
        prompt: "Why is `numbers.first(where: { $0 % 2 == 0 })` generally more efficient than `numbers.filter { $0 % 2 == 0 }.first`?",
        options: [
          { id: "a", text: "They perform identically — this is a myth" },
          { id: "b", text: "`first(where:)` short-circuits and stops scanning as soon as a match is found, while `filter(...).first` must build the entire filtered array first" },
          { id: "c", text: "`filter` always throws an error on large arrays" },
          { id: "d", text: "`first(where:)` only works on sorted arrays" },
        ],
        correctOptionId: "b",
        explanation: "`first(where:)` stops as soon as it finds the first matching element. `filter` must fully process the entire source collection to build a complete filtered array before `.first` can even be taken from it — wasted work if you only needed the first match.",
      },
      {
        id: "q15",
        prompt: "What does `for (index, fruit) in fruits.enumerated()` provide inside the loop body?",
        options: [
          { id: "a", text: "Only the fruit values, with indices discarded" },
          { id: "b", text: "Each element paired with its integer offset within the collection" },
          { id: "c", text: "A random sampling of elements with a random index" },
          { id: "d", text: "The count of fruits, repeated for each iteration" },
        ],
        correctOptionId: "b",
        explanation: "`enumerated()` pairs every element with its zero-based integer offset, replacing manual index-tracking loops (`var i = 0; for fruit in fruits { ...; i += 1 }`) with a clean, direct tuple destructuring in the `for-in` loop.",
      },
      {
        id: "q16",
        prompt: "What happens with `zip(names, ages)` if `names` has 3 elements and `ages` has only 2?",
        options: [
          { id: "a", text: "It crashes with a runtime error" },
          { id: "b", text: "It pads the shorter sequence with `nil` values" },
          { id: "c", text: "It produces only 2 pairs, stopping at the length of the shorter sequence, silently dropping the extra element from the longer one" },
          { id: "d", text: "It throws a compile-time error requiring equal-length arrays" },
        ],
        correctOptionId: "c",
        explanation: "`zip` pairs elements positionally from two sequences and stops once the shorter sequence is exhausted — any extra elements in the longer sequence are simply never included in the output, with no error or crash.",
      },
      {
        id: "q17",
        prompt: "Given `let slice = numbers[1...3]` where `numbers = [10, 20, 30, 40, 50]`, what is true about `slice[1]`?",
        options: [
          { id: "a", text: "It returns `20`, the same as if the slice were zero-indexed" },
          { id: "b", text: "It crashes, because `ArraySlice` preserves the original array's indices rather than renumbering from zero — index `1` isn't within the slice's valid range (which starts at `1` and needs index `2` for the second element)" },
          { id: "c", text: "`ArraySlice` doesn't support subscripting at all" },
          { id: "d", text: "It returns the same result as `slice.first`" },
        ],
        correctOptionId: "b",
        explanation: "This is a well-known `ArraySlice` gotcha: slicing doesn't reindex from zero — it keeps the original array's index numbering. `slice[1]` is invalid because the slice's valid indices are `1...3` (matching the original array), so you'd need `slice[2]` to get the value `30`. Converting with `Array(slice)` gives you a proper zero-based array.",
      },
      {
        id: "q18",
        prompt: "What's the recommended practice for long chains of collection operations like `.filter { }.map { }.reduce(0, +)`?",
        options: [
          { id: "a", text: "Always cram them onto a single line, regardless of length, for consistency" },
          { id: "b", text: "Avoid chaining altogether and always use manual `for` loops instead" },
          { id: "c", text: "Two or three chained operations read fine inline; beyond that, put one operation per line (or use named intermediate variables) to keep the pipeline scannable" },
          { id: "d", text: "Chaining more than one operation is a compile error" },
        ],
        correctOptionId: "c",
        explanation: "Chaining is idiomatic Swift and works well for a couple of operations inline, but a long dense one-liner becomes hard to scan and debug. Breaking each stage onto its own line — one `.filter`, one `.map`, one `.reduce` per line — keeps each transformation step clear and easy to breakpoint individually.",
      },
      {
        id: "q19",
        prompt: "What is the main benefit of using `.lazy` before a chain like `numbers.lazy.map { ... }.filter { ... }.first`?",
        options: [
          { id: "a", text: "It automatically sorts the results" },
          { id: "b", text: "It guarantees thread safety" },
          { id: "c", text: "It defers computation so elements are only processed as needed, avoiding full intermediate arrays — valuable when you only need a small subset of results (like `.first`) from a large or expensive-to-transform source" },
          { id: "d", text: "It converts the array to a `Set` to remove duplicates" },
        ],
        correctOptionId: "c",
        explanation: "Without `.lazy`, each `map`/`filter` call eagerly processes the *entire* collection and builds a full intermediate array before the next step runs. `.lazy` defers all of that, computing only as many elements as are actually consumed — which can save significant work when you only need a few results (e.g., `.first` or `.prefix(3)`) from a very large source.",
      },
      {
        id: "q20",
        prompt: "What is a downside of using `.lazy` sequences compared to eager collection operations?",
        options: [
          { id: "a", text: "`.lazy` sequences cannot be iterated more than once, ever" },
          { id: "b", text: "A lazy sequence re-runs its transform closures every time you iterate it again, since results aren't cached — so it isn't ideal when you need to reuse the same computed results repeatedly" },
          { id: "c", text: "`.lazy` always produces incorrect results for `filter`" },
          { id: "d", text: "`.lazy` only works with `Dictionary`, not `Array`" },
        ],
        correctOptionId: "b",
        explanation: "Laziness trades away caching for deferred computation — every time you iterate a lazy sequence, its transformation closures run again from scratch. For a one-time consumption of a small prefix, this is a big win; if you need the same result set multiple times, converting back to a stored `Array(...)` once is usually better.",
      },
    ],
  },
  {
    id: "optionals-quiz",
    title: "Optionals Quiz",
    description: "20 questions on nil, unwrapping, optional chaining, nil-coalescing, and force unwrapping.",
    relatedArticleSlug: "optionals",
    questions: [
      {
        id: "q1",
        prompt: "Which of these can actually be assigned `nil`?",
        options: [
          { id: "a", text: "`var name: String = \"Nahidul\"`" },
          { id: "b", text: "`var middleName: String? = \"Ahmed\"`" },
          { id: "c", text: "Both A and B" },
          { id: "d", text: "Neither — `nil` doesn't exist in Swift" },
        ],
        correctOptionId: "b",
        explanation: "Only optional types (declared with a trailing `?`) can hold `nil`. A plain `String` can never be `nil` — assigning `nil` to it is a compile-time error, since the type system enforces the distinction entirely at compile time.",
      },
      {
        id: "q2",
        prompt: "What is `Optional` actually implemented as, under the hood?",
        options: [
          { id: "a", text: "A special compiler primitive with no user-visible type" },
          { id: "b", text: "A generic enum with two cases: `.none` and `.some(Wrapped)`" },
          { id: "c", text: "A subclass of `NSObject`" },
          { id: "d", text: "A protocol that any type can conform to" },
        ],
        correctOptionId: "b",
        explanation: "`Optional<Wrapped>` is a standard-library enum with cases `.none` (equivalent to `nil`) and `.some(Wrapped)` (holding an actual value). `let a: Int? = nil` and `let a: Int? = Optional.none` are exactly equivalent.",
      },
      {
        id: "q3",
        prompt: "Why does `let doubled = age * 2` fail to compile when `age` is declared as `Int?`?",
        options: [
          { id: "a", text: "`Int?` doesn't support multiplication at all, even after unwrapping" },
          { id: "b", text: "You cannot use an optional's value directly as its underlying type without first unwrapping it" },
          { id: "c", text: "`age` must be declared with `var`, not `let`, to be used in arithmetic" },
          { id: "d", text: "It's a runtime error, not a compile-time one" },
        ],
        correctOptionId: "b",
        explanation: "Swift requires you to unwrap an optional — via `if let`, `guard let`, force-unwrap, or similar — before treating it as its underlying, non-optional type. This is enforced by the compiler, precisely to prevent accidentally operating on a value that might not exist.",
      },
      {
        id: "q4",
        prompt: "What does the shorthand `if let username { ... }` do (introduced in Swift 5.7)?",
        options: [
          { id: "a", text: "It's invalid syntax — you must always write `if let username = username`" },
          { id: "b", text: "It unwraps `username` and shadows it with the same name, so inside the block `username` refers to the unwrapped, non-optional value" },
          { id: "c", text: "It only works for `Bool` optionals" },
          { id: "d", text: "It force-unwraps `username`, crashing if it's `nil`" },
        ],
        correctOptionId: "b",
        explanation: "The shorthand lets you avoid writing `if let username = username`, instead shadowing the existing optional with an unwrapped constant of the same name — a concise convenience for the extremely common case of unwrapping a variable into itself.",
      },
      {
        id: "q5",
        prompt: "What is the key structural difference between `guard let` and `if let` when unwrapping an optional?",
        options: [
          { id: "a", text: "There is no difference" },
          { id: "b", text: "`guard let`'s unwrapped binding remains available for the rest of the enclosing scope; `if let`'s binding is only available inside its own indented block" },
          { id: "c", text: "`guard let` can only be used inside classes" },
          { id: "d", text: "`if let` requires an explicit `else`, while `guard let` does not" },
        ],
        correctOptionId: "b",
        explanation: "This scope difference is why `guard let` is preferred for validating multiple inputs at the top of a function — it keeps the \"happy path\" flat instead of nesting deeper with every additional `if let` check, since each `guard let` binding stays in scope afterward.",
      },
      {
        id: "q6",
        prompt: "Given `struct Person { var address: Address? }` and `struct Address { var street: String }`, what does `person.address?.street` evaluate to if `person.address` is `nil`?",
        options: [
          { id: "a", text: "It crashes at runtime" },
          { id: "b", text: "It evaluates to `nil`, short-circuiting the rest of the chain without crashing" },
          { id: "c", text: "It returns an empty string `\"\"`" },
          { id: "d", text: "It's a compile-time error" },
        ],
        correctOptionId: "b",
        explanation: "Optional chaining (`?.`) propagates `nil` through the entire chain the moment any link is `nil` — there's no crash, and the whole expression simply evaluates to `nil` rather than attempting to access `.street` on a nonexistent `Address`.",
      },
      {
        id: "q7",
        prompt: "In `let displayName = username ?? \"Guest\"`, when is `\"Guest\"` actually used?",
        options: [
          { id: "a", text: "Always, regardless of `username`'s value" },
          { id: "b", text: "Only when `username` is `nil`" },
          { id: "c", text: "Only when `username` is an empty string" },
          { id: "d", text: "Never — `??` only works with numeric types" },
        ],
        correctOptionId: "b",
        explanation: "The nil-coalescing operator `??` evaluates to the left-hand value if it's non-nil, and falls back to the right-hand default only when the left side is `nil`. The right-hand side is evaluated lazily (short-circuited) — it's skipped entirely if the left side already has a value.",
      },
      {
        id: "q8",
        prompt: "What happens when you force-unwrap (`!`) an optional that is actually `nil`?",
        options: [
          { id: "a", text: "It silently returns a default value like `0` or `\"\"`" },
          { id: "b", text: "It's a compile-time error, caught before running" },
          { id: "c", text: "It's an immediate runtime crash (\"Unexpectedly found nil while unwrapping an Optional value\")" },
          { id: "d", text: "It returns `nil` again, unchanged" },
        ],
        correctOptionId: "c",
        explanation: "Force-unwrapping asserts to the compiler that the optional definitely holds a value. If that assertion is wrong, the app crashes immediately with a fatal error — there's no recoverable exception to catch, which is why `!` should be reserved for cases with a genuine structural guarantee.",
      },
      {
        id: "q9",
        prompt: "What is the primary legitimate modern use case for declaring a new implicitly unwrapped optional (`String!`) in your own code?",
        options: [
          { id: "a", text: "They're the default recommended way to declare all optional properties" },
          { id: "b", text: "There is essentially no reason to write a new one in modern Swift — they exist mainly for Objective-C interop and legacy patterns like `@IBOutlet`, and you should recognize them in existing code rather than write new ones" },
          { id: "c", text: "They avoid the performance cost of regular optionals" },
          { id: "d", text: "They're required for all `class` properties" },
        ],
        correctOptionId: "b",
        explanation: "Implicitly unwrapped optionals exist almost entirely for interoperability with older Objective-C APIs and specific UIKit patterns like `@IBOutlet` (nil until the view loads, then guaranteed set). In new Swift code, prefer a regular optional (`?`) and unwrap it safely, or restructure so the value is genuinely non-optional from the start.",
      },
      {
        id: "q10",
        prompt: "In `switch response { case 200?: ...; case nil: ... }` where `response` is `Int?`, what does the `200?` pattern match?",
        options: [
          { id: "a", text: "Any value greater than 200" },
          { id: "b", text: "Specifically the case where `response` holds the value `200` (equivalent to `.some(200)`)" },
          { id: "c", text: "It's invalid syntax" },
          { id: "d", text: "It matches only `nil`" },
        ],
        correctOptionId: "b",
        explanation: "The `value?` shorthand in a switch pattern is equivalent to writing `.some(value)` explicitly — it matches only when the optional holds that specific wrapped value. `case nil:` (equivalent to `.none`) separately handles the absent case.",
      },
      {
        id: "q11",
        prompt: "Given `let age: Int? = 25`, what does `age.map { $0 * 2 }` produce?",
        options: [
          { id: "a", text: "`50` directly, as a plain `Int`" },
          { id: "b", text: "`Optional(50)` — the transformation is applied to the wrapped value, and the result is re-wrapped in an optional" },
          { id: "c", text: "`nil`, because `map` doesn't work on optionals" },
          { id: "d", text: "A runtime crash" },
        ],
        correctOptionId: "b",
        explanation: "`Optional` supports `map` just like collections do — it applies the closure to the wrapped value if one exists, and produces a new optional wrapping the result. If the original optional were `nil`, the closure would never run and the result would simply be `nil`.",
      },
      {
        id: "q12",
        prompt: "Why would `numberString.map { Int($0) }` be problematic if `numberString` is a `String?`, compared to using `flatMap` instead?",
        options: [
          { id: "a", text: "`map` isn't defined for `String?` at all" },
          { id: "b", text: "`map`'s closure itself returns an optional (`Int?`, since `Int(String)` is failable), so the result becomes a nested optional `Int??`; `flatMap` flattens this back down to a single `Int?`" },
          { id: "c", text: "`flatMap` is simply a deprecated alias for `map`" },
          { id: "d", text: "There's no actual difference between the two here" },
        ],
        correctOptionId: "b",
        explanation: "The rule is: use `map` when your closure returns a plain value, and `flatMap` when your closure itself returns an optional. Since `Int(someString)` already returns `Int?`, wrapping that result again via `map` produces `Int??`; `flatMap` avoids that extra nesting level.",
      },
      {
        id: "q13",
        prompt: "What does a nested optional like `Int??` typically indicate?",
        options: [
          { id: "a", text: "A compiler bug" },
          { id: "b", text: "That the value has been double-wrapped — often from a dictionary of optional values (`[String: Int?]`) or from using `map` where `flatMap` was actually needed" },
          { id: "c", text: "That the value is guaranteed non-nil" },
          { id: "d", text: "A special Swift type used only for JSON parsing" },
        ],
        correctOptionId: "b",
        explanation: "Nested optionals most commonly arise from subscripting a dictionary whose *values* are themselves optional, or from a `map` call whose closure returns an optional (which should have used `flatMap` instead, as in Q12). They can be collapsed with `flatMap { $0 }` or by unwrapping twice.",
      },
      {
        id: "q14",
        prompt: "For `let dict: [String: Int?] = [\"score\": nil]`, what does `dict[\"score\"]` actually evaluate to?",
        options: [
          { id: "a", text: "A plain `nil`" },
          { id: "b", text: "`Optional(nil)` — the outer optional (from the subscript) is `.some`, because the key exists, while it wraps an inner `.none` (the stored value itself is nil)" },
          { id: "c", text: "It throws a runtime error" },
          { id: "d", text: "`0`" },
        ],
        correctOptionId: "b",
        explanation: "This is the classic nested-optional distinction: the key `\"score\"` genuinely exists in the dictionary (so the subscript's outer optional is non-nil), but the *value* stored for that key is itself `nil`. This differs meaningfully from looking up a key that doesn't exist at all, where the subscript's outer optional would be plain `nil`.",
      },
      {
        id: "q15",
        prompt: "Do two optionals of a `Comparable` type support direct `==` comparison, e.g. `let a: Int? = 5; let b: Int? = nil; a == b`?",
        options: [
          { id: "a", text: "No, `==` is never defined for optional types" },
          { id: "b", text: "Yes — optionals of `Equatable`/`Comparable` types support `==` directly, comparing wrapped values, and treating `nil == nil` as `true`" },
          { id: "c", text: "Only if both sides are explicitly force-unwrapped first" },
          { id: "d", text: "It always returns `true` regardless of the actual values" },
        ],
        correctOptionId: "b",
        explanation: "`==` works out of the box on optionals: two non-nil optionals compare their wrapped values, two `nil` optionals are considered equal to each other, and a `nil` compared to a non-nil value is `false`. In the example given, `a == b` evaluates to `false` (5 vs. nil).",
      },
      {
        id: "q16",
        prompt: "Why does `[Int?].sorted()` fail to compile directly, without a custom comparator?",
        options: [
          { id: "a", text: "Arrays of optionals cannot be sorted under any circumstances" },
          { id: "b", text: "Optionals are not `Comparable` by default, since there's no universally correct rule for whether `nil` should sort before or after real values — Swift refuses to guess" },
          { id: "c", text: "`sorted()` only works on `String` arrays" },
          { id: "d", text: "It's a runtime error, not a compile-time one" },
        ],
        correctOptionId: "b",
        explanation: "While `==` is defined for optionals, ordering operators (`<`, `>`) are not, because there's no single correct answer for where `nil` belongs in a sort order. You must supply `sorted(by:)` with custom logic (or substitute a sentinel default via `??`) to explicitly decide.",
      },
      {
        id: "q17",
        prompt: "What does `scores.sorted { ($0 ?? Int.max) < ($1 ?? Int.max) }` accomplish for an array of `Int?`?",
        options: [
          { id: "a", text: "It crashes on any `nil` element" },
          { id: "b", text: "It sorts real values normally, while pushing all `nil` elements to the end of the result (by treating them as `Int.max` for comparison purposes only)" },
          { id: "c", text: "It removes all `nil` elements from the array" },
          { id: "d", text: "It sorts `nil` values before all real values" },
        ],
        correctOptionId: "b",
        explanation: "Substituting `Int.max` for `nil` (via `??`) only for the purposes of the comparison closure means every real value will compare as \"less than\" any `nil`, effectively pushing all `nil`s to the end of the sorted result, without altering the original array's actual contents.",
      },
      {
        id: "q18",
        prompt: "Which unwrapping approach is most appropriate for validating three optional function parameters at the top of a function, each with a different failure message?",
        options: [
          { id: "a", text: "Nesting three separate `if let` blocks, one inside another" },
          { id: "b", text: "Three separate `guard let ... else { return ... }` statements in sequence" },
          { id: "c", text: "Force-unwrapping all three with `!`" },
          { id: "d", text: "A single `switch` statement with sixteen combined cases" },
        ],
        correctOptionId: "b",
        explanation: "Sequential `guard let` statements let each precondition fail independently with its own specific error/return message, while keeping all successfully-unwrapped bindings available afterward without nesting — this is the standard idiomatic pattern for validating multiple inputs.",
      },
      {
        id: "q19",
        prompt: "In `if let first, let last { print(\"\\(first) \\(last)\") }`, what happens if `last` is `nil` but `first` has a value?",
        options: [
          { id: "a", text: "The block still runs, printing `first` with an empty string for `last`" },
          { id: "b", text: "It crashes" },
          { id: "c", text: "The entire condition fails, and the block does not execute at all — every comma-separated binding must succeed" },
          { id: "d", text: "It falls back to a default value for `last`" },
        ],
        correctOptionId: "c",
        explanation: "When multiple optional bindings are chained with commas in a single `if let`, all of them must successfully unwrap for the block to execute. If any one of them is `nil`, the whole condition fails and control moves to the `else` branch (if present) or simply skips the block.",
      },
      {
        id: "q20",
        prompt: "What does `user.profileImageURLString.flatMap { URL(string: $0) }` produce as its overall type, given `profileImageURLString` is `String?` and `URL(string:)` is a failable initializer returning `URL?`?",
        options: [
          { id: "a", text: "`URL??`, a doubly-nested optional" },
          { id: "b", text: "`URL?` — a single-level optional, because `flatMap` flattens the optional-returning closure's result instead of nesting it" },
          { id: "c", text: "`URL`, non-optional, since `flatMap` always unwraps completely" },
          { id: "d", text: "A compile-time error, since `flatMap` doesn't accept failable initializers" },
        ],
        correctOptionId: "b",
        explanation: "This is exactly the use case `flatMap` is for: the closure itself returns an optional (`URL?`), so using `flatMap` instead of `map` avoids producing a nested `URL??` and instead yields a clean, single-level `URL?`.",
      },
    ],
  },
  {
    id: "functions-and-closures-quiz",
    title: "Functions and Closures Quiz",
    description: "20 questions on parameters, closures, capturing, escaping, and higher-order functions.",
    relatedArticleSlug: "functions-and-closures",
    questions: [
      {
        id: "q1",
        prompt: "What is true about `func square(_ n: Int) -> Int { n * n }`?",
        options: [
          { id: "a", text: "It requires an explicit `return` keyword or it fails to compile" },
          { id: "b", text: "It uses implicit return, valid for single-expression function bodies since Swift 5.1" },
          { id: "c", text: "It's invalid Swift syntax — functions must always specify `-> Void` if returning nothing" },
          { id: "d", text: "`n * n` is treated as a discarded statement, and the function actually returns nothing" },
        ],
        correctOptionId: "b",
        explanation: "Since Swift 5.1, a function (or closure) whose body is a single expression can omit the `return` keyword entirely — the expression's value is returned implicitly. A function returning nothing can similarly omit `-> Void`, since it's the implicit default.",
      },
      {
        id: "q2",
        prompt: "In `func move(from start: Int, to end: Int)`, what are `from`/`to` versus `start`/`end`?",
        options: [
          { id: "a", text: "They're identical and interchangeable — Swift doesn't distinguish between them" },
          { id: "b", text: "`from`/`to` are external argument labels used at the call site; `start`/`end` are internal parameter names used inside the function body" },
          { id: "c", text: "`start`/`end` are used at the call site; `from`/`to` are used inside the body" },
          { id: "d", text: "`from`/`to` are optional and can be omitted entirely at the call site" },
        ],
        correctOptionId: "b",
        explanation: "Swift functions have two names per parameter: an external label (what callers write, e.g. `move(from: 0, to: 10)`) and an internal name (what the function body uses internally, e.g. `start`, `end`). This dual-naming lets APIs read naturally at the call site while using clear short names internally.",
      },
      {
        id: "q3",
        prompt: "What does writing `_` as a parameter's external label accomplish, e.g. `func square(_ number: Int) -> Int`?",
        options: [
          { id: "a", text: "It makes the parameter optional" },
          { id: "b", text: "It suppresses the external label entirely, so callers write `square(5)` instead of `square(number: 5)`" },
          { id: "c", text: "It hides the parameter from autocomplete only" },
          { id: "d", text: "It makes the parameter variadic" },
        ],
        correctOptionId: "b",
        explanation: "`_` before a parameter name removes the requirement for an external label at the call site — useful when the label would be redundant, as with `square(5)` where writing `square(number: 5)` would add no clarity.",
      },
      {
        id: "q4",
        prompt: "Given `func greet(name: String, greeting: String = \"Hello\") -> String`, what happens if you call `greet(name: \"Nahidul\")`?",
        options: [
          { id: "a", text: "It's a compile-time error — all parameters must be supplied" },
          { id: "b", text: "It compiles fine, using `\"Hello\"` automatically for `greeting` since it has a default value" },
          { id: "c", text: "It compiles but crashes at runtime because `greeting` is unset" },
          { id: "d", text: "`greeting` becomes `nil`" },
        ],
        correctOptionId: "b",
        explanation: "Parameters with a default value can be omitted entirely by the caller — Swift substitutes the default automatically. Defaults can even reference other parameters or call other functions to compute a value.",
      },
      {
        id: "q5",
        prompt: "In `func sum(_ numbers: Int...) -> Int`, what type is `numbers` exposed as inside the function body?",
        options: [
          { id: "a", text: "A single `Int`" },
          { id: "b", text: "`[Int]`, a plain array" },
          { id: "c", text: "`Int?`" },
          { id: "d", text: "A tuple of unknown length" },
        ],
        correctOptionId: "b",
        explanation: "A variadic parameter (marked with `...`) accepts zero or more values of the same type at the call site, and is exposed inside the function body as a regular array of that type — here, `[Int]`. A function can have at most one variadic parameter.",
      },
      {
        id: "q6",
        prompt: "Why must you write `increment(&counter)` with an explicit `&` when calling `func increment(_ value: inout Int)`?",
        options: [
          { id: "a", text: "It's purely decorative and has no actual meaning" },
          { id: "b", text: "It makes the potential mutation visible right at the call site, and is required syntax for passing an argument by reference to an `inout` parameter" },
          { id: "c", text: "It converts `counter` to an optional" },
          { id: "d", text: "It's only required when `counter` is a global variable" },
        ],
        correctOptionId: "b",
        explanation: "`inout` parameters are passed by reference rather than by value, meaning changes inside the function propagate back to the caller's variable. The explicit `&` is a deliberate design choice — it makes potential mutation visible at the call site, unlike languages where reference semantics are invisible.",
      },
      {
        id: "q7",
        prompt: "Can you pass a `let` constant as an argument to an `inout` parameter?",
        options: [
          { id: "a", text: "Yes, always" },
          { id: "b", text: "No — `inout` requires a mutable variable (`var`); passing a `let` constant is a compile-time error" },
          { id: "c", text: "Only if the constant is of type `Int`" },
          { id: "d", text: "Yes, but only inside closures" },
        ],
        correctOptionId: "b",
        explanation: "Since `inout` allows the function to mutate the caller's variable, the argument must be something that can actually be mutated — a `var`. Passing a `let` constant produces the compile-time error \"cannot pass immutable value as inout argument.\"",
      },
      {
        id: "q8",
        prompt: "What is the benefit of returning a named tuple like `(min: Int, max: Int)` from a function, over an unnamed one like `(Int, Int)`?",
        options: [
          { id: "a", text: "There's no difference — names are purely cosmetic and invisible even in code" },
          { id: "b", text: "Named tuple elements make the call site self-documenting, letting you write `result.min`/`result.max` instead of the less clear `result.0`/`result.1`" },
          { id: "c", text: "Named tuples perform better at runtime" },
          { id: "d", text: "Only named tuples can be returned from a function; unnamed tuples cannot" },
        ],
        correctOptionId: "b",
        explanation: "Named tuple elements are a lightweight, zero-cost way to make a multi-value return type self-explanatory — `bounds.min` is far clearer to a reader than `bounds.0`, though both work identically under the hood.",
      },
      {
        id: "q9",
        prompt: "What does the `@discardableResult` attribute do?",
        options: [
          { id: "a", text: "It causes the function to always return `Void`" },
          { id: "b", text: "It silences the compiler's default warning about an unused non-`Void` return value, for functions where ignoring the result is a legitimate, common choice" },
          { id: "c", text: "It removes the return value entirely at compile time" },
          { id: "d", text: "It forces callers to always use the return value" },
        ],
        correctOptionId: "b",
        explanation: "By default, Swift warns when you call a function and ignore its non-`Void` return value. `@discardableResult` opts a function out of that specific warning — for example, `removeLast()` returns the removed element, but you often don't need to capture it.",
      },
      {
        id: "q10",
        prompt: "What is true about a nested function, e.g. a `subtotal()` function defined inside `processOrder(...)`?",
        options: [
          { id: "a", text: "It's visible and callable from anywhere in the file, just like a top-level function" },
          { id: "b", text: "It's only visible within its enclosing function, and it automatically captures variables from the enclosing scope, just like a closure" },
          { id: "c", text: "It cannot access any variables from the enclosing function" },
          { id: "d", text: "Nested functions are not allowed in Swift" },
        ],
        correctOptionId: "b",
        explanation: "Nested functions are scoped entirely to their enclosing function — invisible outside it — and they capture the enclosing function's variables automatically, behaving essentially like named closures. This is exactly how the `makeCounter()` pattern (returning a nested function that keeps incrementing a captured `count`) works.",
      },
      {
        id: "q11",
        prompt: "Which of these closure expressions is equivalent to `numbers.sorted(by: { (a: Int, b: Int) -> Bool in return a < b })`?",
        options: [
          { id: "a", text: "`numbers.sorted(by: { $0 < $1 })` only" },
          { id: "b", text: "`numbers.sorted(by: <)` only" },
          { id: "c", text: "Both `numbers.sorted(by: { $0 < $1 })` and `numbers.sorted(by: <)` are equivalent, along with several other progressively shortened forms" },
          { id: "d", text: "None of the shortened forms are actually equivalent — they behave differently" },
        ],
        correctOptionId: "c",
        explanation: "Swift lets you progressively drop redundant syntax from a closure — explicit types, the `return` keyword for single-expression bodies, and even the parameter list itself (using `$0`/`$1` shorthand, or passing an operator function like `<` directly) — all while producing identical behavior.",
      },
      {
        id: "q12",
        prompt: "What does trailing closure syntax allow you to do, e.g. `numbers.sorted { $0 < $1 }` instead of `numbers.sorted(by: { $0 < $1 })`?",
        options: [
          { id: "a", text: "It changes the closure's behavior to run asynchronously" },
          { id: "b", text: "It moves a function's final (or only) closure argument outside the parentheses, dropping its label — the idiomatic, common way closures appear in real Swift code" },
          { id: "c", text: "It's only valid for closures with zero parameters" },
          { id: "d", text: "It requires the closure to return `Void`" },
        ],
        correctOptionId: "b",
        explanation: "When a closure is a function's last (or sole) argument, Swift allows it to be written outside the call's parentheses, with the label dropped. If it's the only argument, the parentheses themselves can be omitted entirely, as in `numbers.sorted { $0 < $1 }`.",
      },
      {
        id: "q13",
        prompt: "How does Swift handle a function that needs **multiple** trailing closures, such as `fetchData { ... } onFailure: { ... }`?",
        options: [
          { id: "a", text: "This has never been supported in Swift" },
          { id: "b", text: "Since Swift 5.3, additional trailing closures after the first are each labeled at the closure boundary" },
          { id: "c", text: "Only the first trailing closure can ever have a label" },
          { id: "d", text: "Multiple trailing closures must all be unlabeled" },
        ],
        correctOptionId: "b",
        explanation: "Swift 5.3 introduced support for multiple trailing closures: the first one can be unlabeled (matching the parameter it's assigned to), and each subsequent trailing closure is written with its own label directly before its braces, e.g. `onFailure: { error in ... }`.",
      },
      {
        id: "q14",
        prompt: "In `numbers.map { $0 * 2 }`, what does `$0` refer to?",
        options: [
          { id: "a", text: "The index of the current element" },
          { id: "b", text: "The first parameter of the closure — here, each element being mapped" },
          { id: "c", text: "A global counter variable" },
          { id: "d", text: "The entire array being mapped over" },
        ],
        correctOptionId: "b",
        explanation: "`$0`, `$1`, `$2`, etc. are shorthand names referring to a closure's parameters positionally, letting you skip declaring named parameters for short, self-evident closures. `$0` is the first (and, in a single-parameter closure like this one, only) parameter.",
      },
      {
        id: "q15",
        prompt: "What does it mean that a closure \"captures\" a variable from its surrounding scope, as in `makeMultiplier(factor:)` returning a closure that uses `factor`?",
        options: [
          { id: "a", text: "The closure receives a frozen, one-time copy of the variable's value at creation time, always" },
          { id: "b", text: "By default, the closure keeps a live reference to the captured variable/constant, not a frozen snapshot — later mutations to a captured `var` are visible inside the closure" },
          { id: "c", text: "Captured variables are always converted to global variables" },
          { id: "d", text: "Closures cannot reference any variables outside their own parameter list" },
        ],
        correctOptionId: "b",
        explanation: "Captures are references by default, not copies — this is exactly why two closures sharing a captured `var total` (as in the `makeIncrementers()` example) can both observe and affect the same underlying value across multiple calls, not independent copies of it.",
      },
      {
        id: "q16",
        prompt: "What problem does `[weak self]` in a closure's capture list solve?",
        options: [
          { id: "a", text: "It makes the closure run on a background thread" },
          { id: "b", text: "It breaks a strong reference cycle that could otherwise occur when a class stores a closure that itself strongly captures `self`, keeping the object alive indefinitely" },
          { id: "c", text: "It converts `self` into a value type" },
          { id: "d", text: "It's required syntax for every closure, regardless of context" },
        ],
        correctOptionId: "b",
        explanation: "By default, closures capture reference types like `self` strongly, keeping them alive for the closure's lifetime. If a class stores such a closure as a property (e.g. `onComplete`), and that closure captures `self`, you get a retain cycle — neither object can ever be deallocated. `[weak self]` captures `self` as an optional, weak reference instead, breaking the cycle.",
      },
      {
        id: "q17",
        prompt: "Why must a closure parameter that's going to be stored and called later (e.g. as a network completion handler) be marked `@escaping`?",
        options: [
          { id: "a", text: "`@escaping` makes the closure run faster" },
          { id: "b", text: "By default, closures are non-escaping, meaning the compiler guarantees they'll be called (or discarded) before the function returns; `@escaping` explicitly allows the closure to be stored and potentially called after the function has already returned" },
          { id: "c", text: "`@escaping` is required for every closure parameter, with no exceptions" },
          { id: "d", text: "It converts the closure into a nested function" },
        ],
        correctOptionId: "b",
        explanation: "Non-escaping closures (the default) are guaranteed by the compiler not to outlive the function call they were passed into, enabling certain optimizations. `@escaping` is required whenever a closure might be stored in a property or called asynchronously after the function returns — like a completion handler saved for a later network callback.",
      },
      {
        id: "q18",
        prompt: "What is the purpose of `@autoclosure`, as used implicitly by the standard library's `??` operator?",
        options: [
          { id: "a", text: "It forces immediate evaluation of the expression before the function call happens" },
          { id: "b", text: "It automatically wraps a plain expression argument into a closure, deferring its evaluation, without requiring the caller to write explicit `{ }` braces" },
          { id: "c", text: "It converts the expression's result into an optional" },
          { id: "d", text: "It's identical in effect to `@escaping`" },
        ],
        correctOptionId: "b",
        explanation: "`@autoclosure` lets a function parameter accept a plain expression at the call site while treating it internally as a closure — deferring evaluation until (and unless) it's actually needed. This is exactly why the right-hand side of `??` is only evaluated when the left side is `nil`, without the caller needing to write `expensiveDefault` as `{ expensiveDefault() }`.",
      },
      {
        id: "q19",
        prompt: "What does it mean that \"closures are values\" in Swift, e.g. `let operation: (Int, Int) -> Int = add`?",
        options: [
          { id: "a", text: "It's not actually valid syntax — functions cannot be assigned to variables" },
          { id: "b", text: "Named functions are just closures with a name attached, so they (and closures generally) can be assigned to constants, passed as arguments, stored in arrays, and returned from other functions, just like an `Int` or `String`" },
          { id: "c", text: "Only closures created with `{ }` syntax can be treated this way; named functions cannot" },
          { id: "d", text: "This only works for functions with zero parameters" },
        ],
        correctOptionId: "b",
        explanation: "Functions and closures are first-class values in Swift — a named function's identifier (without call parentheses) refers to the function itself, which can be stored, passed around, and invoked later, exactly like any other value of a matching function type.",
      },
      {
        id: "q20",
        prompt: "What defines a \"higher-order function\"?",
        options: [
          { id: "a", text: "Any function marked `@escaping`" },
          { id: "b", text: "A function that either takes another function/closure as a parameter, or returns one — like the standard library's `map`, or a custom `repeatAction(times:action:)`" },
          { id: "c", text: "Only functions belonging to a class hierarchy" },
          { id: "d", text: "A function that must return a tuple" },
        ],
        correctOptionId: "b",
        explanation: "Higher-order functions operate on other functions, either by accepting them as parameters (like `repeatAction(times:action:)` calling its `action` closure repeatedly) or by returning them (like `makeValidator(minimumLength:)` returning a configured validator closure). `map`, `filter`, and `reduce` are all higher-order functions from the standard library, but you can write your own with exactly the same shape.",
      },
    ],
  },
  {
    id: "structs-classes-enums-quiz",
    title: "Structs, Classes, and Enums Quiz",
    description: "20 questions on struct value semantics, class inheritance and initializers, deinit, computed properties, property observers, static members, subscripts, and enums with raw or associated values.",
    relatedArticleSlug: "structs-classes-enums",
    questions: [
      {
        id: "q1",
        prompt: "What initializer does Swift automatically generate for a struct that defines no initializer of its own?",
        options: [
          { id: "a", text: "A no-argument `init()` only" },
          { id: "b", text: "A memberwise initializer with one parameter per stored property, in declaration order" },
          { id: "c", text: "None — structs always require a manually written initializer" },
          { id: "d", text: "A `required init` inherited from `Any`" },
        ],
        correctOptionId: "b",
        explanation: "Swift synthesizes a free memberwise initializer for any struct without a custom `init`, taking one parameter per stored property in the order they're declared. The moment you add your own initializer, this automatic one disappears.",
      },
      {
        id: "q2",
        prompt: "Why must `func increment()` be marked `mutating` inside `struct Counter { var count = 0; mutating func increment() { count += 1 } }`?",
        options: [
          { id: "a", text: "`mutating` is required on every method regardless of behavior" },
          { id: "b", text: "Struct methods are non-mutating by default since structs are value types passed by copy; any method that changes `self`'s properties must opt in with `mutating`" },
          { id: "c", text: "It's only required for methods with a return value" },
          { id: "d", text: "`mutating` is deprecated and should be avoided" },
        ],
        correctOptionId: "b",
        explanation: "Because structs are copied on assignment, methods can't silently mutate properties unless explicitly marked `mutating` — this makes potential mutation visible in the method's own signature, and it's also why calling a mutating method on a `let`-declared struct is a compile-time error.",
      },
      {
        id: "q3",
        prompt: "What must a subclass do when overriding an inherited method?",
        options: [
          { id: "a", text: "Nothing special — just redefine the method with the same signature" },
          { id: "b", text: "Use the `override` keyword, which is mandatory in Swift" },
          { id: "c", text: "Use the `super` keyword instead of the method name" },
          { id: "d", text: "Mark the new method `final`" },
        ],
        correctOptionId: "b",
        explanation: "Swift requires the explicit `override` keyword any time a subclass redefines an inherited method or property — this prevents accidental shadowing and makes the intent to override clearly visible at the call site and in code review.",
      },
      {
        id: "q4",
        prompt: "What does marking a class or method `final` accomplish?",
        options: [
          { id: "a", text: "It makes the class immutable" },
          { id: "b", text: "It prevents further subclassing (on a class) or further overriding (on a method/property), which also lets the compiler apply certain optimizations" },
          { id: "c", text: "It automatically generates a memberwise initializer" },
          { id: "d", text: "It converts the class into a struct" },
        ],
        correctOptionId: "b",
        explanation: "`final` is a deliberate stop sign — `final class Robot` cannot be subclassed at all, and `final func vin()` cannot be overridden by any subclass. Since the compiler no longer needs to account for possible overrides, it can sometimes generate more efficient code at the call site.",
      },
      {
        id: "q5",
        prompt: "What is required of a `convenience` initializer in a class?",
        options: [
          { id: "a", text: "It must set every stored property directly, just like a designated initializer" },
          { id: "b", text: "It must delegate to another initializer within the same class rather than setting stored properties directly" },
          { id: "c", text: "It can only be used in structs, not classes" },
          { id: "d", text: "It must always take zero parameters" },
        ],
        correctOptionId: "b",
        explanation: "Convenience initializers exist to reduce duplicated setup logic — they're required to call another initializer defined in the same class (ultimately reaching a designated initializer), rather than assigning stored properties themselves.",
      },
      {
        id: "q6",
        prompt: "Why does a subclass typically need to define its own initializer if it adds new stored properties?",
        options: [
          { id: "a", text: "Subclasses can never have stored properties of their own" },
          { id: "b", text: "A subclass doesn't automatically inherit its superclass's designated initializers once it adds new stored properties or defines its own designated initializers — it must ensure all its own properties are initialized and then call `super.init`" },
          { id: "c", text: "Swift requires the exact same initializer signature across the whole class hierarchy" },
          { id: "d", text: "`required init` is mandatory the moment any subclass exists" },
        ],
        correctOptionId: "b",
        explanation: "Swift's initializer inheritance rules mean a subclass with new stored properties must provide an initializer that sets those properties and calls the superclass's designated initializer (via `super.init`) — inherited initializers aren't automatically available in that situation.",
      },
      {
        id: "q7",
        prompt: "What does marking an initializer `required` guarantee?",
        options: [
          { id: "a", text: "That the initializer runs automatically at app launch" },
          { id: "b", text: "That every subclass must provide its own implementation of that initializer, which is essential when constructing instances of a dynamically-determined subclass type" },
          { id: "c", text: "That the initializer can never take parameters" },
          { id: "d", text: "That the initializer is only callable from Objective-C code" },
        ],
        correctOptionId: "b",
        explanation: "`required init` forces every subclass down the hierarchy to implement that initializer, which matters for generic or framework code that needs to construct an instance of an unknown subclass type (e.g. `T(...)` where `T: SomeBaseClass`), relying on the guarantee that the initializer genuinely exists.",
      },
      {
        id: "q8",
        prompt: "When does a class's `deinit` run, and do structs have one?",
        options: [
          { id: "a", text: "`deinit` runs automatically right before a class instance is deallocated; structs don't have `deinit`, since they have no separate identity to tear down" },
          { id: "b", text: "`deinit` must be called manually by the developer; both structs and classes support it" },
          { id: "c", text: "`deinit` runs at app launch for all classes" },
          { id: "d", text: "`deinit` is identical to a static initializer" },
        ],
        correctOptionId: "a",
        explanation: "`deinit` is a special, parameterless method invoked automatically by ARC right before a class instance's memory is reclaimed — it's the standard place to release manually-managed resources tied to that instance's lifetime. Structs and enums, having no separate reference identity, don't have or need `deinit`.",
      },
      {
        id: "q9",
        prompt: "Given `var s1 = PointStruct(); var s2 = s1; s2.x = 100`, what happens to `s1.x`?",
        options: [
          { id: "a", text: "`s1.x` also becomes `100`, since `s2` is the same underlying value" },
          { id: "b", text: "`s1.x` remains unchanged, because assigning a struct copies its value — `s1` and `s2` are now independent" },
          { id: "c", text: "It's a compile-time error to copy a struct this way" },
          { id: "d", text: "`s1.x` becomes `nil`" },
        ],
        correctOptionId: "b",
        explanation: "Structs have value semantics: `var s2 = s1` creates a fully independent copy, so mutating `s2` afterward has no effect on `s1`. This is the core contrast with classes, where the same assignment would make both variables reference the same object.",
      },
      {
        id: "q10",
        prompt: "Given `let c1 = PointClass(); let c2 = c1; c2.x = 100`, why does this compile and affect `c1.x` too, even though both are declared with `let`?",
        options: [
          { id: "a", text: "It shouldn't compile — this is a trick question" },
          { id: "b", text: "`let` on a class reference only prevents reassigning which object the constant points to; it doesn't prevent mutating the properties of the object itself, and `c1`/`c2` refer to the very same instance" },
          { id: "c", text: "Classes ignore `let`/`var` entirely" },
          { id: "d", text: "`c2.x = 100` silently creates a new copy without affecting `c1`" },
        ],
        correctOptionId: "b",
        explanation: "`let c1 = PointClass()` fixes which object `c1` refers to, not the mutability of that object's own properties. Since `c2 = c1` copies the *reference* (both point at the same instance), mutating through `c2` is visible through `c1` as well — both print `100` afterward.",
      },
      {
        id: "q11",
        prompt: "According to standard Swift guidance, what should you default to when choosing between `struct` and `class` for a new type?",
        options: [
          { id: "a", text: "Always use `class`, since it supports inheritance" },
          { id: "b", text: "Default to `struct`, and reach for `class` only when you specifically need identity, shared mutable state, inheritance, or Objective-C/UIKit interop" },
          { id: "c", text: "Always use `class` for anything with more than one property" },
          { id: "d", text: "It doesn't matter — they behave identically in every case" },
        ],
        correctOptionId: "b",
        explanation: "Apple's own guidance (and most Swift codebases) default to `struct` for data models, since value semantics avoid a whole class of shared-mutation bugs. `class` is reserved for cases with a specific, deliberate reason — identity that matters, shared observed state, inheritance hierarchies, or required interop.",
      },
      {
        id: "q12",
        prompt: "What distinguishes a computed property from a regular stored property?",
        options: [
          { id: "a", text: "Computed properties can only be of type `Int`" },
          { id: "b", text: "A computed property has no storage of its own — its value is calculated from other properties every time it's accessed, via `get` (and optionally `set`)" },
          { id: "c", text: "Computed properties are always `static`" },
          { id: "d", text: "There is no real difference — they're just alternate syntax for the same thing" },
        ],
        correctOptionId: "b",
        explanation: "A computed property (like `Rectangle.area` computing `width * height`) never stores a value directly; it recomputes on every access. Adding a `set` block lets assignment to the computed property drive changes back into the underlying stored properties (as with `Temperature.fahrenheit`).",
      },
      {
        id: "q13",
        prompt: "What do `willSet` and `didSet` provide inside their respective blocks?",
        options: [
          { id: "a", text: "`willSet` provides `oldValue`; `didSet` provides `newValue`" },
          { id: "b", text: "`willSet` implicitly provides `newValue` (the value about to be set); `didSet` implicitly provides `oldValue` (the value just replaced)" },
          { id: "c", text: "Both provide only the property's current value, with no distinction" },
          { id: "d", text: "They require you to manually declare `newValue`/`oldValue` as parameters" },
        ],
        correctOptionId: "b",
        explanation: "`willSet` runs immediately before a stored property's value changes and exposes the incoming value as `newValue`; `didSet` runs immediately after and exposes the just-replaced value as `oldValue`. Neither fires during the property's initial assignment inside `init`.",
      },
      {
        id: "q14",
        prompt: "What is distinctive about a `lazy` stored property?",
        options: [
          { id: "a", text: "It's computed once at `init` time, exactly like any other stored property" },
          { id: "b", text: "Its initial value isn't computed until the first time it's actually accessed, and that computed value is then cached for subsequent accesses" },
          { id: "c", text: "It must be a computed property, never a stored one" },
          { id: "d", text: "It automatically synchronizes across threads" },
        ],
        correctOptionId: "b",
        explanation: "`lazy var importer = DataImporter()` defers creating the `DataImporter` instance until `manager.importer` is first read — useful for expensive setup that might never be needed. Note it must be declared `var` and isn't automatically thread-safe for concurrent first access.",
      },
      {
        id: "q15",
        prompt: "What is the key difference between a `static` method and a `class` method on a Swift type?",
        options: [
          { id: "a", text: "They are exactly identical in every context" },
          { id: "b", text: "Both belong to the type rather than an instance, but `class` methods can be overridden by subclasses, while `static` methods are implicitly `final` and cannot be overridden" },
          { id: "c", text: "`static` methods can only be used inside enums" },
          { id: "d", text: "`class` methods cannot access other type-level members" },
        ],
        correctOptionId: "b",
        explanation: "Both `static` and `class` define type-level (rather than instance-level) members, but only `class` allows a subclass to `override` that member — `static` members behave as if they were implicitly `final`.",
      },
      {
        id: "q16",
        prompt: "What Swift language feature underlies the ability to write `array[index]` or `dict[key]`?",
        options: [
          { id: "a", text: "Special-cased compiler syntax unavailable to custom types" },
          { id: "b", text: "`subscript` definitions — the exact same mechanism you can define yourself on custom types with `get`/`set`" },
          { id: "c", text: "Computed properties named `subscript`" },
          { id: "d", text: "Operator overloading of `[]` specifically for built-in types only" },
        ],
        correctOptionId: "b",
        explanation: "`subscript(...)` is a general-purpose language feature, not something reserved for built-in types — `Array` and `Dictionary`'s bracket syntax are themselves just subscript definitions in the standard library, and you can define your own on any custom type, as shown with `Grid`.",
      },
      {
        id: "q17",
        prompt: "What does `Direction(rawValue: \"south\")` return for `enum Direction: String { case north, south, east, west }`?",
        options: [
          { id: "a", text: "`Direction.south` directly, non-optional" },
          { id: "b", text: "`Direction?` — specifically `Optional(Direction.south)`, since the raw-value initializer is failable" },
          { id: "c", text: "A runtime crash if the string doesn't match a case name exactly" },
          { id: "d", text: "Always `nil`, since raw values must be integers" },
        ],
        correctOptionId: "b",
        explanation: "The compiler-synthesized `init?(rawValue:)` for a raw-value enum is failable, returning an optional — `nil` if no case matches that raw value, or the matching case wrapped in `Optional` otherwise. This handles the possibility of malformed input (e.g. from decoding external data) gracefully.",
      },
      {
        id: "q18",
        prompt: "Why can't `enum NetworkResult { case success(data: String); case failure(code: Int, message: String) }` conform to `CaseIterable` automatically?",
        options: [
          { id: "a", text: "`CaseIterable` is only for structs" },
          { id: "b", text: "Cases with associated values have effectively infinite possible instances (any `String`, any `Int`/`String` pair), so there's no fixed, enumerable list of \"all cases\" the compiler could synthesize" },
          { id: "c", text: "`CaseIterable` requires at least three cases" },
          { id: "d", text: "`CaseIterable` doesn't exist in modern Swift" },
        ],
        correctOptionId: "b",
        explanation: "`CaseIterable`'s synthesized `allCases` only works for enums whose cases carry no associated values, since only then is there a genuinely finite, enumerable set of distinct case values. A case like `.success(data: String)` can hold any string, making \"all cases\" undefined.",
      },
      {
        id: "q19",
        prompt: "Why is the `indirect` keyword necessary for an enum case like `case addition(Expression, Expression)` inside `enum Expression`?",
        options: [
          { id: "a", text: "It's purely stylistic and has no functional effect" },
          { id: "b", text: "Without it, the enum would need infinite storage size at compile time, since a case would directly contain another instance of the same enum type; `indirect` stores that case behind a pointer instead" },
          { id: "c", text: "`indirect` is required for every enum, regardless of recursion" },
          { id: "d", text: "It converts the enum into a class" },
        ],
        correctOptionId: "b",
        explanation: "A non-indirect enum case can't hold a value of its own enum type inline, because the compiler must determine a fixed size for the type — a directly recursive case would require infinite storage. `indirect` (applied to the whole enum or just specific cases) stores the recursive case's associated data behind a pointer, breaking the infinite-size problem.",
      },
      {
        id: "q20",
        prompt: "What problem does modeling screen state with an enum (e.g. `enum ScreenState { case idle, loading, loaded([String]), failed(String) }`) solve, compared to a set of independent boolean flags?",
        options: [
          { id: "a", text: "Enums are simply faster at runtime than booleans" },
          { id: "b", text: "It makes illegal state combinations unrepresentable — you can only ever be in exactly one case at a time, unlike independent booleans (`isLoading`, `hasError`, etc.) which can be combined into nonsensical states the compiler can't catch" },
          { id: "c", text: "Enums automatically persist to disk, while booleans do not" },
          { id: "d", text: "There's no real difference — this is purely a stylistic preference" },
        ],
        correctOptionId: "b",
        explanation: "A pile of independent booleans (`isLoading`, `hasError`, `errorMessage`, `items`) allows combinations that don't make logical sense (e.g. loading and showing an error at once). An enum with associated values collapses these into mutually exclusive cases, so the type system itself prevents impossible combinations — a core \"make illegal states unrepresentable\" architecture principle.",
      },
    ],
  },
  {
    id: "protocols-and-extensions-quiz",
    title: "Protocols and Extensions Quiz",
    description: "20 questions on protocol requirements, composition, extensions, protocol extensions with default implementations, conditional conformance, Equatable/Hashable/Comparable, and witness vs static dispatch.",
    relatedArticleSlug: "protocols-and-extensions",
    questions: [
      {
        id: "q1",
        prompt: "What does a protocol provide to a conforming type?",
        options: [
          { id: "a", text: "A default implementation for every requirement automatically" },
          { id: "b", text: "A contract of property and method requirements, without providing the implementation itself" },
          { id: "c", text: "A guaranteed stored property layout" },
          { id: "d", text: "Automatic `Codable` conformance" },
        ],
        correctOptionId: "b",
        explanation: "A protocol declares *what* a conforming type must provide — properties and method signatures — without supplying the actual implementation. The conforming struct, class, or enum is responsible for providing real behavior that satisfies those requirements.",
      },
      {
        id: "q2",
        prompt: "What does `var speed: Double { get set }` in a protocol require of a conforming type?",
        options: [
          { id: "a", text: "Only a computed, read-only property" },
          { id: "b", text: "A property that supports both reading and writing — a stored `var` or a computed property with both `get` and `set` would satisfy this" },
          { id: "c", text: "A `let` constant only" },
          { id: "d", text: "Nothing — `{ get set }` is purely documentation with no compile-time enforcement" },
        ],
        correctOptionId: "b",
        explanation: "`{ get set }` requires the conforming type to support both reading and writing that property. A plain stored `var` naturally satisfies this, as would a computed property that implements both `get` and `set`. A `let` constant would not satisfy it, since it can't be written to after initialization.",
      },
      {
        id: "q3",
        prompt: "What does `func printProfile(_ value: Named & Aged)` express using protocol composition?",
        options: [
          { id: "a", text: "That `value` must conform to a new protocol that combines `Named` and `Aged`" },
          { id: "b", text: "That `value` must conform to *both* `Named` and `Aged` simultaneously, without needing a dedicated combined protocol declared anywhere" },
          { id: "c", text: "That `value` must conform to either `Named` or `Aged`, but not necessarily both" },
          { id: "d", text: "That `Named` and `Aged` are mutually exclusive and this is a compile-time error" },
        ],
        correctOptionId: "b",
        explanation: "The `&` operator composes multiple protocol requirements at the point of use, requiring conformance to all of them at once — without needing to declare a separate combined protocol. This is more flexible than protocol inheritance for one-off requirements at a function boundary.",
      },
      {
        id: "q4",
        prompt: "Which of the following can an `extension` add to an existing type?",
        options: [
          { id: "a", text: "New stored properties" },
          { id: "b", text: "New computed properties, methods, initializers, subscripts, and protocol conformances" },
          { id: "c", text: "A change to the type's underlying memory layout" },
          { id: "d", text: "A new case if the type is an enum" },
        ],
        correctOptionId: "b",
        explanation: "Extensions can add computed properties, methods, initializers, subscripts, and protocol conformances to a type — even one you don't own the source of, like `Int`. They cannot add stored properties, since that would require altering the type's compiled memory layout after the fact.",
      },
      {
        id: "q5",
        prompt: "What happens when you write `func greet() -> String { \"Default greeting\" }` inside `extension Greetable { ... }`, where `greet()` is a declared requirement of the `Greetable` protocol?",
        options: [
          { id: "a", text: "It's a compile-time error — extensions can't touch protocol requirements" },
          { id: "b", text: "It supplies a default implementation that any conforming type automatically gets, unless that type provides its own implementation" },
          { id: "c", text: "It forces every conforming type to use exactly this implementation, with no ability to override" },
          { id: "d", text: "It only applies to structs, never classes" },
        ],
        correctOptionId: "b",
        explanation: "Extending a protocol itself lets you supply default implementations for its requirements. Conforming types automatically inherit that default behavior but remain free to override it with their own implementation if they need different behavior.",
      },
      {
        id: "q6",
        prompt: "What does conditional conformance, like `extension Array: Summable where Element: Summable`, express?",
        options: [
          { id: "a", text: "That every `Array` unconditionally conforms to `Summable`" },
          { id: "b", text: "That `Array` conforms to `Summable` only when its `Element` type parameter itself conforms to `Summable`" },
          { id: "c", text: "That `Array` can never conform to `Summable` under any circumstances" },
          { id: "d", text: "That this syntax is invalid — conditional conformance doesn't exist in Swift" },
        ],
        correctOptionId: "b",
        explanation: "Conditional conformance lets a generic type's protocol conformance depend on its type parameter also satisfying some requirement. This is exactly how `Array<Element>` conforms to `Equatable` only when `Element` itself is `Equatable` — an `[Int]` can use `==`, but an array of a non-`Equatable` type cannot.",
      },
      {
        id: "q7",
        prompt: "For a simple struct like `struct Point: Equatable { var x: Int; var y: Int }`, do you need to implement `==` yourself?",
        options: [
          { id: "a", text: "Yes, always — Swift never synthesizes `==`" },
          { id: "b", text: "No — Swift automatically synthesizes `==` for structs/enums whose members are all themselves `Equatable`" },
          { id: "c", text: "Only if the struct has more than two properties" },
          { id: "d", text: "Only for classes, never for structs" },
        ],
        correctOptionId: "b",
        explanation: "Swift synthesizes `==` automatically for structs and enums as long as every stored property/associated value is itself `Equatable` — comparing all members for equality. You only need to write `==` yourself when you want custom logic, such as comparing by only some properties.",
      },
      {
        id: "q8",
        prompt: "What critical rule must be maintained when implementing custom `hash(into:)` alongside a custom `==` for `Hashable` conformance?",
        options: [
          { id: "a", text: "There is no relationship between the two — they can be implemented completely independently" },
          { id: "b", text: "If two values are considered equal by `==`, they must produce the same hash value; violating this breaks `Set`/`Dictionary` behavior in ways that are hard to debug" },
          { id: "c", text: "`hash(into:)` must always ignore every property that `==` compares" },
          { id: "d", text: "`Hashable` doesn't actually require `Equatable`" },
        ],
        correctOptionId: "b",
        explanation: "`Hashable` requires `Equatable`, and the contract between them is strict: equal values must hash identically. If you customize `==` to compare only a subset of properties (like just `id`), your `hash(into:)` must hash that same subset — otherwise `Set` and `Dictionary` lookups can silently fail or fail to deduplicate correctly.",
      },
      {
        id: "q9",
        prompt: "What is required to make a custom type usable with `sorted()` without passing a custom closure?",
        options: [
          { id: "a", text: "Conformance to `Equatable` only" },
          { id: "b", text: "Conformance to `Comparable`, which requires implementing `<` (Swift derives `>`, `<=`, `>=` from it, along with `Equatable`'s requirements)" },
          { id: "c", text: "Conformance to `Hashable`" },
          { id: "d", text: "Nothing — every custom type supports `sorted()` automatically" },
        ],
        correctOptionId: "b",
        explanation: "`Comparable` (which itself requires `Equatable`) is the protocol that unlocks ordering operators and the closure-free `sorted()` overload. You implement `<` yourself; the rest of the ordering operators are derived from it.",
      },
      {
        id: "q10",
        prompt: "What is the key difference between `Identifiable`'s `id` and `Equatable`'s `==` for a type like `Task`?",
        options: [
          { id: "a", text: "They are functionally identical — both check if two values are exactly the same" },
          { id: "b", text: "`id` provides a stable identity that can persist even as a value's other properties change over time (e.g. `isDone` flips), while `==` compares the *current* values of a type's properties" },
          { id: "c", text: "`Identifiable` requires the id to be a `String`, always" },
          { id: "d", text: "`Equatable` is only usable in SwiftUI, while `Identifiable` is used everywhere else" },
        ],
        correctOptionId: "b",
        explanation: "`Identifiable` is about tracking \"is this the same underlying thing over time,\" independent of whatever its current property values are — crucial for SwiftUI's `List`/`ForEach` to track item identity across state changes. `Equatable` compares snapshots of current values, which is a different concern entirely.",
      },
      {
        id: "q11",
        prompt: "What does conforming to `CustomStringConvertible` and implementing `description` control?",
        options: [
          { id: "a", text: "How the type is stored in memory" },
          { id: "b", text: "What gets printed by `print()` or embedded via string interpolation (`\"\\(value)\"`) for that type" },
          { id: "c", text: "Whether the type can be compared with `==`" },
          { id: "d", text: "The type's `Hashable` conformance" },
        ],
        correctOptionId: "b",
        explanation: "`CustomStringConvertible`'s `description` property directly controls what `print()` shows and what string interpolation embeds for that type, replacing Swift's default, often unhelpful auto-generated description with something more meaningful.",
      },
      {
        id: "q12",
        prompt: "In the `Greeter`/`FriendlyGreeter` example, why does `abstracted.farewell()` (where `abstracted: Greeter`) print the protocol extension's default text instead of `FriendlyGreeter`'s own `farewell()`?",
        options: [
          { id: "a", text: "`FriendlyGreeter`'s `farewell()` was never actually called anywhere, so this is a bug" },
          { id: "b", text: "Because `farewell()` was never declared as a protocol requirement, it dispatches statically based on the variable's *static* type (`Greeter`), not the object's actual runtime type — so it always resolves to the protocol extension's version when accessed through a protocol-typed reference" },
          { id: "c", text: "Because `Greeter` doesn't actually allow method overriding" },
          { id: "d", text: "Because `abstracted` and `concrete` are different objects entirely" },
        ],
        correctOptionId: "b",
        explanation: "Methods declared as protocol requirements use dynamic (witness table) dispatch, correctly resolving to the concrete type's implementation at runtime. Methods added only in a protocol extension, without being declared as requirements, use static dispatch — resolved at compile time based on the variable's declared (static) type, which is why `abstracted.farewell()` differs from `concrete.farewell()` even though both reference the same underlying `FriendlyGreeter` instance.",
      },
      {
        id: "q13",
        prompt: "What would fix the dispatch discrepancy in Q12, if dynamic (runtime-correct) behavior for `farewell()` was actually desired?",
        options: [
          { id: "a", text: "Nothing can fix it — this is a permanent limitation" },
          { id: "b", text: "Declaring `farewell()` as an actual requirement in the `Greeter` protocol itself, not just adding it in an extension" },
          { id: "c", text: "Marking `farewell()` as `final`" },
          { id: "d", text: "Removing the protocol extension's default implementation of `greet()`" },
        ],
        correctOptionId: "b",
        explanation: "Only methods declared as protocol requirements get dynamic witness-table dispatch. Promoting `farewell()` from an extension-only method to an actual protocol requirement would make calls through a `Greeter`-typed reference correctly resolve to each conforming type's own implementation.",
      },
      {
        id: "q14",
        prompt: "What does `@retroactive` signal when writing `extension SomeThirdPartyType: @retroactive Comparable { ... }`?",
        options: [
          { id: "a", text: "That the conformance will only apply at a future Swift version" },
          { id: "b", text: "An intentional, acknowledged conformance added from your module to a type you don't own, flagging to the compiler (and future readers) the risk that another module might add the same conformance incompatibly" },
          { id: "c", text: "That the conformance is temporary and will be removed automatically" },
          { id: "d", text: "That the type will be deprecated in a future release" },
        ],
        correctOptionId: "b",
        explanation: "Since Swift 6, adding a protocol conformance to a type you don't own (a \"retroactive conformance\") produces a warning by default, since two different modules could independently add conflicting conformances. `@retroactive` marks the choice as deliberate, acknowledged, and reviewed — it's purely a compiler/documentation annotation with no runtime effect.",
      },
      {
        id: "q15",
        prompt: "Can an `enum` conform to a protocol in Swift?",
        options: [
          { id: "a", text: "No — only structs and classes can conform to protocols" },
          { id: "b", text: "Yes — any of struct, class, or enum can conform to a protocol" },
          { id: "c", text: "Only if the enum has raw values" },
          { id: "d", text: "Only if the protocol has zero requirements" },
        ],
        correctOptionId: "b",
        explanation: "Protocol conformance in Swift is available to all custom type kinds — structs, classes, and enums alike — as long as the type provides implementations satisfying every requirement.",
      },
      {
        id: "q16",
        prompt: "What happens if you try to add a stored property inside an `extension`, e.g. `extension Int { var cachedValue: Int = 0 }`?",
        options: [
          { id: "a", text: "It compiles fine and works exactly like a normal stored property" },
          { id: "b", text: "It's a compile-time error — extensions must not contain stored properties, since that would require changing the type's memory layout after the fact" },
          { id: "c", text: "It silently becomes a computed property instead" },
          { id: "d", text: "It only works for reference types, not `Int`" },
        ],
        correctOptionId: "b",
        explanation: "Extensions cannot introduce new stored properties on existing types — computed properties are allowed (since they don't require additional storage), but stored properties would require altering the compiled type's memory layout, which extensions cannot do.",
      },
      {
        id: "q17",
        prompt: "Why might you extend a protocol to add a method that is *not* declared as one of its requirements, such as `shout()` in the `Greetable` example?",
        options: [
          { id: "a", text: "You can't do this — every method in a protocol extension must correspond to a declared requirement" },
          { id: "b", text: "It provides a free utility method, built purely in terms of the protocol's actual requirements, automatically available to every conforming type" },
          { id: "c", text: "It forces all conforming types to reimplement `shout()` themselves" },
          { id: "d", text: "It only works if the protocol has no other requirements at all" },
        ],
        correctOptionId: "b",
        explanation: "Protocol extensions can add entirely new methods that were never part of the protocol's declared requirement list — these become available \"for free\" on every conforming type, built using whatever the protocol's actual requirements (like `greet()` and `name`) provide.",
      },
      {
        id: "q18",
        prompt: "What does it mean that `Comparable` \"requires `Equatable`\"?",
        options: [
          { id: "a", text: "The two protocols are unrelated and this statement is false" },
          { id: "b", text: "Any type conforming to `Comparable` must also conform to `Equatable` — `Comparable` inherits from `Equatable`, since ordering relies on being able to determine equality too" },
          { id: "c", text: "`Equatable` is deprecated in favor of `Comparable`" },
          { id: "d", text: "`Comparable` only works on classes, while `Equatable` only works on structs" },
        ],
        correctOptionId: "b",
        explanation: "`Comparable` is declared as inheriting from `Equatable` in the standard library, meaning any `Comparable` type must also satisfy `Equatable`'s requirements — ordering (`<`) and equality (`==`) are treated as related, necessarily co-existing capabilities.",
      },
      {
        id: "q19",
        prompt: "In `struct User: Hashable { ... static func == ... func hash(into hasher: inout Hasher) { hasher.combine(id) } }`, why does `hash(into:)` only combine `id` and not `name`?",
        options: [
          { id: "a", text: "It's a mistake — `hash(into:)` must always hash every stored property" },
          { id: "b", text: "Because the custom `==` implementation only compares `id`, so the hash must be consistent with that — only hashing what equality actually considers, otherwise two `==`-equal users could produce different hashes" },
          { id: "c", text: "`name` cannot be hashed because it's a `String`" },
          { id: "d", text: "`Hasher.combine` only accepts one call per type" },
        ],
        correctOptionId: "b",
        explanation: "This directly enforces the equal-values-must-hash-equally rule from Q8: since `==` was customized to compare only `id`, `hash(into:)` must mirror that and hash only `id` too, keeping the two in sync so `Set`/`Dictionary` behave correctly.",
      },
      {
        id: "q20",
        prompt: "Which statement about protocol inheritance (e.g. `protocol Person: Named, Aged`) is accurate?",
        options: [
          { id: "a", text: "A protocol can inherit from at most one other protocol" },
          { id: "b", text: "A protocol can inherit from multiple other protocols at once, requiring conformers to satisfy the combined set of all inherited requirements plus its own" },
          { id: "c", text: "Protocol inheritance and protocol composition (`&`) are exactly the same feature with different syntax" },
          { id: "d", text: "Inherited protocol requirements don't need to be satisfied by conforming types" },
        ],
        correctOptionId: "b",
        explanation: "Unlike class inheritance (single superclass only), a protocol can inherit from several other protocols simultaneously, comma-separated. A type conforming to `Person` must satisfy `Named`'s requirement (`name`), `Aged`'s requirement (`age`), and `Person`'s own requirement (`introduce()`) all at once.",
      },
    ],
  },
  {
    id: "generics-quiz",
    title: "Generics Quiz",
    description: "20 questions on generic functions and types, type constraints, associated types, constrained extensions, opaque (some) vs existential (any) types, and phantom types.",
    relatedArticleSlug: "generics",
    questions: [
      {
        id: "q1",
        prompt: "What core problem do generics solve, as illustrated by `swapInts`, `swapStrings`, and `swapDoubles`?",
        options: [
          { id: "a", text: "They make functions run faster at runtime" },
          { id: "b", text: "They eliminate the need to write near-identical functions repeatedly, once per concrete type" },
          { id: "c", text: "They allow functions to have no return type" },
          { id: "d", text: "They remove the need for function parameters entirely" },
        ],
        correctOptionId: "b",
        explanation: "Without generics, supporting the same logic across multiple types requires writing a separate, nearly identical function for each one. Generics let you write the logic exactly once, with the compiler generating type-specific versions as needed.",
      },
      {
        id: "q2",
        prompt: "In `func swapValues<T>(_ a: inout T, _ b: inout T)`, what does `T` represent?",
        options: [
          { id: "a", text: "A specific, fixed type that must always be `Int`" },
          { id: "b", text: "A type parameter — a placeholder the compiler resolves based on the actual argument types at each call site" },
          { id: "c", text: "A protocol that `a` and `b` must conform to" },
          { id: "d", text: "An associated type belonging to a protocol" },
        ],
        correctOptionId: "b",
        explanation: "`T` is a generic type parameter — it isn't a concrete type itself, but a placeholder. When you call `swapValues(&x, &y)` with two `Int` variables, the compiler resolves `T` to `Int` for that call; with two `String` variables, it resolves to `String`.",
      },
      {
        id: "q3",
        prompt: "What are `Array<Element>` and `Optional<Wrapped>` examples of?",
        options: [
          { id: "a", text: "Special compiler primitives, not real generic types" },
          { id: "b", text: "Generic types defined in the standard library using the exact same generic type mechanism available to user code" },
          { id: "c", text: "Protocols with associated types, not generic types" },
          { id: "d", text: "Non-generic types that only look generic syntactically" },
        ],
        correctOptionId: "b",
        explanation: "There's nothing magical about `Array` or `Optional` — they're ordinary generic types (a struct and an enum, respectively) defined in the standard library using the same `<Element>`/`<Wrapped>` generic parameter syntax available to any Swift developer.",
      },
      {
        id: "q4",
        prompt: "Why does `func findLargest<T>(_ items: [T]) -> T?` need a constraint like `<T: Comparable>` to actually compile its `item > largest` comparison?",
        options: [
          { id: "a", text: "It doesn't need one — `>` works on any type in Swift" },
          { id: "b", text: "An unconstrained `T` could be *any* type, including ones with no `>` operator defined at all; the `Comparable` constraint guarantees `T` supports ordering comparisons" },
          { id: "c", text: "`Comparable` is required for all generic type parameters, regardless of usage" },
          { id: "d", text: "The constraint is purely stylistic and has no compile-time effect" },
        ],
        correctOptionId: "b",
        explanation: "Without a constraint, `T` could be filled in with literally any type — including ones that don't support `>` at all. Constraining `T: Comparable` tells the compiler that whatever concrete type fills in `T` is guaranteed to support ordering operators, making `item > largest` a legal expression to write generically.",
      },
      {
        id: "q5",
        prompt: "What is the purpose of a `where` clause on a generic function, e.g. `func allEqual<T: Sequence>(_ sequence: T) -> Bool where T.Element: Equatable`?",
        options: [
          { id: "a", text: "It's purely stylistic, identical to omitting it" },
          { id: "b", text: "It expresses additional, often more complex constraints — here, constraining the *associated type* of `T` (its `Element`) rather than `T` itself" },
          { id: "c", text: "It converts the function into a protocol requirement" },
          { id: "d", text: "It removes the need for a type parameter altogether" },
        ],
        correctOptionId: "b",
        explanation: "`where` clauses are especially useful when constraining an associated type or relationship between multiple generic parameters, as opposed to simple inline constraints on the parameter itself (`<T: Comparable>`). Here, it ensures the *elements* produced by the sequence are `Equatable`, not the sequence type itself.",
      },
      {
        id: "q6",
        prompt: "What is an `associatedtype` in a protocol, and who determines its actual concrete type?",
        options: [
          { id: "a", text: "It's a type parameter the *caller* of a function specifies explicitly, just like a generic function's `<T>`" },
          { id: "b", text: "It's a placeholder type declared by the protocol itself; the *conforming type* determines (and Swift usually infers) what concrete type fills it in, based on how that type implements the protocol's requirements" },
          { id: "c", text: "It must always be explicitly written as `associatedtype Item = Int`" },
          { id: "d", text: "It only exists for enums, not structs or classes" },
        ],
        correctOptionId: "b",
        explanation: "Unlike a generic function's type parameter (chosen by the caller at each call site), a protocol's associated type is filled in by whichever type conforms to the protocol — inferred automatically from that type's requirement implementations, as with `IntContainer`'s `add(_ item: Int)` implicitly fixing `Item` to `Int`.",
      },
      {
        id: "q7",
        prompt: "What does `extension Stack where Element: Equatable { func contains(_ item: Element) -> Bool { ... } }` accomplish?",
        options: [
          { id: "a", text: "It adds `contains` to every `Stack`, regardless of what `Element` is" },
          { id: "b", text: "It adds `contains` only to specializations of `Stack` whose `Element` type conforms to `Equatable`; a `Stack` of a non-`Equatable` type simply won't have this method available" },
          { id: "c", text: "It's a compile-time error, since extensions can't be constrained" },
          { id: "d", text: "It removes `Equatable` conformance from `Element`" },
        ],
        correctOptionId: "b",
        explanation: "Constrained extensions add functionality conditionally, only to specializations of a generic type that satisfy the stated constraint. `Stack<Int>.contains` exists because `Int: Equatable`, but `Stack<NotEquatableType>.contains` simply doesn't exist — the compiler enforces this at the call site.",
      },
      {
        id: "q8",
        prompt: "What does `func makeShape() -> some Shape` communicate to callers?",
        options: [
          { id: "a", text: "That the function returns a value of a different concrete type on every single call" },
          { id: "b", text: "That the function returns *some specific, concrete type* conforming to `Shape`, consistent across calls to this function, without revealing exactly which concrete type it is" },
          { id: "c", text: "That the return value could be `nil`" },
          { id: "d", text: "That `Shape` must be a class, not a protocol" },
        ],
        correctOptionId: "b",
        explanation: "`some Protocol` (an opaque return type) means the function commits to returning one specific, fixed underlying concrete type — the compiler knows exactly which one, preserving full static type information — while hiding that specific type from the caller's view, exposing only the guarantee that it conforms to `Shape`.",
      },
      {
        id: "q9",
        prompt: "Why can `let shapes: [any Shape] = [Circle(radius: 2), Square(side: 3)]` mix genuinely different concrete types in one array, while `some Shape` could not?",
        options: [
          { id: "a", text: "`any Shape` is actually invalid Swift syntax" },
          { id: "b", text: "`any Shape` is a runtime box that can hold any type conforming to `Shape`, permitting genuine heterogeneous storage; `some Shape` fixes exactly one concrete type per context and can't vary across elements" },
          { id: "c", text: "`Circle` and `Square` are secretly the same type under the hood" },
          { id: "d", text: "Arrays in Swift can never hold protocol types at all" },
        ],
        correctOptionId: "b",
        explanation: "`any Shape` (an existential type) erases the concrete type into a runtime-checked container, allowing genuinely different types to coexist in the same collection. `some Shape` requires exactly one fixed concrete type determined at compile time for a given context, making mixed storage of different concrete types impossible with `some`.",
      },
      {
        id: "q10",
        prompt: "Which of `some` and `any` generally has better runtime performance, and why?",
        options: [
          { id: "a", text: "`any`, because it avoids compile-time type checking" },
          { id: "b", text: "`some`, because it preserves full static type information, enabling direct static dispatch without the boxing and dynamic dispatch overhead that `any` typically requires" },
          { id: "c", text: "They are always identical in performance" },
          { id: "d", text: "`any`, because it uses less memory than `some`" },
        ],
        correctOptionId: "b",
        explanation: "`some` (opaque types) let the compiler know the exact concrete type at compile time, enabling non-boxed, statically-dispatched calls. `any` (existential types) erase the concrete type into a runtime box, typically requiring heap allocation and dynamic witness-table dispatch — more flexible, but generally slower.",
      },
      {
        id: "q11",
        prompt: "Why couldn't `func makeSomeShape(random: Bool) -> any Shape` be rewritten to return `some Shape` instead, given it returns either a `Circle` or a `Square` depending on a runtime condition?",
        options: [
          { id: "a", text: "It actually could be rewritten that way with no issues" },
          { id: "b", text: "`some` requires exactly one fixed underlying concrete type per function — since this function might return two genuinely different concrete types depending on the branch taken, only `any` can express that" },
          { id: "c", text: "`Shape` isn't a valid protocol for `some` to use" },
          { id: "d", text: "`some` doesn't support `Bool` parameters" },
        ],
        correctOptionId: "b",
        explanation: "This is precisely the legality boundary between the two: `some Shape` commits the function to always returning the exact same concrete type on every call, so a function that might return either a `Circle` or a `Square` depending on runtime logic can only be expressed with `any Shape`.",
      },
      {
        id: "q12",
        prompt: "What is a \"primary associated type,\" as in `some Collection<Int>`?",
        options: [
          { id: "a", text: "A completely different feature unrelated to associated types" },
          { id: "b", text: "Syntax (since Swift 5.7) that exposes a protocol's associated type directly in angle brackets at the use site, as sugar over the equivalent, more verbose `where C.Element == Int` clause" },
          { id: "c", text: "A requirement that a protocol have at least two associated types" },
          { id: "d", text: "A special associated type reserved only for `Array`" },
        ],
        correctOptionId: "b",
        explanation: "Primary associated types let you write `Collection<Int>` instead of the older, more verbose `<C: Collection> ... where C.Element == Int` form — purely a readability improvement, compiling down to the same underlying constraint.",
      },
      {
        id: "q13",
        prompt: "What problem does type erasure (writing an `AnyX` wrapper struct) solve?",
        options: [
          { id: "a", text: "It makes generic code run without any type checking at all" },
          { id: "b", text: "It hides a generic type's concrete parameter behind a concrete wrapper type, often to enable heterogeneous storage for protocols with associated types that can't otherwise form a plain existential array" },
          { id: "c", text: "It automatically synthesizes `Equatable` conformance" },
          { id: "d", text: "It's purely a historical technique with no remaining use now that `some`/`any` exist" },
        ],
        correctOptionId: "b",
        explanation: "Type erasure (as in the standard library's `AnySequence`, `AnyHashable`) wraps a concrete generic implementation behind closures captured at initialization, hiding the specific concrete type while preserving behavior — useful for protocols with associated types where forming a plain existential collection isn't otherwise straightforward.",
      },
      {
        id: "q14",
        prompt: "How does an `AnyAnimal<Food>` type-erasing wrapper typically preserve the behavior of the original wrapped instance?",
        options: [
          { id: "a", text: "It copies the original instance's memory layout directly" },
          { id: "b", text: "It captures the original instance's method calls as closures at initialization time, forwarding calls to the original concrete instance when the wrapper's own methods are invoked" },
          { id: "c", text: "It doesn't preserve behavior — it only preserves the type name" },
          { id: "d", text: "It requires the original type to also conform to `Codable`" },
        ],
        correctOptionId: "b",
        explanation: "The wrapper's initializer captures a reference/copy of the concrete instance and stores closures (like `_eat`) that call through to that instance's real implementation — this is what lets the type-erased wrapper behave identically to the original while presenting a uniform, concrete public type.",
      },
      {
        id: "q15",
        prompt: "What do parameter packs (`each T`, introduced in Swift 5.9) enable that wasn't previously possible without hand-writing separate overloads?",
        options: [
          { id: "a", text: "Functions that accept a variable number of arguments, all of the exact same type (already possible with `T...`)" },
          { id: "b", text: "Functions and types generic over a variable number of *differently-typed* parameters at once, like accepting and returning an arbitrary-length, arbitrarily-typed tuple" },
          { id: "c", text: "The ability to define more than one `associatedtype` in a protocol" },
          { id: "d", text: "Automatic conformance to `Comparable`" },
        ],
        correctOptionId: "b",
        explanation: "Ordinary variadics (`Int...`, from section 5.4) only support multiple values of the *same* type. Parameter packs (`each T` with `repeat each`) generalize this to multiple, independently-typed parameters — exactly what's needed to write a single generic function handling tuples of any arity and any mix of types.",
      },
      {
        id: "q16",
        prompt: "In `struct Distance<Unit> { var value: Double }` with `enum Meters {}` and `enum Feet {}` used as the `Unit` parameter, what is `Unit` an example of?",
        options: [
          { id: "a", text: "An associated type" },
          { id: "b", text: "A phantom type — a generic parameter that appears in the declaration but is never actually stored or used by any property, existing purely for compile-time distinction" },
          { id: "c", text: "A required protocol conformance" },
          { id: "d", text: "A primary associated type" },
        ],
        correctOptionId: "b",
        explanation: "`Unit` never actually appears in any stored property of `Distance` — `value` is just a `Double` regardless of which `Unit` is used. `Unit` exists purely as a compile-time tag, letting the type system distinguish `Distance<Meters>` from `Distance<Feet>` and catch unit-mismatch bugs at compile time.",
      },
      {
        id: "q17",
        prompt: "What is the runtime cost of a phantom type parameter like `Unit` in `Distance<Unit>`?",
        options: [
          { id: "a", text: "It doubles the struct's memory footprint" },
          { id: "b", text: "Zero — phantom type parameters disappear entirely after compilation, since they're never actually used to store data" },
          { id: "c", text: "It requires an additional heap allocation per instance" },
          { id: "d", text: "It slows down every method call involving `Distance`" },
        ],
        correctOptionId: "b",
        explanation: "Because a phantom type parameter is never used to store or compute anything, it has no runtime representation at all — it exists purely at compile time to enable the type checker to reject mismatched combinations (like adding meters to feet), at zero runtime cost.",
      },
      {
        id: "q18",
        prompt: "Given `func makePair<A, B>(_ first: A, _ second: B) -> (A, B)`, what does calling `makePair(1, \"one\")` produce?",
        options: [
          { id: "a", text: "A compile-time error, since `A` and `B` must be the same type" },
          { id: "b", text: "A tuple of type `(Int, String)`, with `A` resolved to `Int` and `B` resolved to `String` independently for this call" },
          { id: "c", text: "`(String, String)`, since Swift converts everything to strings" },
          { id: "d", text: "`nil`, since mismatched types aren't allowed" },
        ],
        correctOptionId: "b",
        explanation: "`A` and `B` are independent type parameters — nothing requires them to match. The compiler infers `A = Int` and `B = String` separately from the arguments passed, producing a properly-typed `(Int, String)` tuple.",
      },
      {
        id: "q19",
        prompt: "Why is it accurate to say associated types are \"filled in by the conforming type\" rather than \"by the caller,\" in contrast to a generic function's type parameter?",
        options: [
          { id: "a", text: "There's no real distinction — both work identically" },
          { id: "b", text: "A generic function's caller explicitly (or via inference from arguments) determines the type parameter at the call site; a protocol's associated type is instead determined once, structurally, by how each conforming type implements the protocol's requirements — not chosen freshly at each usage site" },
          { id: "c", text: "Associated types must always be written out explicitly by the protocol's author" },
          { id: "d", text: "Only classes can determine associated types; structs cannot" },
        ],
        correctOptionId: "b",
        explanation: "When you call `swapValues(&x, &y)`, the caller (via the arguments) determines `T` for that specific call. But `IntContainer`'s `Item` is fixed once, structurally, by how `IntContainer` itself implements `add(_:)` — every use of an `IntContainer` value sees `Item == Int`, it's not re-chosen per call site the way a generic function's type parameter is.",
      },
      {
        id: "q20",
        prompt: "Which combination correctly matches Swift feature to its primary purpose?",
        options: [
          { id: "a", text: "`some` → heterogeneous runtime storage; `any` → single compile-time-known concrete type" },
          { id: "b", text: "`some` → single compile-time-known concrete type with static dispatch; `any` → heterogeneous runtime storage via a type-erased box" },
          { id: "c", text: "Phantom types → runtime type checking; associated types → compile-time-only tags with no storage" },
          { id: "d", text: "Parameter packs → single-type variadics; `Int...` → multi-type variadics" },
        ],
        correctOptionId: "b",
        explanation: "This correctly pairs each feature with its role: `some` fixes one concrete, statically-known type per context for performance; `any` provides a runtime-erased box enabling mixed concrete types in the same collection. (Phantom types are compile-time-only tags with no storage, and parameter packs — not `Int...` — are what enable multi-type variadics, the reverse of option D.)",
      },
    ],
  },
  {
    id: "error-handling-quiz",
    title: "Error Handling Quiz",
    description: "20 questions on the Error protocol, throws/throw, do/catch, try vs try? vs try!, rethrows, typed throws, the Result type, and designing an error taxonomy.",
    relatedArticleSlug: "error-handling",
    questions: [
      {
        id: "q1",
        prompt: "What is required for a type to represent an error in Swift?",
        options: [
          { id: "a", text: "It must be a class inheriting from `NSError`" },
          { id: "b", text: "It must conform to the `Error` protocol, which declares no requirements of its own — usually implemented as an enum" },
          { id: "c", text: "It must implement a `code: Int` property" },
          { id: "d", text: "It must conform to `Codable`" },
        ],
        correctOptionId: "b",
        explanation: "`Error` is an empty marker protocol — conforming to it is what allows a type to participate in Swift's `throw`/`try`/`catch` machinery. In practice, custom errors are almost always enums, since errors naturally form a finite, related set of cases, often with associated data for context.",
      },
      {
        id: "q2",
        prompt: "What must a caller do when calling a function marked `throws`?",
        options: [
          { id: "a", text: "Nothing special — `throws` functions are called exactly like any other function" },
          { id: "b", text: "Prefix the call with `try` — the compiler enforces this at every call site" },
          { id: "c", text: "Wrap the call in a `guard` statement" },
          { id: "d", text: "Declare the calling function as `@escaping`" },
        ],
        correctOptionId: "b",
        explanation: "Swift requires every call to a `throws` function to be marked with `try` (or one of its variants, `try?`/`try!`). This makes error-producing calls visible directly in the source wherever they occur, unlike exceptions in some other languages that can propagate silently and invisibly.",
      },
      {
        id: "q3",
        prompt: "In a `do`/`catch` block, what does a bare `catch { }` (with no pattern) do?",
        options: [
          { id: "a", text: "It's a syntax error — every `catch` must specify a pattern" },
          { id: "b", text: "It catches any error not matched by earlier, more specific `catch` clauses, implicitly binding the thrown value to a local constant named `error`" },
          { id: "c", text: "It only catches errors of type `NSError`" },
          { id: "d", text: "It silently ignores all errors without giving access to them" },
        ],
        correctOptionId: "b",
        explanation: "A bare `catch { }` acts as a catch-all, matching any error not already handled by a preceding, more specific pattern (like `catch ValidationError.tooShort`), and it implicitly provides the thrown value as `error` inside its block.",
      },
      {
        id: "q4",
        prompt: "What is the difference between `try` and `try?` when calling a throwing function?",
        options: [
          { id: "a", text: "They are identical in every way" },
          { id: "b", text: "`try` propagates the error upward (requiring `do`/`catch` or an enclosing `throws` function); `try?` converts the result into an optional — `nil` on failure, discarding the specific error" },
          { id: "c", text: "`try?` always crashes on failure, while `try` returns `nil`" },
          { id: "d", text: "`try?` can only be used with functions that never actually throw" },
        ],
        correctOptionId: "b",
        explanation: "`try` requires proper error propagation or handling. `try?` instead converts the whole expression into an optional, silently discarding the specific error and reducing the outcome to just \"succeeded\" (a wrapped value) or \"failed\" (`nil`) — appropriate only when you genuinely don't need to know *why* it failed.",
      },
      {
        id: "q5",
        prompt: "What happens if a function called with `try!` actually throws an error at runtime?",
        options: [
          { id: "a", text: "The error is silently ignored and execution continues" },
          { id: "b", text: "The app crashes immediately, exactly like force-unwrapping a `nil` optional with `!`" },
          { id: "c", text: "It's automatically converted to a `Result` value" },
          { id: "d", text: "It's converted to an optional, like `try?`" },
        ],
        correctOptionId: "b",
        explanation: "`try!` asserts, with no fallback, that the call will never throw. If that assertion turns out to be wrong, the app crashes immediately and unrecoverably — the same risk profile as force-unwrapping `nil` with `!` on an optional, and it should be used with the same caution.",
      },
      {
        id: "q6",
        prompt: "What does marking a function `rethrows` (instead of plain `throws`) communicate, as in `func customMap<T, U>(_ items: [T], _ transform: (T) throws -> U) rethrows -> [U]`?",
        options: [
          { id: "a", text: "That the function always throws, no matter what" },
          { id: "b", text: "That the function itself doesn't throw directly, but only throws if a closure parameter passed to it actually throws — letting callers skip `try` when they pass a non-throwing closure" },
          { id: "c", text: "That the function can throw multiple different errors simultaneously" },
          { id: "d", text: "That the function is equivalent to marking it `@escaping`" },
        ],
        correctOptionId: "b",
        explanation: "`rethrows` precisely describes higher-order functions whose only possible source of error is a passed-in closure. If the caller supplies a non-throwing closure, no `try` is needed at the call site at all; if they supply a throwing closure, `try` becomes necessary — a plain `throws` function wouldn't allow that flexibility.",
      },
      {
        id: "q7",
        prompt: "What does `func parse(_ input: String) throws(ParsingError) -> Int` express that a plain `throws` function does not?",
        options: [
          { id: "a", text: "That the function can throw any type of error, without restriction" },
          { id: "b", text: "That the function throws exactly the specific type `ParsingError`, rather than the implicit `any Error` that ordinary `throws` uses — giving callers exhaustive, statically-typed `catch` handling" },
          { id: "c", text: "That the function is guaranteed to never actually throw" },
          { id: "d", text: "That `ParsingError` must conform to `Codable`" },
        ],
        correctOptionId: "b",
        explanation: "Typed throws (`throws(SpecificType)`, introduced in Swift 6) narrows the thrown error to one precise, known type instead of the boxed `any Error`. This lets a `catch` block exhaustively switch over the specific error type with compile-time checking, and can avoid the small overhead of boxing an arbitrary existential error.",
      },
      {
        id: "q8",
        prompt: "What are the two cases of `Result<Success, Failure: Error>`?",
        options: [
          { id: "a", text: "`.value` and `.error`" },
          { id: "b", text: "`.success(Success)` and `.failure(Failure)`" },
          { id: "c", text: "`.ok` and `.fail`" },
          { id: "d", text: "`.some(Success)` and `.none`" },
        ],
        correctOptionId: "b",
        explanation: "`Result` is a standard-library enum with exactly two cases: `.success`, wrapping the successful value, and `.failure`, wrapping the specific error type. It represents an operation's outcome as a plain value, rather than via `throws`/`try`/`catch` control flow.",
      },
      {
        id: "q9",
        prompt: "What does `Result`'s `get()` method do?",
        options: [
          { id: "a", text: "It always returns the wrapped value directly, even for a `.failure` case" },
          { id: "b", text: "It returns the success value if the `Result` is `.success`, or throws the wrapped error if the `Result` is `.failure` — converting a `Result` back into `throws`-style code" },
          { id: "c", text: "It converts the `Result` into an optional" },
          { id: "d", text: "It's only available on `.failure` cases" },
        ],
        correctOptionId: "b",
        explanation: "`get()` bridges `Result` back to Swift's `throws` mechanism: calling `try someResult.get()` returns the wrapped success value, or re-throws the wrapped error if the result was a `.failure` — the inverse of `Result(catching:)`, which builds a `Result` from a throwing closure.",
      },
      {
        id: "q10",
        prompt: "Why might an app organize its errors into layered types, e.g. `NetworkError`, `PersistenceError`, and a top-level `AppError` that wraps both?",
        options: [
          { id: "a", text: "Swift requires at least three error enums per project" },
          { id: "b", text: "It keeps each subsystem's errors focused and independently testable, while still letting a higher layer (like the UI) handle one unified error type without needing to know every low-level error case each subsystem might produce" },
          { id: "c", text: "It's required for `Result` to work correctly" },
          { id: "d", text: "Layered error types are automatically converted to `NSError` for Objective-C interop" },
        ],
        correctOptionId: "b",
        explanation: "A single, flat error enum with dozens of unrelated cases becomes unwieldy as an app grows. Layering — one focused error type per subsystem, composed into a higher-level type like `AppError` — keeps each layer's concerns separate while still giving the top of the app (e.g. UI code) one consistent type to switch over.",
      },
      {
        id: "q11",
        prompt: "Why shouldn't a raw, internal error message (like a database error string) typically be shown directly to an end user?",
        options: [
          { id: "a", text: "Swift doesn't allow errors to be converted to strings" },
          { id: "b", text: "It can be confusing or unhelpful to a non-technical user, and may leak internal details (server internals, file paths, schema information) that shouldn't be user-facing — a curated message should be shown instead" },
          { id: "c", text: "`Error` values cannot be interpolated into strings at all" },
          { id: "d", text: "It would cause a compile-time error" },
        ],
        correctOptionId: "b",
        explanation: "Internal error details are meant for developers (logs, crash reports) — showing them raw to end users is both a poor user experience and a potential security/privacy concern. The recommended pattern is mapping the internal error to a separate, curated `userFacingMessage` while logging the full detail elsewhere.",
      },
      {
        id: "q12",
        prompt: "In `enum NetworkError: Error { case invalidURL; case noConnection; case serverError(statusCode: Int) }`, what is the purpose of the associated value on `.serverError`?",
        options: [
          { id: "a", text: "It's required syntax with no functional purpose" },
          { id: "b", text: "It lets that specific error case carry exactly the contextual data relevant to it — here, the actual HTTP status code — rather than forcing every case to share one generic, less-specific shape" },
          { id: "c", text: "It converts the enum into a struct" },
          { id: "d", text: "It must be a `String`, never an `Int`" },
        ],
        correctOptionId: "b",
        explanation: "Associated values (recall 6.17) let each error case carry its own relevant data. `.serverError(statusCode: 404)` conveys specific, useful diagnostic information that a case-less `.serverError` alone wouldn't, without needing every other case to also carry a status code.",
      },
      {
        id: "q13",
        prompt: "Given `let result = try? validate(username: \"ab\")` where `validate` throws `ValidationError.tooShort`, what is `result`?",
        options: [
          { id: "a", text: "`ValidationError.tooShort`" },
          { id: "b", text: "`nil`" },
          { id: "c", text: "`Optional(\"ab\")`" },
          { id: "d", text: "A compile-time error" },
        ],
        correctOptionId: "b",
        explanation: "`try?` converts the outcome to an optional, discarding the specific thrown error — since `validate` actually threw here, `result` is simply `nil`, with no way to recover which specific error occurred from `result` alone.",
      },
      {
        id: "q14",
        prompt: "Which catch clause correctly matches and extracts a specific error case with an associated value, given `enum ValidationError: Error { case tooShort; case tooLong }`?",
        options: [
          { id: "a", text: "`catch ValidationError.tooShort { ... }`" },
          { id: "b", text: "`catch case .tooShort { ... }`" },
          { id: "c", text: "`catch(ValidationError.tooShort) { ... }`" },
          { id: "d", text: "`catch throws ValidationError.tooShort { ... }`" },
        ],
        correctOptionId: "a",
        explanation: "`catch` clauses use the same pattern-matching syntax as `switch` cases (recall section 2) — `catch ValidationError.tooShort` matches specifically that case, exactly as you'd write `case ValidationError.tooShort` inside a `switch`.",
      },
      {
        id: "q15",
        prompt: "What is a key advantage of typed throws (`throws(SpecificError)`) over plain `throws` in performance-sensitive code?",
        options: [
          { id: "a", text: "Typed throws functions run on a separate thread automatically" },
          { id: "b", text: "Plain `throws` boxes the error as an existential `any Error`, which typed throws can avoid, alongside giving callers a precise, exhaustively-checkable error type" },
          { id: "c", text: "Typed throws functions cannot actually throw at runtime" },
          { id: "d", text: "Typed throws is required for all networking code" },
        ],
        correctOptionId: "b",
        explanation: "Ordinary `throws` uses the general `any Error` existential, which involves some boxing overhead. `throws(SpecificError)` narrows this to one concrete, known type, avoiding that overhead in performance-sensitive contexts while also giving callers exhaustive compile-time checking in their `catch` blocks.",
      },
      {
        id: "q16",
        prompt: "What does `Result(catching:)` do?",
        options: [
          { id: "a", text: "It converts a `Result` value back into a plain optional" },
          { id: "b", text: "It builds a `Result` from a throwing closure — running the closure, capturing a thrown error as `.failure` or a returned value as `.success`" },
          { id: "c", text: "It's used exclusively for networking code and nowhere else" },
          { id: "d", text: "It requires the closure to be marked `@escaping`" },
        ],
        correctOptionId: "b",
        explanation: "`Result(catching: { try someThrowingCall() })` runs the given throwing closure and automatically wraps its outcome — either the returned value as `.success`, or any thrown error as `.failure` — producing a `Result` value from ordinary `throws`-style code.",
      },
      {
        id: "q17",
        prompt: "Why is `try!` compared directly to force-unwrapping (`!`) an optional in terms of risk?",
        options: [
          { id: "a", text: "They are unrelated language features with no meaningful comparison" },
          { id: "b", text: "Both assert a guarantee (of success, or of non-nil-ness) that, if wrong, causes an immediate, unrecoverable runtime crash — both should be reserved for cases with a genuine, structural guarantee" },
          { id: "c", text: "`try!` is always safer than `!`, since errors are more predictable than nil values" },
          { id: "d", text: "`try!` only works on optional-returning functions" },
        ],
        correctOptionId: "b",
        explanation: "Both `try!` and force-unwrap `!` make an unconditional assertion to the compiler — \"this will succeed\" / \"this is non-nil\" — and both crash immediately and unrecoverably if that assertion proves false at runtime. The lesson guidance treats them with the same level of caution, reserving both for genuinely guaranteed cases.",
      },
      {
        id: "q18",
        prompt: "What does the `Error` protocol require conforming types to implement?",
        options: [
          { id: "a", text: "A `localizedDescription` property" },
          { id: "b", text: "Nothing — `Error` declares no requirements of its own; it's purely a marker protocol" },
          { id: "c", text: "A `code: Int` property and an `underlyingError` property" },
          { id: "d", text: "An `init(from:)` initializer" },
        ],
        correctOptionId: "b",
        explanation: "`Error` is intentionally an empty protocol — conforming to it is purely a marker that grants access to Swift's throw/catch machinery. Any type, including a simple enum with no additional requirements implemented, can conform.",
      },
      {
        id: "q19",
        prompt: "Consider `func loadUser() throws -> String { try validate(username: \"ab\") }`. What happens to an error thrown by `validate` inside `loadUser`?",
        options: [
          { id: "a", text: "It's automatically caught and converted to `nil`" },
          { id: "b", text: "It propagates upward out of `loadUser` as well, since `loadUser` itself doesn't catch it — it just re-declares `throws` and uses `try` to pass the error along" },
          { id: "c", text: "It crashes the app immediately, since there's no `do`/`catch` present" },
          { id: "d", text: "It's silently discarded" },
        ],
        correctOptionId: "b",
        explanation: "Since `loadUser` doesn't wrap the call in `do`/`catch`, any error `validate` throws simply propagates further up the call stack — this is why `loadUser` itself must also be marked `throws`, and its own callers will need to handle (or further propagate) the error.",
      },
      {
        id: "q20",
        prompt: "What is the recommended default choice among `try`, `try?`, and `try!` for most application code?",
        options: [
          { id: "a", text: "`try!`, since it's the most concise" },
          { id: "b", text: "`try?`, since it avoids needing a `do`/`catch` block" },
          { id: "c", text: "`try`, combined with `do`/`catch` (or further propagation via `throws`), reserving `try?` for when the specific error genuinely doesn't matter and `try!` for cases with a true structural guarantee of success" },
          { id: "d", text: "None of them — plain function calls without any `try` variant are preferred" },
        ],
        correctOptionId: "c",
        explanation: "`try` (with proper handling or propagation) is the default, safest, and most informative approach, since it preserves the specific error for handling. `try?` and `try!` are narrower tools for specific situations — discarding the error entirely, or asserting guaranteed success — not general-purpose defaults.",
      },
    ],
  },
  {
    id: "memory-management-quiz",
    title: "Memory Management Quiz",
    description: "20 questions on ARC, strong reference cycles, weak vs unowned references, closure and delegate retain cycles, copy-on-write, stack vs heap allocation, and the Law of Exclusivity.",
    relatedArticleSlug: "memory-management",
    questions: [
      {
        id: "q1",
        prompt: "What does ARC (Automatic Reference Counting) actually track and manage?",
        options: [
          { id: "a", text: "The memory of every Swift value, including structs and enums" },
          { id: "b", text: "The number of strong references to each class instance, deallocating it once that count reaches zero" },
          { id: "c", text: "Only global variables" },
          { id: "d", text: "Garbage collection pauses during app execution" },
        ],
        correctOptionId: "b",
        explanation: "ARC counts *strong references* specifically to class instances. Structs, enums, and other value types aren't reference-counted at all, since they have no shared identity to track — assigning a value type copies it rather than adding a reference.",
      },
      {
        id: "q2",
        prompt: "In the `Person`/`Apartment` retain cycle example, why does neither object's `deinit` ever run, even after setting both local variables to `nil`?",
        options: [
          { id: "a", text: "`deinit` never runs automatically in Swift" },
          { id: "b", text: "The two objects hold strong references to *each other* (`apartment.tenant` and `person.apartment`), so even after all *external* references are gone, their mutual strong references keep both reference counts above zero forever" },
          { id: "c", text: "Structs cannot be deallocated once created" },
          { id: "d", text: "`deinit` only runs when the app terminates" },
        ],
        correctOptionId: "b",
        explanation: "This is the definition of a retain cycle: two objects each hold a strong reference to the other. Even though the external variables `john` and `unit4A` are set to `nil`, the objects keep each other alive via their mutual strong properties, so neither reference count ever reaches zero, and both leak silently.",
      },
      {
        id: "q3",
        prompt: "What must the type of a `weak` reference always be, and why?",
        options: [
          { id: "a", text: "A non-optional class type, since `weak` guarantees the value always exists" },
          { id: "b", text: "An optional type, because the referenced object could be deallocated at any time via its other strong references, and Swift automatically sets a `weak` reference to `nil` when that happens" },
          { id: "c", text: "Always `String`, regardless of what it references" },
          { id: "d", text: "A value type like `struct` or `enum`" },
        ],
        correctOptionId: "b",
        explanation: "Since a `weak` reference doesn't keep its target alive, the target could be deallocated while the `weak` reference still exists. Swift handles this safely by requiring `weak` references to be optional, automatically setting them to `nil` the instant their target is deallocated.",
      },
      {
        id: "q4",
        prompt: "What is the key difference between `weak` and `unowned` references?",
        options: [
          { id: "a", text: "They are functionally identical" },
          { id: "b", text: "`weak` is optional and safely becomes `nil` when its target deallocates; `unowned` is non-optional and crashes if accessed after its target has already deallocated" },
          { id: "c", text: "`unowned` keeps its target alive, while `weak` does not" },
          { id: "d", text: "`weak` can only be used on structs; `unowned` only on classes" },
        ],
        correctOptionId: "b",
        explanation: "Both avoid contributing to the strong reference count, but they differ in the safety guarantee: `weak` provides a safety net (auto-nilling, requiring optional unwrapping), while `unowned` asserts the target will always be valid when accessed — a wrong assumption there causes an immediate crash, with no recoverable fallback.",
      },
      {
        id: "q5",
        prompt: "When is `unowned` an appropriate choice over `weak`?",
        options: [
          { id: "a", text: "Whenever you want to avoid ever writing optional-unwrapping code, regardless of lifetime guarantees" },
          { id: "b", text: "Only when the referenced object is guaranteed to outlive (or share an identical lifetime with) the reference holding it, such as a `CreditCard` that logically cannot exist without its associated `Customer`" },
          { id: "c", text: "Never — `unowned` should always be avoided in favor of `weak`" },
          { id: "d", text: "Only inside SwiftUI views" },
        ],
        correctOptionId: "b",
        explanation: "`unowned` should only be used when there's a genuine structural guarantee that the referenced object will still be valid whenever the `unowned` reference is accessed. Using it purely to avoid unwrapping syntax, without that lifetime guarantee, risks a hard crash later.",
      },
      {
        id: "q6",
        prompt: "Why does a class storing a closure as a property (like `onUpdate`) risk a retain cycle if that closure references `self`?",
        options: [
          { id: "a", text: "Closures never actually capture anything in Swift" },
          { id: "b", text: "Closures capture reference types strongly by default, so if `self` stores the closure and the closure captures `self`, both end up strongly referencing each other, exactly like the two-object cycle" },
          { id: "c", text: "This only happens with structs, never classes" },
          { id: "d", text: "`deinit` prevents this automatically" },
        ],
        correctOptionId: "b",
        explanation: "A closure captures `self` strongly unless told otherwise (recall section 5.12–5.13). When that same closure is stored as a property on `self` (like `onUpdate`), you get a cycle: `self` strongly holds the closure, and the closure strongly holds `self` — neither can ever be deallocated on its own.",
      },
      {
        id: "q7",
        prompt: "What is the practical difference between using `[weak self]` versus `[unowned self]` inside a stored closure?",
        options: [
          { id: "a", text: "There is no practical difference between them" },
          { id: "b", text: "`[weak self]` produces an optional `self` that must be safely unwrapped (and might legitimately be `nil` when the closure runs); `[unowned self]` asserts `self` will always exist, crashing if that assumption is wrong when the closure actually runs" },
          { id: "c", text: "`[unowned self]` is always the safer choice for asynchronous work" },
          { id: "d", text: "`[weak self]` prevents the closure from ever running at all" },
        ],
        correctOptionId: "b",
        explanation: "Both break the strong-capture cycle, but with different safety trade-offs: `[weak self]` requires handling the possibility that `self` is already gone (typically via `guard let self else { return }`), while `[unowned self]` skips that check and simply crashes if `self` has already been deallocated by the time the closure executes.",
      },
      {
        id: "q8",
        prompt: "Why is `weak var delegate` the conventional pattern for delegate properties, and why must delegate protocols typically be constrained to `AnyObject`?",
        options: [
          { id: "a", text: "`weak` works on both value and reference types equally well" },
          { id: "b", text: "A parent object often strongly owns a child that has a `delegate` pointing back at the parent; making `delegate` `weak` avoids a two-object retain cycle — and `weak` only applies to reference types, which is why the protocol must be class-only (`AnyObject`-constrained)" },
          { id: "c", text: "`AnyObject` is required for all Swift protocols, regardless of use case" },
          { id: "d", text: "`weak` delegates are faster to call than strong ones" },
        ],
        correctOptionId: "b",
        explanation: "In the classic delegate pattern, a parent (e.g. a view controller) strongly owns a child (e.g. a downloader), and the child holds a `delegate` reference pointing back at the parent. Making that `delegate` property `weak` avoids the resulting retain cycle — and since `weak` only applies to reference types, the delegate protocol must be constrained to classes via `AnyObject`.",
      },
      {
        id: "q9",
        prompt: "Why can a never-completing `Task { }` that captures `self` strongly cause the same kind of leak as a closure-based retain cycle?",
        options: [
          { id: "a", text: "`Task` values are always value types with no capture behavior" },
          { id: "b", text: "`Task`'s closure captures its surrounding context strongly by default, just like an ordinary closure; if the class stores the `Task` and the task's closure never finishes (e.g. an infinite polling loop) while capturing `self`, `self` is kept alive indefinitely" },
          { id: "c", text: "`Task` automatically uses `weak` capture semantics, so this scenario cannot occur" },
          { id: "d", text: "This is only a concern in Objective-C code, not Swift" },
        ],
        correctOptionId: "b",
        explanation: "`Task { }` closures follow the same strong-capture-by-default rules as any other closure. A stored, long-running or infinite `Task` capturing `self` strongly will keep `self` alive as long as the task keeps running — the fix mirrors ordinary closures: `[weak self]` plus a cancellation/nil check.",
      },
      {
        id: "q10",
        prompt: "What is a simple, practical technique for detecting an unexpected retain cycle during development?",
        options: [
          { id: "a", text: "Reading the compiled binary's assembly output" },
          { id: "b", text: "Adding a `print` statement (or breakpoint) inside `deinit` to confirm an object is actually deallocated when expected — if the message never appears when it should, something is holding an unwanted strong reference" },
          { id: "c", text: "Marking every property `weak` by default" },
          { id: "d", text: "Disabling ARC temporarily" },
        ],
        correctOptionId: "b",
        explanation: "Since retain cycles fail silently with no crash or warning, logging inside `deinit` gives a quick, practical signal: if you navigate away from a screen and never see its `deinit` log, something (an unbroken closure capture, a strong delegate, a leaked `Task`) is keeping it alive longer than intended.",
      },
      {
        id: "q11",
        prompt: "What does copy-on-write (COW) actually do when you write `var array2 = array1` for two arrays?",
        options: [
          { id: "a", text: "It immediately creates a full, independent copy of the underlying storage" },
          { id: "b", text: "It shares the same underlying storage buffer between `array1` and `array2` with no copy at all, until one of them is actually mutated — at which point a real copy is made just before that mutation" },
          { id: "c", text: "It converts both arrays into reference types permanently" },
          { id: "d", text: "It deletes `array1` entirely" },
        ],
        correctOptionId: "b",
        explanation: "Copy-on-write defers the actual, potentially expensive copy of underlying storage until the first mutation after a share happens — assignment itself is cheap (just sharing a reference to the same buffer), which is exactly what makes Swift's value types efficient to pass around despite their value semantics.",
      },
      {
        id: "q12",
        prompt: "What does `isKnownUniquelyReferenced` check, and why would a custom COW-implementing type call it inside a mutating method?",
        options: [
          { id: "a", text: "Whether a value is `nil`" },
          { id: "b", text: "Whether a reference-counted storage object currently has exactly one owner — if not (i.e., it's shared with another copy), the mutating method must make a fresh copy of that storage before mutating it, to avoid corrupting a value another variable still holds" },
          { id: "c", text: "Whether a type conforms to `Equatable`" },
          { id: "d", text: "Whether the app has enough available memory" },
        ],
        correctOptionId: "b",
        explanation: "`isKnownUniquelyReferenced(&storage)` tells a custom type whether its backing storage is exclusively owned by this instance. If it's shared (multiple copies point at the same storage), the mutating method must copy the storage first — this is exactly the mechanism that makes copy-on-write correct and efficient for custom types wrapping reference-counted storage.",
      },
      {
        id: "q13",
        prompt: "Why does Swift generally favor `struct` over `class` for simple data types, from a memory-allocation perspective?",
        options: [
          { id: "a", text: "Structs are always heap-allocated, just like classes" },
          { id: "b", text: "Value types like structs are typically stack-allocated — fast, automatically reclaimed on scope exit, with no ARC (reference-counting) overhead — unlike class instances, which are heap-allocated and require ARC management" },
          { id: "c", text: "Classes are always faster than structs" },
          { id: "d", text: "There is no actual performance difference between them" },
        ],
        correctOptionId: "b",
        explanation: "Simple structs generally live on the stack, avoiding heap allocation and the retain/release traffic ARC imposes on class instances. This is one of the practical performance reasons behind the \"default to struct\" guidance from section 6.10, alongside the value-semantics safety benefits.",
      },
      {
        id: "q14",
        prompt: "Why can storing a large value type inside an `any Protocol` existential require an additional heap allocation, beyond what `some`/generic code would need?",
        options: [
          { id: "a", text: "`any` always allocates on the stack, avoiding this problem entirely" },
          { id: "b", text: "The existential container has a small fixed inline buffer; value types larger than that buffer must be boxed onto the heap to fit inside the existential, adding allocation overhead that statically-typed generic code avoids" },
          { id: "c", text: "This only affects reference types, never structs" },
          { id: "d", text: "`any` and `some` have identical memory behavior in all cases" },
        ],
        correctOptionId: "b",
        explanation: "Existential types (`any Protocol`) reserve a small fixed-size inline buffer for the wrapped value. Value types that don't fit within that buffer (larger structs, like the `BigShape` example) must be heap-allocated and boxed instead — an extra cost that `some`/generic parameters, which preserve the concrete static type, don't incur.",
      },
      {
        id: "q15",
        prompt: "In what scenario is `autoreleasepool { }` still occasionally relevant in Swift code?",
        options: [
          { id: "a", text: "In every single Swift function, as a required best practice" },
          { id: "b", text: "In tight loops that heavily create temporary Objective-C-bridged objects, where it forces those temporary objects to be released promptly rather than accumulating until the run loop iteration ends" },
          { id: "c", text: "Only for structs and enums, never for classes" },
          { id: "d", text: "It has no remaining relevance in modern Swift at all" },
        ],
        correctOptionId: "b",
        explanation: "Autorelease pools are a holdover from Objective-C's memory model. In pure native Swift code they're rarely needed, since ARC manages Swift object lifetimes deterministically — but in tight loops exercising Objective-C-bridged Foundation/UIKit APIs, wrapping the loop body in `autoreleasepool { }` can prevent temporary objects from piling up in memory before being reclaimed.",
      },
      {
        id: "q16",
        prompt: "What does the Law of Exclusivity forbid?",
        options: [
          { id: "a", text: "Ever using `inout` parameters" },
          { id: "b", text: "Having two overlapping accesses to the same variable where at least one of the accesses is a mutation — this is checked (partly compile-time, partly runtime) to prevent aliasing bugs and enable safe compiler optimizations" },
          { id: "c", text: "Declaring more than one `var` in the same function" },
          { id: "d", text: "Using classes alongside structs in the same file" },
        ],
        correctOptionId: "b",
        explanation: "Exclusive access rules prevent overlapping mutable access to the same memory location, since Swift's optimizer assumes no aliasing between mutable references unless explicitly proven otherwise. Violating this (even if it \"seems to work\") could lead to undefined behavior once such optimizations are applied — which is why Swift actively detects and rejects these situations.",
      },
      {
        id: "q17",
        prompt: "Why does `swapValues(&x, &x)` crash at runtime with \"simultaneous accesses to the same variable\"?",
        options: [
          { id: "a", text: "`swapValues` is not a valid function signature in Swift" },
          { id: "b", text: "Passing the same variable as two different `inout` arguments to the same call means the function body might read/write the same memory location twice \"at once,\" violating the Law of Exclusivity" },
          { id: "c", text: "`Int` values cannot be passed as `inout` parameters" },
          { id: "d", text: "This code doesn't actually crash — it swaps `x` with itself harmlessly" },
        ],
        correctOptionId: "b",
        explanation: "Both `a` and `b` in `swapValues` would refer to the exact same underlying storage (`x`), and the function body treats them as if they were independent, overlapping mutable accesses — Swift's runtime exclusivity check detects and rejects this specific case as unsafe.",
      },
      {
        id: "q18",
        prompt: "What deallocates a class instance once its strong reference count reaches zero?",
        options: [
          { id: "a", text: "A separate garbage collector process, at some undetermined future time" },
          { id: "b", text: "ARC deallocates it immediately and deterministically, running `deinit` first" },
          { id: "c", text: "The instance remains in memory until the app quits, regardless of reference count" },
          { id: "d", text: "Only calling `instance = nil` explicitly triggers deallocation, never automatic reference counting" },
        ],
        correctOptionId: "b",
        explanation: "Unlike garbage-collected languages where cleanup timing can be unpredictable, ARC deallocates a class instance immediately and deterministically the moment its strong reference count reaches zero, running its `deinit` first — this determinism is part of what makes `deinit` logging (Q10) a reliable debugging technique.",
      },
      {
        id: "q19",
        prompt: "Which of the following is NOT reference-counted by ARC?",
        options: [
          { id: "a", text: "A `class` instance stored in a variable" },
          { id: "b", text: "A `class` instance captured strongly by a closure" },
          { id: "c", text: "A `struct` value assigned to a variable" },
          { id: "d", text: "A `class` instance referenced by another class's stored property" },
        ],
        correctOptionId: "c",
        explanation: "ARC exclusively tracks references to class instances. Structs (and other value types) are copied on assignment rather than reference-counted — they have no shared identity for ARC to track in the first place.",
      },
      {
        id: "q20",
        prompt: "Why is the risk profile of `unowned` similar to that of force-unwrapping (`!`) an optional?",
        options: [
          { id: "a", text: "They are entirely unrelated concepts with no meaningful similarity" },
          { id: "b", text: "Both make an unconditional assertion (that a reference is still valid, or that an optional holds a value) that, if wrong, causes an immediate, unrecoverable runtime crash — both should be reserved for cases with a genuine, structural guarantee" },
          { id: "c", text: "`unowned` is always safer than `!`, since it applies only to classes" },
          { id: "d", text: "`unowned` automatically converts to `weak` if the assertion turns out to be false" },
        ],
        correctOptionId: "b",
        explanation: "Both `unowned` and `!` trade safety for conciseness by asserting a guarantee with no fallback — `unowned` asserts the reference will still be valid whenever accessed, `!` asserts an optional definitely holds a value. In both cases, an incorrect assertion produces an immediate, unrecoverable crash rather than a recoverable error.",
      },
    ],
  },
  {
    id: "advanced-type-system-quiz",
    title: "Advanced Type System Quiz",
    description: "20 questions on key paths, @dynamicMemberLookup/@dynamicCallable, operator overloading, noncopyable types and ownership modifiers, access control including package, and @frozen/ABI stability.",
    relatedArticleSlug: "advanced-type-system",
    questions: [
      {
        id: "q1",
        prompt: "What does a key path like `\\Person.name` actually represent?",
        options: [
          { id: "a", text: "The current value of `name` for one specific `Person` instance" },
          { id: "b", text: "A first-class, reusable reference to the `.name` property itself, which can be stored, passed around, and applied to different instances later" },
          { id: "c", text: "A closure that always returns the string `\"name\"`" },
          { id: "d", text: "A compile-time-only construct with no runtime representation at all" },
        ],
        correctOptionId: "b",
        explanation: "A key path doesn't capture a specific value — it's a reusable handle to \"the `.name` property,\" which can later be applied to any matching instance via `instance[keyPath: path]`, or reused across many different instances.",
      },
      {
        id: "q2",
        prompt: "What is the difference between `KeyPath` and `WritableKeyPath`?",
        options: [
          { id: "a", text: "They are interchangeable in every context" },
          { id: "b", text: "`KeyPath` supports only reading a property's value; `WritableKeyPath` supports both reading and writing, and requires the underlying property to be a mutable `var`" },
          { id: "c", text: "`WritableKeyPath` can only be used with classes" },
          { id: "d", text: "`KeyPath` is deprecated in favor of `WritableKeyPath`" },
        ],
        correctOptionId: "b",
        explanation: "`KeyPath` is read-only, appropriate for `let` properties or get-only computed properties. `WritableKeyPath` additionally supports assignment through the key path (`instance[keyPath: path] = newValue`), which requires the underlying property to actually be settable.",
      },
      {
        id: "q3",
        prompt: "What does `people.map(\\.name)` do, compared to `people.map { $0.name }`?",
        options: [
          { id: "a", text: "They are functionally equivalent — the key path is automatically bridged to a closure of the matching shape at the call site" },
          { id: "b", text: "`map(\\.name)` is invalid Swift syntax" },
          { id: "c", text: "`map(\\.name)` returns key paths themselves, not the actual name values" },
          { id: "d", text: "They produce different element counts" },
        ],
        correctOptionId: "a",
        explanation: "Since Swift 5.2, a key path literal can be used directly wherever a function of type `(Root) -> Value` is expected. `\\.name` behaves exactly like `{ $0.name }` in this context, just more concisely.",
      },
      {
        id: "q4",
        prompt: "What does `@dynamicMemberLookup` allow a type to do?",
        options: [
          { id: "a", text: "Automatically conform to `Codable`" },
          { id: "b", text: "Support dot-syntax access to properties not declared at compile time, by rewriting `instance.someProperty` into a call to a `subscript(dynamicMember:)` method you implement" },
          { id: "c", text: "Automatically synthesize `Equatable` and `Hashable`" },
          { id: "d", text: "Make all of its properties `static`" },
        ],
        correctOptionId: "b",
        explanation: "`@dynamicMemberLookup` lets the compiler translate undeclared dot-syntax property access into a call to your custom `subscript(dynamicMember:)`, commonly used for flexible wrappers around loosely-typed data like JSON — at the cost of losing compile-time typo checking for those dynamic members.",
      },
      {
        id: "q5",
        prompt: "What is a notable trade-off of using `@dynamicMemberLookup`?",
        options: [
          { id: "a", text: "It makes the type unable to conform to any protocols" },
          { id: "b", text: "Typos in a \"property\" name become runtime `nil`/failures instead of compile-time errors, since the compiler can't verify dynamic member names ahead of time" },
          { id: "c", text: "It requires the type to be a class" },
          { id: "d", text: "It disables ARC for that type" },
        ],
        correctOptionId: "b",
        explanation: "Because the accessed \"properties\" aren't actually declared, the compiler can't catch a misspelled member name at compile time the way it would for an ordinary property — errors surface only at runtime (e.g. as a `nil` lookup), which is why the lesson recommends using this feature sparingly.",
      },
      {
        id: "q6",
        prompt: "What does `@dynamicCallable` enable?",
        options: [
          { id: "a", text: "Automatic operator overloading for `+` and `-`" },
          { id: "b", text: "An instance of the type to be called directly like a function (`instance(...)`), by implementing a `dynamicallyCall` method" },
          { id: "c", text: "A type to automatically become `Codable`" },
          { id: "d", text: "A type to skip ARC reference counting" },
        ],
        correctOptionId: "b",
        explanation: "`@dynamicCallable` lets you write `instance(2, 3, 4)` for a struct/class instance, which the compiler translates into a call to your `dynamicallyCall(withArguments:)` implementation — a rarer feature typically used for embedded DSLs or scripting bridges.",
      },
      {
        id: "q7",
        prompt: "What must you declare before defining an entirely new custom operator like `**`, e.g. `2.0 ** 10.0`?",
        options: [
          { id: "a", text: "Nothing — you can define the function directly with no other declarations" },
          { id: "b", text: "The operator must first be declared with `prefix`, `infix`, or `postfix` (and typically an associated precedence group for infix operators) before its implementing function is written" },
          { id: "c", text: "It must be declared as `@dynamicCallable`" },
          { id: "d", text: "Custom operators are not supported in Swift at all" },
        ],
        correctOptionId: "b",
        explanation: "New operators require an explicit declaration stating their fixity (`prefix`/`infix`/`postfix`), and for infix operators, typically a precedence group — e.g. `infix operator **: MultiplicationPrecedence` — before you can define the actual function implementing that operator's behavior.",
      },
      {
        id: "q8",
        prompt: "What does a precedence group's `associativity: right` setting control, as used for a custom `**` operator?",
        options: [
          { id: "a", text: "Whether the operator can be used with `Double` values" },
          { id: "b", text: "How a chain of the same operator groups — `2 ** 3 ** 2` would group as `2 ** (3 ** 2)` under right-associativity, rather than `(2 ** 3) ** 2`" },
          { id: "c", text: "Whether the operator is visible outside the current file" },
          { id: "d", text: "Whether the operator requires `@inlinable`" },
        ],
        correctOptionId: "b",
        explanation: "Associativity determines how repeated uses of the same operator in one expression are grouped when there's no other operator to disambiguate. Right-associativity groups from the right (matching mathematical exponentiation convention), while left-associativity (the default for most operators) groups from the left.",
      },
      {
        id: "q9",
        prompt: "What does opting a type out of default copyability with `~Copyable` accomplish?",
        options: [
          { id: "a", text: "It makes the type conform to `Codable` automatically" },
          { id: "b", text: "It produces a type that can only be moved, never duplicated — useful for modeling unique resources (like a file handle or lock) where having two independent copies would be semantically incorrect" },
          { id: "c", text: "It converts the type into a class automatically" },
          { id: "d", text: "It has no actual effect; `~Copyable` is purely documentation" },
        ],
        correctOptionId: "b",
        explanation: "By default, every Swift type is implicitly copyable. `~Copyable` (Swift 5.9+) removes that default, enforcing at compile time that a value can only have one owner at a time — moved between owners, never duplicated — which is ideal for modeling genuinely unique resources.",
      },
      {
        id: "q10",
        prompt: "What happens if you try to use a `~Copyable` value again after passing it to a function parameter marked `consuming`?",
        options: [
          { id: "a", text: "Nothing — the value can still be used normally afterward" },
          { id: "b", text: "It's a compile-time error — the value was moved (consumed), so the original binding becomes unusable afterward" },
          { id: "c", text: "It automatically creates a fresh copy for continued use" },
          { id: "d", text: "It triggers a runtime crash, not a compile-time error" },
        ],
        correctOptionId: "b",
        explanation: "A `consuming` parameter takes full ownership of a noncopyable value, ending the caller's ability to use the original binding. The compiler catches any subsequent use of that now-moved-from binding as a compile-time error, enforcing single ownership statically.",
      },
      {
        id: "q11",
        prompt: "What is the difference between `borrowing` and `consuming` as parameter ownership modifiers?",
        options: [
          { id: "a", text: "They are synonyms with no functional difference" },
          { id: "b", text: "`borrowing` requests temporary, read-only access while the caller retains ownership; `consuming` takes full ownership, ending the caller's ability to use the value afterward" },
          { id: "c", text: "`borrowing` is only valid for classes; `consuming` only for structs" },
          { id: "d", text: "`consuming` is a synonym for `inout`" },
        ],
        correctOptionId: "b",
        explanation: "`borrowing` is like a temporary loan — the caller keeps ownership and can continue using the value after the call. `consuming` transfers ownership entirely to the function, after which the caller can no longer use that value (particularly enforced for noncopyable types).",
      },
      {
        id: "q12",
        prompt: "What is the practical motivation for `~Escapable` types?",
        options: [
          { id: "a", text: "To make types conform to `Sendable` automatically" },
          { id: "b", text: "To let low-level APIs hand out a lightweight, non-owning view into existing memory, while preventing that view from being stored or used beyond the lifetime of the memory it depends on" },
          { id: "c", text: "To speed up `Array` sorting" },
          { id: "d", text: "To enable automatic JSON encoding" },
        ],
        correctOptionId: "b",
        explanation: "`~Escapable` ties a value's validity to a bounded lifetime, preventing it from \"escaping\" beyond the scope where the underlying data it depends on is guaranteed valid — relevant for APIs like `Span` that provide a non-owning view into memory without allowing unsafe use after that memory's lifetime ends.",
      },
      {
        id: "q13",
        prompt: "Rank Swift's access control levels from most to least restrictive.",
        options: [
          { id: "a", text: "`public` < `package` < `internal` < `fileprivate` < `private`" },
          { id: "b", text: "`private` < `fileprivate` < `internal` < `package` < `public`/`open`" },
          { id: "c", text: "`internal` < `private` < `fileprivate` < `package` < `public`" },
          { id: "d", text: "They have no defined ordering relative to each other" },
        ],
        correctOptionId: "b",
        explanation: "From most restrictive to least: `private` (enclosing declaration and same-file extensions), `fileprivate` (same source file), `internal` (default, same module), `package` (same package, across modules), and `public`/`open` (visible outside the module, with `open` also allowing external subclassing/overriding).",
      },
      {
        id: "q14",
        prompt: "What specific problem does the `package` access level (introduced in Swift 5.9) solve?",
        options: [
          { id: "a", text: "It allows a type to conform to multiple protocols simultaneously" },
          { id: "b", text: "Before `package` existed, sharing code between multiple related modules within the same app or package (without exposing it externally) required either over-exposing it as `public` or duplicating code; `package` lets modules within the same package collaborate freely while staying invisible to external consumers" },
          { id: "c", text: "It replaces `private` entirely" },
          { id: "d", text: "It makes a type ABI-stable automatically" },
        ],
        correctOptionId: "b",
        explanation: "`package` fills the gap between `internal` (too restrictive for sharing across separate modules in one package) and `public` (too permissive, exposing the API to genuinely external consumers) — letting multiple modules within the same package share code without leaking it beyond the package's boundary.",
      },
      {
        id: "q15",
        prompt: "Why does an `@inlinable` public function requiring access to an `internal` property need that property marked `@usableFromInline`?",
        options: [
          { id: "a", text: "`@usableFromInline` is required on every property regardless of context" },
          { id: "b", text: "`@inlinable` exposes the function's implementation for inlining into external callers' compiled code; anything internal that implementation touches must be explicitly opted into being visible to that inlined code via `@usableFromInline`, without making it fully `public`" },
          { id: "c", text: "`@usableFromInline` converts the property into a computed property automatically" },
          { id: "d", text: "This combination is illegal in Swift and will not compile under any circumstances" },
        ],
        correctOptionId: "b",
        explanation: "Since `@inlinable` allows the compiler to copy a function's actual body into a different module's compiled output, any `internal` members that body references must be visible to that external code — `@usableFromInline` grants exactly that narrow visibility without fully publicizing the member as `public`.",
      },
      {
        id: "q16",
        prompt: "What does marking a public struct `@frozen` promise?",
        options: [
          { id: "a", text: "That the struct's methods can never be called from outside the module" },
          { id: "b", text: "That the struct's stored-property layout is fixed forever — the library author will never add, remove, or reorder its properties in a future version, unlocking performance optimizations for callers at the cost of future flexibility" },
          { id: "c", text: "That the struct will automatically become `Codable`" },
          { id: "d", text: "That the struct can only be used within the same file it's declared in" },
        ],
        correctOptionId: "b",
        explanation: "In a resilient (ABI-stable) library, a non-frozen public type's layout is treated as potentially changing in future versions, requiring indirection for safety. `@frozen` is the library author's binding promise that the layout is permanently fixed, which lets callers avoid that indirection — at the cost of giving up the ability to evolve the type's stored properties later.",
      },
      {
        id: "q17",
        prompt: "What does ABI stability specifically guarantee?",
        options: [
          { id: "a", text: "That source code compiles faster" },
          { id: "b", text: "That compiled binaries built against one version of a library continue working correctly against a newer version of that library's compiled binary, without needing recompilation" },
          { id: "c", text: "That a type will always be `Codable`" },
          { id: "d", text: "That a function will never throw an error" },
        ],
        correctOptionId: "b",
        explanation: "ABI (Application Binary Interface) stability is what allows Apple to ship the Swift standard library and system frameworks baked into the OS itself — apps compiled against an older OS version's library continue to run correctly against newer OS versions' updated library binaries, without needing to be recompiled.",
      },
      {
        id: "q18",
        prompt: "How does module stability differ from ABI stability?",
        options: [
          { id: "a", text: "They are the exact same guarantee under two different names" },
          { id: "b", text: "Module stability guarantees a library's public interface can be reliably consumed by a different (potentially newer) version of the Swift *compiler* than the one that built it; ABI stability guarantees binary compatibility across library *versions* at runtime" },
          { id: "c", text: "Module stability only applies to Objective-C code" },
          { id: "d", text: "ABI stability is a subset of module stability with no independent meaning" },
        ],
        correctOptionId: "b",
        explanation: "The two guarantees address different compatibility concerns: module stability is about compiler-version interoperability for consuming a library's public interface, while ABI stability is about runtime binary compatibility across different versions of the compiled library itself. Together, they enable both OS-embedded frameworks and cross-compiler-version binary distribution.",
      },
      {
        id: "q19",
        prompt: "Which of these Swift features is primarily relevant to typical application developers, versus primarily relevant to platform/library authors?",
        options: [
          { id: "a", text: "`@frozen`, `@inlinable`, and ABI stability are all primarily relevant to typical app developers" },
          { id: "b", text: "Key paths (11.1–11.2) and operator overloading (11.5) are commonly useful in everyday app code; `@frozen`, `@inlinable`/`@usableFromInline`, and ABI/module stability matter almost exclusively to framework/platform engineers" },
          { id: "c", text: "`@dynamicMemberLookup` is required in every SwiftUI view" },
          { id: "d", text: "`~Copyable` types are the most commonly used feature in typical app code" },
        ],
        correctOptionId: "b",
        explanation: "Key paths and operator overloading are broadly useful, everyday tools in application code. In contrast, `@frozen`, `@inlinable`/`@usableFromInline`, and ABI/module stability are specialized concerns almost exclusively relevant to authors of resilient, binary-distributed libraries (like Apple's own system frameworks), not typical app-level Swift code.",
      },
      {
        id: "q20",
        prompt: "In `static prefix func - (vector: Vector2D) -> Vector2D`, what role does the `prefix` keyword play?",
        options: [
          { id: "a", text: "It's optional and has no effect on how the operator is parsed" },
          { id: "b", text: "It declares that this overload of `-` applies to the *unary negation* usage (`-a`), as opposed to the binary subtraction usage (`a - b`), which would instead be declared as an `infix` (or default, unmarked) operator function" },
          { id: "c", text: "It means the function can only be called with two arguments" },
          { id: "d", text: "It marks the function as `@inlinable` implicitly" },
        ],
        correctOptionId: "b",
        explanation: "Swift distinguishes operator overloads by fixity — `prefix` for a unary operator appearing before its operand (like `-vector`), versus a binary/infix version of the same symbol (`lhs - rhs`) declared separately. Declaring `prefix func -` specifically implements the \"negate this single value\" behavior, distinct from subtraction.",
      },
    ],
  },
  {
    id: "result-builders-and-property-wrappers-quiz",
    title: "Result Builders and Property Wrappers Quiz",
    description: "20 questions on @resultBuilder, buildBlock/buildOptional/buildEither, how @ViewBuilder works internally, property wrapper anatomy, wrappedValue/projectedValue, and composing wrappers.",
    relatedArticleSlug: "result-builders-and-property-wrappers",
    questions: [
      {
        id: "q1",
        prompt: "What does a result builder fundamentally do to a specially-annotated closure?",
        options: [
          { id: "a", text: "It executes each statement and discards all but the last one's result" },
          { id: "b", text: "It intercepts the block at compile time and rewrites the sequence of statements into a series of method calls that combine them into one single value" },
          { id: "c", text: "It converts the closure into an asynchronous function automatically" },
          { id: "d", text: "It requires every statement inside to return `Void`" },
        ],
        correctOptionId: "b",
        explanation: "A result builder transforms what looks like ordinary sequential code into a single combined value by rewriting each statement in the block into calls against the builder's static methods (like `buildBlock`), which is exactly what allows SwiftUI's `VStack { Text(...); Text(...) }` to assemble a single composite view.",
      },
      {
        id: "q2",
        prompt: "What attribute marks a type as a result builder, and what method must it implement at minimum?",
        options: [
          { id: "a", text: "`@propertyWrapper`; it must implement `wrappedValue`" },
          { id: "b", text: "`@resultBuilder`; it must implement at least `buildBlock(_:)`" },
          { id: "c", text: "`@resultBuilder`; it must implement `dynamicallyCall`" },
          { id: "d", text: "`@ViewBuilder`; it must implement `body`" },
        ],
        correctOptionId: "b",
        explanation: "`@resultBuilder` is the attribute applied to the builder type itself (often an empty `enum`), and `buildBlock(_:)` is the minimum required static method — it combines a list of individual component values produced by the block's statements into one final result.",
      },
      {
        id: "q3",
        prompt: "In `func makeText(@StringBuilder content: () -> String) -> String`, what does the `@StringBuilder` annotation on the `content` parameter do?",
        options: [
          { id: "a", text: "It marks `content` as `@escaping`" },
          { id: "b", text: "It tells the compiler to apply the result-builder transformation to whatever closure is passed for that parameter, turning its bare statements into components combined by the builder" },
          { id: "c", text: "It requires `content` to always return an empty string" },
          { id: "d", text: "It's purely documentation with no compiler effect" },
        ],
        correctOptionId: "b",
        explanation: "Annotating a closure parameter's type with a specific result builder (`@StringBuilder content: () -> String`) instructs the compiler to rewrite the body of any closure passed there according to that builder's rules — each bare statement becomes a component fed into `buildBlock` (and other `build...` methods, as needed).",
      },
      {
        id: "q4",
        prompt: "What additional method(s) does a result builder need to support plain `if` statements with no `else` inside its block?",
        options: [
          { id: "a", text: "`buildBlock` alone is always sufficient for any control flow" },
          { id: "b", text: "`buildOptional`, since the condition might produce no component at all if the `if` doesn't execute" },
          { id: "c", text: "`buildEither(first:)` and `buildEither(second:)` only" },
          { id: "d", text: "`buildArray`" },
        ],
        correctOptionId: "b",
        explanation: "`buildOptional` handles the case where a conditional block inside the builder's closure might or might not produce a component, receiving an optional component and deciding how to fold \"nothing produced\" into the overall result.",
      },
      {
        id: "q5",
        prompt: "What is the purpose of `buildEither(first:)` and `buildEither(second:)` together?",
        options: [
          { id: "a", text: "They support `if`/`else` branches inside a result-builder-annotated closure — one for the true branch's result, one for the false branch's result" },
          { id: "b", text: "They are used exclusively for `for` loops" },
          { id: "c", text: "They convert the builder into a property wrapper" },
          { id: "d", text: "They are only relevant for `switch` statements with exactly two cases" },
        ],
        correctOptionId: "a",
        explanation: "When a result-builder block contains an `if`/`else`, the compiler calls `buildEither(first:)` for whichever branch is the \"true\" side and `buildEither(second:)` for the \"false\" side, letting the builder combine either outcome into a matching final type.",
      },
      {
        id: "q6",
        prompt: "What is SwiftUI's `@ViewBuilder`, in relation to the general result-builder mechanism described earlier in this section?",
        options: [
          { id: "a", text: "A completely separate, unrelated compiler feature specific to SwiftUI" },
          { id: "b", text: "A concrete result builder itself, using the exact same `@resultBuilder` mechanism — its `buildBlock` combines multiple `View` values into a composite view, and it implements `buildOptional`/`buildEither`/`buildArray` for `if`, `if-else`, and `for` support" },
          { id: "c", text: "A property wrapper used to declare `@State` properties" },
          { id: "d", text: "A macro that expands into SwiftUI view code" },
        ],
        correctOptionId: "b",
        explanation: "`@ViewBuilder` is not a special, separate SwiftUI-only feature — it's built entirely on top of the general `@resultBuilder` mechanism covered earlier, with `View`-specific combining logic (often via internal tuple-view wrapper types) instead of the simple string concatenation used in the section's minimal examples.",
      },
      {
        id: "q7",
        prompt: "What must a `@propertyWrapper`-annotated type define at minimum?",
        options: [
          { id: "a", text: "A `projectedValue` property" },
          { id: "b", text: "A `wrappedValue` property, which is what gets exposed whenever code accesses the property the wrapper is attached to" },
          { id: "c", text: "A static `buildBlock` method" },
          { id: "d", text: "A `deinit`" },
        ],
        correctOptionId: "b",
        explanation: "`wrappedValue` is the required property for any property wrapper — it's what the wrapper actually exposes (via `get`/`set`) to code interacting with the wrapped property, letting the wrapper interpose custom logic around every read and write.",
      },
      {
        id: "q8",
        prompt: "For `@Clamped(0...100) var volume: Int = 150`, what actually happens under the hood?",
        options: [
          { id: "a", text: "`volume` is stored exactly as written, with no additional transformation" },
          { id: "b", text: "Swift stores a hidden `_volume: Clamped` property, and every access to `volume` is transparently routed through `_volume.wrappedValue`" },
          { id: "c", text: "The property becomes computed and loses all storage entirely" },
          { id: "d", text: "`Clamped` replaces `Int` as `volume`'s actual declared type from the caller's perspective" },
        ],
        correctOptionId: "b",
        explanation: "The `@Clamped(...) var volume: Int = 150` syntax is sugar for a hidden backing property (`_volume`) of the wrapper's type, with `volume` itself transparently forwarding reads and writes to `_volume.wrappedValue` — entirely invisible to code that just uses `settings.volume` normally.",
      },
      {
        id: "q9",
        prompt: "What does a property wrapper's `projectedValue`, accessed via the `$` prefix, provide?",
        options: [
          { id: "a", text: "It's required on every property wrapper and always returns the same value as `wrappedValue`" },
          { id: "b", text: "An optional secondary value the wrapper can expose — this is exactly the mechanism behind `@State`'s `$isEnabled` producing a `Binding<Bool>`" },
          { id: "c", text: "It converts the property wrapper into a result builder" },
          { id: "d", text: "It's only accessible from within the wrapper's own implementation, never from outside" },
        ],
        correctOptionId: "b",
        explanation: "`projectedValue` is optional but, when defined, is accessed with a `$` prefix (`$count`), exposing a secondary representation distinct from the everyday `wrappedValue` — exactly how SwiftUI's `@State private var isEnabled: Bool` lets you write `$isEnabled` to get a `Binding<Bool>` for two-way data flow.",
      },
      {
        id: "q10",
        prompt: "In the `Logged<Value>` example, what does `counter.$count` actually access?",
        options: [
          { id: "a", text: "`counter.count` directly, with no distinction" },
          { id: "b", text: "`_count.projectedValue`, in exact parallel to how `counter.count` accesses `_count.wrappedValue`" },
          { id: "c", text: "A `Binding` type that doesn't exist in this example" },
          { id: "d", text: "It causes a compile-time error since `Logged` has no `projectedValue`" },
        ],
        correctOptionId: "b",
        explanation: "Just as `count` transparently routes to `_count.wrappedValue`, the `$count` syntax routes to `_count.projectedValue` — in the `Logged` example this returns a descriptive `String`, but the underlying mechanism is identical regardless of what type the wrapper chooses to expose as its projection.",
      },
      {
        id: "q11",
        prompt: "What is a realistic, common use case for a custom property wrapper like `@UserDefault(\"key\")`?",
        options: [
          { id: "a", text: "Making a property immutable" },
          { id: "b", text: "Wrapping a cross-cutting concern — here, automatically persisting a value to `UserDefaults` on every write and reading it back on access — so that behavior can be attached declaratively without repeating get/set logic manually at every use site" },
          { id: "c", text: "Converting a stored property into a result builder" },
          { id: "d", text: "Preventing a property from ever being read" },
        ],
        correctOptionId: "b",
        explanation: "This is exactly the motivating use case for property wrappers: packaging a repeated pattern (here, UserDefaults-backed persistence) into a reusable wrapper type, so any property can opt in with a simple `@UserDefault(\"key\")` annotation instead of hand-writing the same get/set logic repeatedly.",
      },
      {
        id: "q12",
        prompt: "Can property wrappers generally be applied to computed properties?",
        options: [
          { id: "a", text: "Yes, with no restrictions at all" },
          { id: "b", text: "No — property wrappers generally cannot be applied to computed properties, since the wrapper itself needs to own the actual storage, which only stored properties provide" },
          { id: "c", text: "Only if the computed property has both `get` and `set`" },
          { id: "d", text: "Only for property wrappers with a `projectedValue`" },
        ],
        correctOptionId: "b",
        explanation: "Since a property wrapper's whole mechanism relies on owning a hidden backing storage property (like `_volume`), it fundamentally requires a stored property to attach to — computed properties have no storage of their own for the wrapper to take over.",
      },
      {
        id: "q13",
        prompt: "In `struct Form { @Capitalized @Trimmed var name: String = \"  ada  \" }`, which wrapper's transformation is applied first, closest to the raw initial value?",
        options: [
          { id: "a", text: "`Capitalized`, since it's listed first (outermost)" },
          { id: "b", text: "`Trimmed`, since composed property wrappers apply innermost-first — the wrapper closest to the property name applies to the raw value before the outer wrapper processes its result" },
          { id: "c", text: "Both apply simultaneously with no defined order" },
          { id: "d", text: "Neither actually runs — only the last-listed wrapper takes effect" },
        ],
        correctOptionId: "b",
        explanation: "When wrappers are stacked, the innermost one (closest to the property declaration, `@Trimmed` here) processes the raw value first; its result is then passed to the next outer wrapper (`@Capitalized`). This is why `\"  ada  \"` first gets trimmed to `\"ada\"`, then capitalized to `\"Ada\"`.",
      },
      {
        id: "q14",
        prompt: "What is a practical caution around composing (stacking) multiple property wrappers on one property?",
        options: [
          { id: "a", text: "Swift doesn't actually support this at all — it's a compile-time error" },
          { id: "b", text: "While technically supported, deeply-stacked wrappers quickly become hard to reason about and debug, so most real-world code uses at most one property wrapper per property" },
          { id: "c", text: "Composed wrappers always execute in a random, non-deterministic order" },
          { id: "d", text: "It requires every wrapper involved to have an identical `wrappedValue` type" },
        ],
        correctOptionId: "b",
        explanation: "Composition works (as shown with `@Capitalized @Trimmed`), but the lesson explicitly notes that reasoning about and debugging stacked wrappers becomes difficult quickly, which is why most practical Swift code sticks to a single wrapper per property, reserving composition for narrow, well-understood cases.",
      },
      {
        id: "q15",
        prompt: "What does `TupleView((v0, v1))` conceptually represent in the simplified `SimpleViewBuilder` example modeling `@ViewBuilder`'s internals?",
        options: [
          { id: "a", text: "A random name with no functional significance" },
          { id: "b", text: "The mechanism by which a result builder's `buildBlock` combines multiple individual `View` values into a single, strongly-typed composite view value that can then be laid out" },
          { id: "c", text: "A property wrapper's projected value" },
          { id: "d", text: "An error type thrown when views don't combine correctly" },
        ],
        correctOptionId: "b",
        explanation: "This illustrates how `buildBlock` for view-combining result builders wraps multiple distinct view values into one combined, nested composite type (here a simplified `TupleView`) — this is conceptually how a `VStack { Text(...); Text(...) }` closure's two separate `Text` statements become one single value the `VStack` can actually render and lay out.",
      },
      {
        id: "q16",
        prompt: "Why does a result builder need a separate `buildArray` method (not mentioned as part of the minimal example, but referenced as a general capability)?",
        options: [
          { id: "a", text: "To support `for` loops producing multiple components inside the builder's block" },
          { id: "b", text: "To support optional bindings like `if let`" },
          { id: "c", text: "To support `switch` statements specifically" },
          { id: "d", text: "`buildArray` doesn't exist as a real result-builder method" },
        ],
        correctOptionId: "a",
        explanation: "Just as `buildOptional` and `buildEither` extend a minimal `buildBlock`-only builder to support `if` and `if/else`, `buildArray` is the corresponding method needed to support `for` loops that might produce a variable number of components within the builder's block — used by `@ViewBuilder` to support looping over data to produce multiple views.",
      },
      {
        id: "q17",
        prompt: "What is the underlying reason `@Clamped`'s initializer takes a `wrappedValue: Int` parameter specifically named that way?",
        options: [
          { id: "a", text: "It's an arbitrary naming convention with no special meaning" },
          { id: "b", text: "Swift specifically recognizes an initializer parameter named `wrappedValue` as the one used to translate the property's declared initial value (`= 150`) into the wrapper's own initializer call" },
          { id: "c", text: "`wrappedValue` as a parameter name is required for every Swift initializer, unrelated to property wrappers" },
          { id: "d", text: "It must always be the second parameter in the initializer's parameter list" },
        ],
        correctOptionId: "b",
        explanation: "Swift specifically looks for an `init(wrappedValue:)` parameter to bridge the property declaration's assigned initial value (`var volume: Int = 150`) into a call to the wrapper's initializer — this is what lets `@Clamped(0...100) var volume: Int = 150` work as syntactic sugar for constructing the wrapper with that initial value.",
      },
      {
        id: "q18",
        prompt: "What does the result-builder mechanism ultimately allow that ordinary Swift closures don't, as demonstrated by `makeText { \"Hello\"; \"World\" }`?",
        options: [
          { id: "a", text: "Executing multiple unrelated statements and printing each one automatically" },
          { id: "b", text: "Treating a sequence of bare expression statements (which would normally just be individually discarded, aside from possibly the last one) as a list of components to be explicitly combined into a single meaningful result, via the builder's `build...` methods" },
          { id: "c", text: "Automatically converting strings to numbers" },
          { id: "d", text: "Skipping compilation entirely for the closure's body" },
        ],
        correctOptionId: "b",
        explanation: "Without a result builder, a closure containing several bare string-literal statements in a row wouldn't do anything meaningful with all of them — result builders are precisely what capture each statement as a \"component\" and feed them through `buildBlock` (and friends) to produce one deliberate, combined value.",
      },
      {
        id: "q19",
        prompt: "Which statement accurately distinguishes a property wrapper's `wrappedValue` from its `projectedValue`?",
        options: [
          { id: "a", text: "They are always identical in type and value" },
          { id: "b", text: "`wrappedValue` is required and is what ordinary property access (`property`) exposes; `projectedValue` is optional and is what `$`-prefixed access (`$property`) exposes, often providing a different, complementary representation" },
          { id: "c", text: "`projectedValue` is required, while `wrappedValue` is optional" },
          { id: "d", text: "Only classes can define a `projectedValue`" },
        ],
        correctOptionId: "b",
        explanation: "Every property wrapper must define `wrappedValue` (the everyday value exposed by plain property access), while `projectedValue` is an optional addition exposed via the `$` prefix, typically providing a different kind of value related to the wrapper — like a `Binding` for `@State`, or a descriptive `String` for the `Logged` example.",
      },
      {
        id: "q20",
        prompt: "Why is it accurate to say SwiftUI's declarative view syntax \"isn't special-cased compiler magic exclusive to SwiftUI\"?",
        options: [
          { id: "a", text: "Because SwiftUI doesn't actually use result builders at all" },
          { id: "b", text: "Because `@ViewBuilder` is built using the same general-purpose `@resultBuilder` feature (with `buildBlock`, `buildOptional`, `buildEither`, `buildArray`) that's available to any Swift developer to define their own custom result builders for entirely unrelated purposes, as shown with the `StringBuilder` examples" },
          { id: "c", text: "Because SwiftUI views must be written using `@propertyWrapper` instead" },
          { id: "d", text: "Because result builders only work within Apple's own frameworks" },
        ],
        correctOptionId: "b",
        explanation: "Result builders are a general Swift language feature, not something exclusive to or hard-coded for SwiftUI — `@ViewBuilder` is simply one particular, concrete result builder Apple defined using the exact same mechanism (as demonstrated by building a working `StringBuilder` from scratch), which any developer can replicate for their own domain-specific needs.",
      },
    ],
  },
  {
    id: "macros-quiz",
    title: "Macros Quiz",
    description: "20 questions on freestanding vs attached macros, member/peer/accessor/extension macros, SwiftSyntax, testing macros with assertMacroExpansion, and macro build-time cost.",
    relatedArticleSlug: "macros",
    questions: [
      {
        id: "q1",
        prompt: "What is a Swift macro, fundamentally?",
        options: [
          { id: "a", text: "A runtime reflection mechanism for inspecting types" },
          { id: "b", text: "Code that runs at compile time, inspecting and generating Swift source code as part of the build itself" },
          { id: "c", text: "A special kind of closure that captures its enclosing scope" },
          { id: "d", text: "A property wrapper variant introduced in Swift 6" },
        ],
        correctOptionId: "b",
        explanation: "Macros execute during compilation, not at runtime — they inspect and generate real Swift source code as part of the build process, introduced in Swift 5.9 specifically to replace older external codegen tools.",
      },
      {
        id: "q2",
        prompt: "What was the older, pre-macro approach that macros were designed to replace?",
        options: [
          { id: "a", text: "Property wrappers" },
          { id: "b", text: "External scripts or tools (like Sourcery, or hand-rolled Python scripts) that scanned source files and generated additional `.swift` files before the real build started" },
          { id: "c", text: "Objective-C categories" },
          { id: "d", text: "Result builders" },
        ],
        correctOptionId: "b",
        explanation: "Before macros, generating boilerplate code often meant running a separate external tool as a pre-build step, producing generated `.swift` files that then got compiled alongside hand-written code — a workflow requiring a separate tool and build phase to stay in sync.",
      },
      {
        id: "q3",
        prompt: "What key advantage do macros have over the older external-codegen-script approach?",
        options: [
          { id: "a", text: "Macros run after the app has already shipped to users" },
          { id: "b", text: "Macros run inside the normal Swift compilation pipeline, are fully type-checked, integrate with Xcode's autocomplete and \"Expand Macro\" inspector, and don't require a separate external tool or build phase" },
          { id: "c", text: "Macros eliminate the need for any type checking at all" },
          { id: "d", text: "Macros can only be used with SwiftUI" },
        ],
        correctOptionId: "b",
        explanation: "Because macros are a first-class part of the compiler itself, their generated code is type-checked like any other Swift code and is directly inspectable within Xcode — unlike external generator scripts, which require a separate tool, a separate build phase, and produce code the IDE doesn't understand as directly.",
      },
      {
        id: "q4",
        prompt: "What distinguishes a freestanding macro like `#stringify(1 + 2)` from an attached macro?",
        options: [
          { id: "a", text: "Freestanding macros are attached to a type declaration with `@`; attached macros stand alone with `#`" },
          { id: "b", text: "Freestanding macros are invoked directly with a `#` prefix, standing on their own at a call site rather than being attached to an existing declaration" },
          { id: "c", text: "There is no real difference between the two categories" },
          { id: "d", text: "Freestanding macros can only be used inside SwiftUI views" },
        ],
        correctOptionId: "b",
        explanation: "Freestanding macros use `#` and appear as independent expressions/statements in code (like `#stringify(...)`), while attached macros use `@` and modify an existing declaration they're applied to (like `@Observable` on a class).",
      },
      {
        id: "q5",
        prompt: "What does `#stringify(1 + 2)` do that an ordinary function call could not?",
        options: [
          { id: "a", text: "Nothing special — a regular function could achieve the identical result" },
          { id: "b", text: "It expands at compile time into code that both evaluates the expression *and* captures its literal, original source text (`\"1 + 2\"`) as a string — something a function can't do, since it only ever receives the already-evaluated result" },
          { id: "c", text: "It converts the expression into a `Result` type" },
          { id: "d", text: "It only works with string literals, not arithmetic expressions" },
        ],
        correctOptionId: "b",
        explanation: "A macro operates on the actual source code at compile time, so it can capture the literal text of an expression as written — `\"1 + 2\"` — alongside its evaluated result (`3`). An ordinary function only ever receives the already-computed value `3`, with no access to the original source text that produced it.",
      },
      {
        id: "q6",
        prompt: "What does a member macro add, as illustrated by `@AddCompletionHandler` generating a completion-handler-based overload of an `async` function?",
        options: [
          { id: "a", text: "A new protocol conformance only" },
          { id: "b", text: "New members (properties, methods) to the type or declaration it's attached to — here, an additional companion function alongside the original" },
          { id: "c", text: "New whitespace formatting only" },
          { id: "d", text: "A new file to the Xcode project" },
        ],
        correctOptionId: "b",
        explanation: "A member macro's job is to add new members to whatever it's attached to. In the `@AddCompletionHandler` example, it generates an entirely new completion-handler-style function alongside the hand-written `async` version, so the developer only has to write one version by hand.",
      },
      {
        id: "q7",
        prompt: "What is the key difference between an accessor macro and a member macro?",
        options: [
          { id: "a", text: "They are exactly the same feature under two different names" },
          { id: "b", text: "A member macro adds new members alongside an existing declaration; an accessor macro instead adds `get`/`set`/`willSet`/`didSet` logic directly onto an *existing* stored property, without introducing a separate wrapper type" },
          { id: "c", text: "Accessor macros can only be used with classes, never structs" },
          { id: "d", text: "Member macros can only generate computed properties" },
        ],
        correctOptionId: "b",
        explanation: "An accessor macro (like the conceptual `@Clamped` example) attaches directly to an existing property and adds accessor logic (`get`/`set`) to it in place — achieving similar clamping behavior to the section 12 property wrapper example, but generating logic directly on the original property rather than introducing a separate wrapper struct.",
      },
      {
        id: "q8",
        prompt: "What does an extension macro (and its more specific form, a conformance macro) typically generate?",
        options: [
          { id: "a", text: "Only new local variables inside a function" },
          { id: "b", text: "An entire `extension` block for the type it's attached to — often used to add protocol conformances along with the generated implementation code needed to satisfy them, as with `@Observable`" },
          { id: "c", text: "A new freestanding `#` macro" },
          { id: "d", text: "A new Xcode project template" },
        ],
        correctOptionId: "b",
        explanation: "Extension macros generate whole `extension` blocks, commonly adding protocol conformances plus whatever generated implementation is needed to satisfy them — this is exactly how `@Observable` adds both new members and the necessary conformance/tracking machinery in one annotation.",
      },
      {
        id: "q9",
        prompt: "What is SwiftSyntax, and what role does it play in macro implementations?",
        options: [
          { id: "a", text: "A runtime library for parsing JSON" },
          { id: "b", text: "Apple's library for parsing and manipulating Swift source code as a structured tree of typed nodes, which macro implementations use to inspect and transform code, rather than manipulating raw text/strings directly" },
          { id: "c", text: "A replacement for the Swift standard library's collection types" },
          { id: "d", text: "A tool exclusively used for UI testing" },
        ],
        correctOptionId: "b",
        explanation: "SwiftSyntax represents Swift source code as a structured tree (functions, expressions, and so on, each as typed nodes), which is what macro implementations actually operate on — the key mental model is that macros work on structured syntax trees, not on raw text via string manipulation.",
      },
      {
        id: "q10",
        prompt: "What does \"trivia\" refer to in the context of SwiftSyntax?",
        options: [
          { id: "a", text: "Error messages produced during macro expansion" },
          { id: "b", text: "The \"non-meaningful\" parts of source code — whitespace, comments, line breaks — which SwiftSyntax tracks separately from meaningful syntax nodes, allowing macros to preserve sensible formatting in generated output" },
          { id: "c", text: "A category of freestanding macros" },
          { id: "d", text: "Deprecated syntax nodes no longer supported by the compiler" },
        ],
        correctOptionId: "b",
        explanation: "Trivia covers whitespace, comments, and similar non-semantic source elements. SwiftSyntax tracks this separately from the meaningful syntax tree, which is what allows macro-generated code to preserve reasonable formatting rather than collapsing everything into an unreadable single line.",
      },
      {
        id: "q11",
        prompt: "Why does a macro package split into two separate targets — a declaration and a compiler-plugin implementation?",
        options: [
          { id: "a", text: "It's an arbitrary Xcode project convention with no functional purpose" },
          { id: "b", text: "Macro implementations need to run as compiler plugins using the full SwiftSyntax/compiler-plugin infrastructure during the build; the declaration is a lightweight, ordinary-looking signature that consuming app code imports and calls, without needing SwiftSyntax as a dependency itself" },
          { id: "c", text: "Declarations run at runtime, while implementations run only in Xcode Previews" },
          { id: "d", text: "This split is only required for freestanding macros, not attached ones" },
        ],
        correctOptionId: "b",
        explanation: "The implementation half needs the heavier SwiftSyntax/compiler-plugin machinery and only runs during compilation, never shipping inside the app. The declaration half is what consumer code actually imports and calls, kept lightweight so consuming app targets don't need to depend on SwiftSyntax themselves.",
      },
      {
        id: "q12",
        prompt: "What does `assertMacroExpansion` let you do when testing a macro?",
        options: [
          { id: "a", text: "Measure the macro's exact build-time performance cost" },
          { id: "b", text: "Verify that a macro produces exactly the expected expanded source code for a given piece of input source code, treating expansion as a testable input-to-output transformation" },
          { id: "c", text: "Automatically fix bugs in a macro's implementation" },
          { id: "d", text: "Convert a macro into a property wrapper for testing purposes" },
        ],
        correctOptionId: "b",
        explanation: "`assertMacroExpansion` is a dedicated SwiftSyntax testing utility that lets you write ordinary unit tests pinning down the exact expected generated source for a given macro invocation — catching regressions in expansion behavior the same way tests catch regressions elsewhere in a codebase.",
      },
      {
        id: "q13",
        prompt: "What does Xcode's \"Expand Macro\" contextual menu option let you do?",
        options: [
          { id: "a", text: "Automatically rewrite the macro's implementation source code" },
          { id: "b", text: "See exactly what a specific macro usage expands to, revealed inline nested underneath the macro call in the editor — without needing to write and run a separate test" },
          { id: "c", text: "Convert an attached macro into a freestanding one" },
          { id: "d", text: "Disable the macro for the current build only" },
        ],
        correctOptionId: "b",
        explanation: "\"Expand Macro\" is Xcode's built-in, everyday debugging tool for macros — it reveals the actual generated code for a specific usage directly in the editor, which is especially useful for understanding third-party/system macros or diagnosing confusing compiler errors that originate from expanded code.",
      },
      {
        id: "q14",
        prompt: "Why can heavy macro usage across a large codebase measurably slow down build times?",
        options: [
          { id: "a", text: "Macros are executed once per app launch, not per build" },
          { id: "b", text: "Each macro executes as a separate compiler-plugin process during every relevant build, and the SwiftSyntax-based parsing/expansion work involved isn't free — every file using a given macro invokes that plugin process and its expansion logic during compilation" },
          { id: "c", text: "Macros require a live network connection to expand" },
          { id: "d", text: "Macros always run on the main thread, blocking the UI during builds" },
        ],
        correctOptionId: "b",
        explanation: "Because macro expansion happens as part of compilation via a separate compiler-plugin process, and that process must parse and analyze source using SwiftSyntax, extensive macro usage across many files adds real, cumulative build-time cost — a genuine practical trade-off to weigh against the convenience macros provide.",
      },
      {
        id: "q15",
        prompt: "According to the lesson's guidance, when should you prefer a protocol extension or property wrapper over writing a custom macro?",
        options: [
          { id: "a", text: "Always — macros should never be used under any circumstances" },
          { id: "b", text: "When the simpler mechanism (a protocol extension's default implementation, or a property wrapper's get/set interception) can solve the problem just as well, since macros carry real implementation complexity and build-time cost that aren't justified if a simpler tool suffices" },
          { id: "c", text: "Only when targeting older Swift versions that don't support macros" },
          { id: "d", text: "Property wrappers and protocol extensions should always be replaced by macros for consistency" },
        ],
        correctOptionId: "b",
        explanation: "The lesson's practical guidance is to reach for a macro specifically when you need compile-time source inspection/generation that protocols and property wrappers genuinely can't achieve (like capturing literal source text, or generating cross-cutting tracking machinery) — not as a default replacement for simpler, established mechanisms.",
      },
      {
        id: "q16",
        prompt: "What is a genuine capability that macros provide, which neither protocol extensions nor property wrappers can achieve?",
        options: [
          { id: "a", text: "Adding a computed property to a type" },
          { id: "b", text: "Compile-time inspection or generation involving the actual source structure — like `#stringify` capturing an expression's literal source text, or `@Observable` generating an entire type's tracking implementation across multiple stored properties" },
          { id: "c", text: "Enabling `Equatable` conformance" },
          { id: "d", text: "Supporting default parameter values" },
        ],
        correctOptionId: "b",
        explanation: "Both `#stringify` (capturing literal source text) and `@Observable` (generating comprehensive per-property tracking machinery across an entire type) require actual compile-time source inspection and generation — capabilities that neither a protocol extension's default method nor a single property wrapper's get/set interception can replicate on their own.",
      },
      {
        id: "q17",
        prompt: "What does `@freestanding(expression)` (as part of a macro's declaration) indicate about the macro being declared?",
        options: [
          { id: "a", text: "That the macro is attached to a type and adds new members" },
          { id: "b", text: "That the macro is a freestanding macro expanding into an expression, invoked with `#` at its call site rather than being attached to a declaration with `@`" },
          { id: "c", text: "That the macro can only be used inside test targets" },
          { id: "d", text: "That the macro requires SwiftUI to function" },
        ],
        correctOptionId: "b",
        explanation: "The `@freestanding(expression)` attribute on a macro's declaration specifies its kind and role — here, a macro that stands alone (invoked via `#`) and expands into an expression, distinguishing it from the `@attached(...)` declarations used for member/peer/accessor/extension macros.",
      },
      {
        id: "q18",
        prompt: "In the `StringifyMacro` implementation example, what does the `expansion(of:in:)` method receive and return?",
        options: [
          { id: "a", text: "It receives a `String` and returns a `Bool`" },
          { id: "b", text: "It receives the syntax node representing the macro's usage (via SwiftSyntax types) and returns the generated `ExprSyntax` representing the expanded replacement code" },
          { id: "c", text: "It receives a `URL` and returns `Data`" },
          { id: "d", text: "It receives nothing and returns `Void`" },
        ],
        correctOptionId: "b",
        explanation: "A macro's expansion implementation operates on SwiftSyntax types — it receives the structured syntax node representing how the macro was invoked (`of node: some FreestandingMacroExpansionSyntax`) and produces the replacement code as a syntax node (`ExprSyntax`), which the compiler then substitutes at the macro's call site.",
      },
      {
        id: "q19",
        prompt: "Why is it inaccurate to think of macros as \"fancy text templates\" performing simple string substitution?",
        options: [
          { id: "a", text: "Because macros can only produce empty output" },
          { id: "b", text: "Because macros operate on a structured syntax tree of typed nodes (via SwiftSyntax), inspecting and generating real, type-checked Swift code — not manipulating raw text strings the way a simple template engine would" },
          { id: "c", text: "Because macros are actually resolved entirely at runtime, not compile time" },
          { id: "d", text: "Because macros cannot access any information about the code they're attached to" },
        ],
        correctOptionId: "b",
        explanation: "The section explicitly frames this as a key mental-model correction: macros work with SwiftSyntax's structured representation of source code (typed nodes for expressions, declarations, and so on, with trivia tracked separately), not by doing naive string find-and-replace on raw source text.",
      },
      {
        id: "q20",
        prompt: "Which macro kind would be most appropriate for a scenario requiring an entirely new standalone expression to be generated at an arbitrary call site, not attached to any existing declaration?",
        options: [
          { id: "a", text: "A member macro" },
          { id: "b", text: "An accessor macro" },
          { id: "c", text: "A freestanding expression macro" },
          { id: "d", text: "An extension macro" },
        ],
        correctOptionId: "c",
        explanation: "Freestanding expression macros (like `#stringify`) are specifically designed to stand alone at a call site and expand into an expression, independent of any existing declaration — unlike member, accessor, and extension macros, which are all attached macros modifying something that already exists.",
      },
    ],
  },
  {
    id: "standard-library-deep-dive-quiz",
    title: "Standard Library Deep Dive Quiz",
    description: "20 questions on Sequence/Collection protocols, Codable synthesis and CodingKeys, polymorphic JSON, regex literals and RegexBuilder, FormatStyle, Duration/Clock, and Observation.",
    relatedArticleSlug: "standard-library-deep-dive",
    questions: [
      {
        id: "q1",
        prompt: "What two requirements does the `Sequence` protocol fundamentally build on?",
        options: [
          { id: "a", text: "`startIndex` and `endIndex`" },
          { id: "b", text: "A `makeIterator()` method returning a type conforming to `IteratorProtocol`, which itself requires just a `next()` method returning the next element or `nil` when exhausted" },
          { id: "c", text: "`subscript(position:)` and `index(after:)`" },
          { id: "d", text: "`Equatable` and `Hashable` conformance" },
        ],
        correctOptionId: "b",
        explanation: "`Sequence` requires only `makeIterator()`, and `IteratorProtocol` requires only `next()`. Every `for-in` loop desugars to calling `makeIterator()` once and then repeatedly calling `next()` until it returns `nil` — the most fundamental iteration mechanism in Swift.",
      },
      {
        id: "q2",
        prompt: "What does `Collection` add on top of `Sequence`?",
        options: [
          { id: "a", text: "Thread safety" },
          { id: "b", text: "Indexed, subscript-based access — `startIndex`, `endIndex`, `subscript(position:)`, and `index(after:)` — enabling more than just sequential one-at-a-time iteration" },
          { id: "c", text: "Automatic `Codable` conformance" },
          { id: "d", text: "Support for `async`/`await`" },
        ],
        correctOptionId: "b",
        explanation: "While `Sequence` only supports walking through elements one at a time via an iterator, `Collection` adds a full index-based model, enabling direct subscript access (`collection[index]`) and repeatable traversal, not just single-pass iteration.",
      },
      {
        id: "q3",
        prompt: "Why does `String` conform to `BidirectionalCollection` but not `RandomAccessCollection`, while `Array` conforms to both?",
        options: [
          { id: "a", text: "`String` doesn't actually support any indexed access at all" },
          { id: "b", text: "`RandomAccessCollection` requires that advancing an index by an arbitrary offset or computing distance between indices be O(1); because `String`'s grapheme clusters have variable byte-width, this requires scanning rather than instant arithmetic, unlike `Array`'s fixed-width elements" },
          { id: "c", text: "`String` is not a `Collection` at all" },
          { id: "d", text: "`Array` cannot be traversed backward, unlike `String`" },
        ],
        correctOptionId: "b",
        explanation: "This directly connects back to sections 1.7/1.9: because Swift `Character`s can vary in byte-width, jumping forward N characters in a `String` requires actual scanning, disqualifying it from the O(1) guarantee `RandomAccessCollection` requires — while `Array`'s fixed-size elements make arbitrary offset jumps instant.",
      },
      {
        id: "q4",
        prompt: "What is the difference between `MutableCollection` and `RangeReplaceableCollection`?",
        options: [
          { id: "a", text: "They are identical protocols with different names" },
          { id: "b", text: "`MutableCollection` allows changing existing elements in place via subscript assignment without changing the collection's length; `RangeReplaceableCollection` additionally supports insertion, removal, and appending, which change the collection's actual size" },
          { id: "c", text: "`MutableCollection` only applies to `Set` and `Dictionary`" },
          { id: "d", text: "`RangeReplaceableCollection` is a subtype exclusive to `String`" },
        ],
        correctOptionId: "b",
        explanation: "`MutableCollection` covers \"editing what's already there\" (`arr[0] = 100`), which doesn't affect the total count. `RangeReplaceableCollection` covers operations that change the collection's size, like `append`, `insert`, and `remove` — this is specifically what backs `Array`'s mutation methods from section 3.2.",
      },
      {
        id: "q5",
        prompt: "What is the practical payoff of implementing a custom `Collection` conformance (as with `FixedStack` in the lesson) by satisfying just four requirements?",
        options: [
          { id: "a", text: "Nothing — you must still implement `map`, `filter`, and `reduce` yourself" },
          { id: "b", text: "Every higher-order operation from section 3 — `map`, `filter`, `reduce`, `first(where:)`, and dozens more — becomes available automatically, entirely for free, once `startIndex`, `endIndex`, `subscript`, and `index(after:)` are implemented" },
          { id: "c", text: "It automatically makes the type `Codable`" },
          { id: "d", text: "It's required before a type can conform to `Equatable`" },
        ],
        correctOptionId: "b",
        explanation: "This is the core benefit of Swift's protocol hierarchy design: implementing a small number of primitive requirements unlocks a large surface area of default-implemented operations (`map`, `filter`, `reduce`, and many more) automatically, without writing any of them by hand.",
      },
      {
        id: "q6",
        prompt: "For a simple struct like `struct User: Codable { var name: String; var age: Int }`, what is required to make it encode/decode correctly?",
        options: [
          { id: "a", text: "Manually implementing `init(from:)` and `encode(to:)`" },
          { id: "b", text: "Nothing beyond the `Codable` conformance declaration itself, as long as every property is itself `Codable` — Swift synthesizes the full implementation automatically" },
          { id: "c", text: "A `CodingKeys` enum is always mandatory" },
          { id: "d", text: "The struct must also conform to `Equatable`" },
        ],
        correctOptionId: "b",
        explanation: "For simple structs whose properties are all themselves `Codable` types, Swift automatically synthesizes a complete, correct `Codable` implementation — no manual code required, and no `CodingKeys` needed unless you specifically need to remap key names.",
      },
      {
        id: "q7",
        prompt: "What is the purpose of a `CodingKeys` enum, as in mapping a Swift property `fullName` to a JSON key `\"full_name\"`?",
        options: [
          { id: "a", text: "It disables `Codable` synthesis entirely" },
          { id: "b", text: "It remaps Swift property names to different external JSON key names, while still keeping automatic synthesis for the rest of the encoding/decoding logic" },
          { id: "c", text: "It's required for every `Codable` type, with no exceptions" },
          { id: "d", text: "It converts the struct into a class" },
        ],
        correctOptionId: "b",
        explanation: "`CodingKeys` lets you handle the common mismatch between Swift's `camelCase` convention and JSON's frequent `snake_case` convention, remapping specific property names to specific JSON keys via the enum's raw values, without needing to abandon automatic synthesis altogether.",
      },
      {
        id: "q8",
        prompt: "When would you need to write custom `init(from:)` and `encode(to:)` implementations by hand, rather than relying on synthesis?",
        options: [
          { id: "a", text: "Whenever a type has more than two properties" },
          { id: "b", text: "When the external representation requires a transformation synthesis can't express — like storing Fahrenheit externally while representing the value internally as Celsius" },
          { id: "c", text: "Only when the type is a class, never a struct" },
          { id: "d", text: "Custom implementations are never actually necessary in modern Swift" },
        ],
        correctOptionId: "b",
        explanation: "As shown in the `Temperature` example, when the internal Swift representation (`celsius`) genuinely differs from the external representation (`fahrenheit`) — requiring an actual computed transformation during encode/decode — automatic synthesis can't express that, and a manual `init(from:)`/`encode(to:)` implementation becomes necessary.",
      },
      {
        id: "q9",
        prompt: "What is the standard approach for decoding polymorphic JSON, where a field's shape depends on the value of another field (like a `\"type\"` discriminator)?",
        options: [
          { id: "a", text: "It's impossible with `Codable` and requires a completely separate parsing library" },
          { id: "b", text: "Decode the discriminator field first (often via a preliminary keyed container), then branch to decode the rest of the payload according to that discriminator's value — mapping naturally onto an enum with associated values" },
          { id: "c", text: "Automatic synthesis handles this case with zero additional code" },
          { id: "d", text: "Convert the entire JSON payload to a `Dictionary<String, Any>` and never use `Codable` at all" },
        ],
        correctOptionId: "b",
        explanation: "Polymorphic JSON requires manual `init(from:)` logic: decode the discriminator field first, then use that value to decide which concrete type's decoding logic to invoke for the remainder of the payload — this maps naturally onto an enum with associated values, one case per possible shape.",
      },
      {
        id: "q10",
        prompt: "What does `decoder.keyDecodingStrategy = .convertFromSnakeCase` accomplish?",
        options: [
          { id: "a", text: "It converts every decoded string value to lowercase" },
          { id: "b", text: "It applies a global convention across the entire decode, automatically converting `snake_case` JSON keys to `camelCase` Swift property names, without needing per-property `CodingKeys` entries" },
          { id: "c", text: "It's only relevant for encoding, not decoding" },
          { id: "d", text: "It requires a custom `CodingKeys` enum to function at all" },
        ],
        correctOptionId: "b",
        explanation: "Decoder/encoder strategies apply a convention globally across an entire decode/encode operation — `.convertFromSnakeCase` specifically handles the common `snake_case`-to-`camelCase` mismatch automatically, avoiding the need for explicit `CodingKeys` mappings for every mismatched property.",
      },
      {
        id: "q11",
        prompt: "What is a key advantage of Swift's regex literals (`/pattern/`, since Swift 5.7) over building a regex from a plain string?",
        options: [
          { id: "a", text: "Regex literals run faster on every platform, with no other differences" },
          { id: "b", text: "Regex literals are checked and type-inferred at compile time (catching malformed patterns before runtime) and provide strongly-typed capture groups, rather than the stringly-typed, manually-indexed approach of older regex APIs" },
          { id: "c", text: "Regex literals can only match ASCII characters" },
          { id: "d", text: "Regex literals cannot be used with `firstMatch(of:)`" },
        ],
        correctOptionId: "b",
        explanation: "Because a regex literal is parsed by the compiler itself, malformed patterns are caught before runtime, and capture groups become strongly-typed properties on the match result (`match.1`, `match.2` as `Substring`), a meaningful improvement over older APIs like `NSRegularExpression`, which required manual, stringly-typed index-based group access.",
      },
      {
        id: "q12",
        prompt: "What is the main benefit of `RegexBuilder` over writing a dense inline regex pattern string?",
        options: [
          { id: "a", text: "`RegexBuilder` regexes run without any performance cost, unlike literal patterns" },
          { id: "b", text: "It lets you construct complex patterns declaratively from named, reusable pieces, generally offering much better readability and named (rather than positionally-numbered) capture group access" },
          { id: "c", text: "`RegexBuilder` is required for all regex usage in Swift 6 and later" },
          { id: "d", text: "It replaces the need for the `Regex` type entirely" },
        ],
        correctOptionId: "b",
        explanation: "`RegexBuilder` (built on the result-builder mechanism from section 12) trades some conciseness for substantially better readability, letting you name and compose pattern pieces explicitly and access captures by name (`match[areaCode]`) instead of by position.",
      },
      {
        id: "q13",
        prompt: "What does `price.formatted(.currency(code: \"USD\"))` do differently from manually formatting a number with string interpolation?",
        options: [
          { id: "a", text: "Nothing — they always produce identical output regardless of locale" },
          { id: "b", text: "It automatically applies the correct grouping and decimal separator conventions for the user's current locale, without any additional configuration code" },
          { id: "c", text: "It only works for `Int` values, not `Double`" },
          { id: "d", text: "It requires manually specifying the user's locale every time" },
        ],
        correctOptionId: "b",
        explanation: "`FormatStyle`'s locale-awareness is automatic — the same `.formatted(.currency(code: \"USD\"))` call adapts its grouping/decimal separator presentation based on the user's current locale settings, something that required considerably more manual configuration with the older `NumberFormatter` API.",
      },
      {
        id: "q14",
        prompt: "What does `pastDate.formatted(.relative(presentation: .named))` produce?",
        options: [
          { id: "a", text: "A raw ISO 8601 timestamp string" },
          { id: "b", text: "A human-friendly relative phrasing, like \"2 hours ago,\" computed automatically without manually calculating the time difference yourself" },
          { id: "c", text: "A `Duration` value, not a `String`" },
          { id: "d", text: "An error, since relative formatting isn't supported for past dates" },
        ],
        correctOptionId: "b",
        explanation: "`FormatStyle`'s relative date formatting produces natural-language phrasing like \"2 hours ago\" or \"in 3 days,\" handling the underlying time-difference calculation and locale-appropriate phrasing automatically — the same system powering relative timestamps throughout iOS apps like Messages and Mail.",
      },
      {
        id: "q15",
        prompt: "What compile-time safety benefit does `Measurement<UnitType>` provide over storing a plain `Double` and remembering its unit separately?",
        options: [
          { id: "a", text: "`Measurement` values run computations faster than plain doubles" },
          { id: "b", text: "Since the unit is part of the value's type-level information, the compiler can catch category errors (like accidentally treating meters as miles) as a type mismatch, rather than silently producing an incorrect numeric result" },
          { id: "c", text: "`Measurement` automatically converts all units to metric" },
          { id: "d", text: "`Measurement` values cannot be added together at all" },
        ],
        correctOptionId: "b",
        explanation: "By encoding the unit directly into the type (`Measurement<UnitLength>` vs. `Measurement<UnitTemperature>`, for instance), the compiler can catch unit-category mismatches at compile time — directly analogous to the phantom-type pattern from section 8.13, but as a ready-to-use standard-library feature.",
      },
      {
        id: "q16",
        prompt: "What is `AttributedString`, and how does it relate to SwiftUI's `Text` view?",
        options: [
          { id: "a", text: "An older, deprecated API being phased out in favor of `NSAttributedString`" },
          { id: "b", text: "A modern, value-type representation of text with formatting information attached to specific ranges (including built-in Markdown parsing support), which SwiftUI's `Text` view accepts directly" },
          { id: "c", text: "A property wrapper for storing string values in `UserDefaults`" },
          { id: "d", text: "A macro that generates localized strings automatically" },
        ],
        correctOptionId: "b",
        explanation: "`AttributedString` is the modern, value-type successor to `NSAttributedString`, supporting formatting attached to string ranges and native Markdown parsing. SwiftUI's `Text` view (section 23) accepts it directly, letting you author simple rich text (bold, links, emphasis) using plain Markdown syntax.",
      },
      {
        id: "q17",
        prompt: "What is the key functional difference between `ContinuousClock` and `SuspendingClock`?",
        options: [
          { id: "a", text: "They are interchangeable with no behavioral difference" },
          { id: "b", text: "`ContinuousClock` keeps ticking even while the device is asleep, measuring genuine elapsed wall-clock time; `SuspendingClock` pauses while the device is asleep, measuring only time the device was actually awake and running" },
          { id: "c", text: "`SuspendingClock` is more precise than `ContinuousClock`" },
          { id: "d", text: "`ContinuousClock` can only measure time in whole seconds" },
        ],
        correctOptionId: "b",
        explanation: "This distinction matters specifically for anything spanning a potential device sleep/wake cycle: a user-facing countdown almost always wants `ContinuousClock` (real elapsed time, regardless of sleep), while measuring actual active execution time might specifically call for `SuspendingClock`.",
      },
      {
        id: "q18",
        prompt: "What does `withObservationTracking` do, in the context of the `Observation` module underlying `@Observable`?",
        options: [
          { id: "a", text: "It permanently disables change tracking for a type" },
          { id: "b", text: "It registers a callback that fires the next time any property actually *read* inside its tracked closure subsequently changes — precise, per-property tracking determined dynamically by what was actually accessed" },
          { id: "c", text: "It converts a class into a struct" },
          { id: "d", text: "It requires manually calling `objectWillChange.send()`" },
        ],
        correctOptionId: "b",
        explanation: "`withObservationTracking`'s tracking is dynamic and precise: only properties genuinely accessed inside the tracked closure are registered, and the `onChange` callback fires exactly once, the next time one of those specific properties changes — a more fine-grained model than the older `ObservableObject`/`@Published` system's blanket \"notify on any change\" broadcast.",
      },
      {
        id: "q19",
        prompt: "What is `Mutex`/`Atomic` (from the `Synchronization` module) generally most appropriate for?",
        options: [
          { id: "a", text: "Every single instance of shared mutable state in an app, as the default first choice" },
          { id: "b", text: "Specialized, performance-oriented situations with proven, measured thread contention on a specific piece of shared state — not a default first reach, which should generally prefer actors for everyday app-level concurrency" },
          { id: "c", text: "Replacing all uses of `Codable` in a project" },
          { id: "d", text: "Formatting dates and numbers with locale awareness" },
        ],
        correctOptionId: "b",
        explanation: "The lesson is explicit that `Mutex`/`Atomic` are low-level, performance-oriented tools most relevant for library authors or code with actual measured contention — everyday app-level concurrency should generally prefer actors (covered in Part 2), reserving these lower-level primitives for narrower, justified cases.",
      },
      {
        id: "q20",
        prompt: "In what context is the `Subprocess` API (reaching 1.0 alongside recent Swift releases) primarily useful?",
        options: [
          { id: "a", text: "Building SwiftUI view hierarchies" },
          { id: "b", text: "Launching and communicating with external processes — primarily relevant for command-line tools, server-side Swift, or build/automation scripts, since sandboxed iOS apps generally cannot spawn arbitrary subprocesses at all" },
          { id: "c", text: "Formatting currency values for different locales" },
          { id: "d", text: "Declaring new custom operators" },
        ],
        correctOptionId: "b",
        explanation: "`Subprocess` provides a modern, `async`/`await`-based API for launching external processes, but this capability is essentially irrelevant to typical iOS app code (since sandboxed apps can't spawn arbitrary subprocesses) — it's primarily useful for CLI tools, server-side Swift, and Swift-based automation/build scripts.",
      },
    ],
  },
  {
    id: "low-level-swift-quiz",
    title: "Low-Level Swift Quiz",
    description: "20 questions on UnsafePointer family, Span/RawSpan, InlineArray, ContiguousArray, ManagedBuffer, memory rebinding, and C/C++/Objective-C interoperability.",
    relatedArticleSlug: "low-level-swift",
    questions: [
      {
        id: "q1",
        prompt: "What does \"unsafe\" mean specifically in the context of `UnsafePointer`/`UnsafeMutablePointer`?",
        options: [
          { id: "a", text: "It's just a naming convention with no technical meaning" },
          { id: "b", text: "The compiler provides zero safety net — no bounds checking, no ARC management, no protection against dangling pointers or use-after-free; all of that becomes the developer's responsibility" },
          { id: "c", text: "It means the code cannot be compiled in Release mode" },
          { id: "d", text: "It only applies to networking code" },
        ],
        correctOptionId: "b",
        explanation: "\"Unsafe\" is a precise technical term here — these pointer types deliberately bypass Swift's usual safety guarantees (bounds checking, automatic memory management via ARC) in exchange for direct, C-level control and performance, making it entirely the developer's responsibility to avoid dangling pointers, out-of-bounds access, and similar bugs.",
      },
      {
        id: "q2",
        prompt: "What is the difference between `UnsafePointer<T>` and `UnsafeRawPointer`?",
        options: [
          { id: "a", text: "They are exactly the same type with different names" },
          { id: "b", text: "`UnsafePointer<T>` is typed — it knows it points at a `T`; `UnsafeRawPointer` is untyped, representing raw bytes that require explicit reinterpretation before use" },
          { id: "c", text: "`UnsafeRawPointer` is always safer than `UnsafePointer<T>`" },
          { id: "d", text: "`UnsafePointer<T>` can only be used with class types" },
        ],
        correctOptionId: "b",
        explanation: "The typed pointer family (`UnsafePointer<T>`/`UnsafeMutablePointer<T>`) carries type information about what it points to, while the raw pointer family (`UnsafeRawPointer`/`UnsafeMutableRawPointer`) represents untyped bytes, requiring you to explicitly specify a type when reading or reinterpreting the memory.",
      },
      {
        id: "q3",
        prompt: "Why is the pointer inside a `withUnsafeBufferPointer { buffer in ... }` closure considered safer to use than a manually-created, long-lived unsafe pointer?",
        options: [
          { id: "a", text: "It performs automatic bounds checking on every access, just like `Array`'s normal subscript" },
          { id: "b", text: "Its validity is scoped to the duration of the closure, narrowing the \"unsafe\" window considerably compared to a pointer that's created and held onto indefinitely" },
          { id: "c", text: "It's actually a `Span`, not a real unsafe pointer at all" },
          { id: "d", text: "It automatically converts to a safe `Array` after the closure ends" },
        ],
        correctOptionId: "b",
        explanation: "While the buffer pointer itself is still technically \"unsafe\" (no bounds checking within the closure), its lifetime is scoped to that specific closure call — using it after the closure returns would be a use-after-free bug, but the API's design strongly discourages that by construction, unlike a raw pointer you might store and use much later.",
      },
      {
        id: "q4",
        prompt: "What compile-time guarantee does `Span<Element>` provide that `UnsafeBufferPointer` does not?",
        options: [
          { id: "a", text: "`Span` automatically converts to JSON" },
          { id: "b", text: "Using `~Escapable`, `Span` statically prevents it from ever outliving the memory it points into — a guarantee enforced by the compiler, not just a convention `withUnsafeBufferPointer`'s closure-scoping merely discourages" },
          { id: "c", text: "`Span` is always faster than `UnsafeBufferPointer` in every case" },
          { id: "d", text: "`Span` requires no imports and works identically to `Array`" },
        ],
        correctOptionId: "b",
        explanation: "`Span` builds on the `~Escapable`/lifetime-dependency features from section 11.9 to make the \"don't outlive your source memory\" rule an actual compiler-enforced guarantee, whereas `withUnsafeBufferPointer`'s closure scoping only discourages misuse by convention — nothing stops you from smuggling the raw pointer out of the closure if you tried.",
      },
      {
        id: "q5",
        prompt: "What makes `InlineArray<3, Int>` different from a regular `Array<Int>` with three elements?",
        options: [
          { id: "a", text: "They are functionally and structurally identical" },
          { id: "b", text: "`InlineArray`'s elements are stored directly inline (typically on the stack) as part of the type itself, with its fixed size baked into the type — avoiding `Array`'s separate heap-allocated backing storage entirely, at the cost of never being able to grow or shrink" },
          { id: "c", text: "`InlineArray` can only store `String` values" },
          { id: "d", text: "`InlineArray` always performs slower than `Array`" },
        ],
        correctOptionId: "b",
        explanation: "Because its size (`3`) is encoded in the type itself, `InlineArray<3, Int>` can allocate its storage inline (often on the stack) rather than needing a separate heap allocation the way `Array` does — a deliberate tradeoff of flexibility (fixed size only) for reduced allocation overhead, useful for small, fixed-count performance-critical data.",
      },
      {
        id: "q6",
        prompt: "When does the distinction between `Array` and `ContiguousArray` actually matter in practice?",
        options: [
          { id: "a", text: "It matters equally for every element type, always" },
          { id: "b", text: "It matters specifically for class or `@objc`-protocol element types, where plain `Array` might sometimes bridge to/from `NSArray`; for native value types like `Int` or custom structs, the two perform identically" },
          { id: "c", text: "`ContiguousArray` cannot store value types at all" },
          { id: "d", text: "`Array` is always slower than `ContiguousArray`, regardless of element type" },
        ],
        correctOptionId: "b",
        explanation: "For pure value-type elements, `Array` and `ContiguousArray` behave identically — the difference only surfaces for class/`@objc`-protocol elements, where `Array` might involve `NSArray` bridging overhead that `ContiguousArray` guarantees to avoid. `Array` remains the correct general-purpose default.",
      },
      {
        id: "q7",
        prompt: "What is `ManagedBuffer` used for, as illustrated by its role in `Array`'s own implementation?",
        options: [
          { id: "a", text: "It's a networking utility for managing HTTP request buffers" },
          { id: "b", text: "It's the low-level mechanism allocating a single heap block combining a fixed header with a variable-length \"tail\" of elements immediately following it — avoiding a separate allocation for the elements, which is exactly how `Array`'s copy-on-write storage is implemented internally" },
          { id: "c", text: "It's a property wrapper for observing changes to a buffer" },
          { id: "d", text: "It's used exclusively for `Codable` synthesis" },
        ],
        correctOptionId: "b",
        explanation: "`ManagedBuffer` lets you combine a header (arbitrary properties, like `Array`'s count/capacity bookkeeping) and a tail of elements into one contiguous heap allocation — precisely the technique underlying `Array`'s own copy-on-write storage (recall section 10.11), avoiding the overhead of a separate allocation for the header versus the elements.",
      },
      {
        id: "q8",
        prompt: "What does \"rebinding\" memory mean, as with `withMemoryRebound(to:capacity:)`?",
        options: [
          { id: "a", text: "Releasing memory back to the operating system" },
          { id: "b", text: "Telling the compiler to reinterpret an existing block of memory as holding a different type than it was originally allocated for — an inherently unsafe operation governed by strict layout-compatibility rules" },
          { id: "c", text: "Converting a class instance into a struct" },
          { id: "d", text: "Automatically resizing an array's capacity" },
        ],
        correctOptionId: "b",
        explanation: "Rebinding reinterprets existing memory's type without changing the underlying bytes — a legitimately unsafe operation, since Swift's type system normally assumes memory holds exactly the type it was allocated as. Violating the rules governing valid rebinds (layout/size compatibility) produces undefined behavior.",
      },
      {
        id: "q9",
        prompt: "What happens if memory rebinding rules are violated (e.g. rebinding to an incompatible layout/size)?",
        options: [
          { id: "a", text: "Swift throws a catchable runtime error" },
          { id: "b", text: "It produces undefined behavior — code that might happen to work by coincidence on one platform or compiler version, but isn't guaranteed to work correctly, and could silently break elsewhere" },
          { id: "c", text: "The compiler automatically corrects the mistake" },
          { id: "d", text: "Nothing happens; rebinding rules are purely advisory with no real consequences" },
        ],
        correctOptionId: "b",
        explanation: "Memory rebinding violations fall into undefined behavior territory — there's no guaranteed error or crash; the code might appear to work in some configurations while being fundamentally unreliable, which is exactly why this is considered expert-level, high-risk territory reserved for very specific low-level use cases.",
      },
      {
        id: "q10",
        prompt: "What does Swift's C interoperability generally allow you to do?",
        options: [
          { id: "a", text: "Nothing — Swift cannot interact with C code at all" },
          { id: "b", text: "Call C functions, use C structs, and work with C pointers nearly directly, with automatic translation of most C conventions into Swift equivalents, via a bridging header or module map" },
          { id: "c", text: "Only interact with C code that has been manually rewritten in Objective-C first" },
          { id: "d", text: "C interop requires a separate paid Apple developer tool" },
        ],
        correctOptionId: "b",
        explanation: "Swift's C interop is near-seamless: importing a C header makes C functions, structs, and types directly callable/usable from Swift, with C's pointer-heavy APIs typically surfacing as `UnsafePointer`/`UnsafeMutablePointer` types — no intermediate Objective-C wrapper layer is required.",
      },
      {
        id: "q11",
        prompt: "How do C's pointer-based APIs typically surface once imported into Swift?",
        options: [
          { id: "a", text: "As `Array` values automatically" },
          { id: "b", text: "As `UnsafePointer`/`UnsafeMutablePointer` types, tying directly back to the unsafe pointer family covered earlier in this section" },
          { id: "c", text: "As `Codable` structs" },
          { id: "d", text: "They cannot be represented in Swift at all and must be avoided" },
        ],
        correctOptionId: "b",
        explanation: "Since C's memory model is fundamentally pointer-based with no built-in safety guarantees, imported C pointer APIs naturally map onto Swift's own unsafe pointer types (`UnsafePointer`, `UnsafeMutablePointer`), which exist precisely to represent this kind of direct, unchecked memory access.",
      },
      {
        id: "q12",
        prompt: "Why is Swift's C++ interoperability mode described as substantially more involved than its C interop?",
        options: [
          { id: "a", text: "Because C++ doesn't actually have any features beyond what C offers" },
          { id: "b", text: "Because C++'s richer type system — templates, references, destructors, inheritance — doesn't map onto Swift's model as directly as C's comparatively simple type system does" },
          { id: "c", text: "Because C++ interop has been fully deprecated and removed from Swift" },
          { id: "d", text: "Because C++ code cannot be compiled on Apple platforms" },
        ],
        correctOptionId: "b",
        explanation: "C's type system is comparatively simple (functions, structs, pointers), mapping fairly directly onto Swift equivalents. C++ introduces significantly more complex features — templates, references, inheritance, destructors — that require much more sophisticated bridging logic, making C++ interop a more involved, actively-evolving area of the language.",
      },
      {
        id: "q13",
        prompt: "What is a practical use case for adopting Swift's C++ interoperability mode?",
        options: [
          { id: "a", text: "Formatting currency values for different locales" },
          { id: "b", text: "Gradually adopting Swift inside a large, established C++ codebase (common in games or long-lived cross-platform libraries) without requiring a full rewrite" },
          { id: "c", text: "Writing SwiftUI views more concisely" },
          { id: "d", text: "Automatically synthesizing `Codable` conformance" },
        ],
        correctOptionId: "b",
        explanation: "C++ interop is specifically valuable for incrementally introducing Swift into an existing, substantial C++ codebase — letting teams adopt Swift piece by piece in contexts like games or established cross-platform libraries, rather than committing to a complete rewrite up front.",
      },
      {
        id: "q14",
        prompt: "Why isn't bridging a Swift `Array`/`String` to its Objective-C counterpart (`NSArray`/`NSString`) always \"free,\" despite being syntactically seamless?",
        options: [
          { id: "a", text: "Bridging is always instantaneous and has zero cost in every scenario" },
          { id: "b", text: "The bridging can involve actual conversion work reconciling potentially different internal representations between the Swift and Objective-C types, which becomes a real, measurable cost for large collections/strings or in tight, repeated loops" },
          { id: "c", text: "Bridging only works one direction (Objective-C to Swift), never the reverse" },
          { id: "d", text: "`NSArray` and `Array` always share byte-for-byte identical memory layouts, making bridging trivial" },
        ],
        correctOptionId: "b",
        explanation: "While the syntax for crossing the Swift/Objective-C boundary looks effortless (`swiftArray as NSArray`), the underlying representations aren't always identical, so real conversion work can occur — usually invisible and irrelevant for typical app code, but a genuine, measurable cost in performance-critical code that crosses this boundary repeatedly, especially with large data.",
      },
      {
        id: "q15",
        prompt: "What is `RawSpan`'s relationship to `Span`, analogous to which other pair of types?",
        options: [
          { id: "a", text: "`RawSpan` is a completely unrelated type with no connection to `Span`" },
          { id: "b", text: "`RawSpan` is the untyped counterpart to `Span`, analogous to how `UnsafeRawPointer` relates to `UnsafePointer<T>`" },
          { id: "c", text: "`RawSpan` is a deprecated predecessor to `Span`" },
          { id: "d", text: "`RawSpan` only works with `String` values" },
        ],
        correctOptionId: "b",
        explanation: "Just as `UnsafeRawPointer` represents untyped bytes requiring explicit reinterpretation (versus `UnsafePointer<T>`'s typed access), `RawSpan` is `Span`'s untyped counterpart — both pairs follow the same typed-vs-raw distinction within their respective safety models.",
      },
      {
        id: "q16",
        prompt: "For most everyday iOS app development, how commonly should a developer expect to reach for `UnsafePointer`, `ManagedBuffer`, or manual memory rebinding?",
        options: [
          { id: "a", text: "Constantly — these are foundational tools required for basic SwiftUI development" },
          { id: "b", text: "Rarely — these are expert-level tools primarily relevant for optimizing hot paths, implementing custom high-performance collection types, or bridging to non-Swift code, not typical application-level logic" },
          { id: "c", text: "Every time an array is created" },
          { id: "d", text: "Only when using `Codable`" },
        ],
        correctOptionId: "b",
        explanation: "The lesson explicitly frames this entire section as expert-level material \"rarely needed in typical app code\" — most developers will only reach for these tools when optimizing specific hot paths or building interop shims, not as part of everyday application logic.",
      },
      {
        id: "q17",
        prompt: "What advantage does `InlineArray` offer specifically for something like a fixed 3D vector's components, compared to using `Array`?",
        options: [
          { id: "a", text: "`InlineArray` supports appending new elements more easily than `Array`" },
          { id: "b", text: "Since the vector's component count (3) is fixed and known at compile time, `InlineArray` avoids `Array`'s heap allocation overhead entirely by storing the elements inline, typically on the stack" },
          { id: "c", text: "`InlineArray` automatically conforms to `Codable`, while `Array` does not" },
          { id: "d", text: "`InlineArray` is required for any type used inside a SwiftUI view" },
        ],
        correctOptionId: "b",
        explanation: "A fixed-size, compile-time-known collection like a 3D vector's three components is exactly the scenario `InlineArray` is designed for — since the size never needs to change, storing it inline (avoiding a separate heap allocation) is a pure performance win with no real downside for that specific use case.",
      },
      {
        id: "q18",
        prompt: "What must be true about the types involved for a memory-rebinding operation to be well-defined, rather than undefined behavior?",
        options: [
          { id: "a", text: "Nothing — any rebind is automatically safe as long as it compiles" },
          { id: "b", text: "The types involved must have layout/size compatibility, per Swift's memory model's strict rules governing valid rebinds" },
          { id: "c", text: "The rebind must always target `String`" },
          { id: "d", text: "Rebinding is always undefined behavior, with no well-defined cases at all" },
        ],
        correctOptionId: "b",
        explanation: "Swift's memory model does permit rebinding under strict compatibility rules — generally requiring the involved types to have compatible size/layout for the operation to be well-defined. Rebinding outside those rules is what produces the undefined behavior discussed in Q9.",
      },
      {
        id: "q19",
        prompt: "What role does `withUnsafeBufferPointer` commonly play when interoperating with C APIs?",
        options: [
          { id: "a", text: "It converts Swift arrays into JSON automatically" },
          { id: "b", text: "It provides a scoped, contiguous raw view into a Swift collection's storage — exactly the shape (a pointer plus a length) that many C APIs expect to receive as arguments" },
          { id: "c", text: "It's used exclusively for encoding/decoding `Codable` types" },
          { id: "d", text: "It has no relationship to C interoperability whatsoever" },
        ],
        correctOptionId: "b",
        explanation: "Many C APIs expect a pointer-and-length pair for array-like data. `withUnsafeBufferPointer` provides exactly that shape — a scoped, contiguous view into a Swift collection's storage — making it a natural bridge when calling into C functions that expect this classic C convention.",
      },
      {
        id: "q20",
        prompt: "Which best summarizes the overall theme connecting `Span`, `InlineArray`, and `~Copyable`/ownership features (from section 11) as a broader direction in recent Swift?",
        options: [
          { id: "a", text: "They are all deprecated features being removed from the language" },
          { id: "b", text: "They represent Swift's ongoing effort to provide memory-safe, low-overhead alternatives to older unsafe or heap-allocation-heavy patterns, extending compile-time safety guarantees further into performance-critical, low-level code" },
          { id: "c", text: "They exist purely for backward compatibility with Objective-C" },
          { id: "d", text: "They are exclusively used for networking code" },
        ],
        correctOptionId: "b",
        explanation: "`Span` (safe, bounds-checked, lifetime-enforced memory views), `InlineArray` (safe, allocation-free fixed-size storage), and the ownership/`~Copyable` features from section 11 all share a common thread: extending Swift's compile-time safety guarantees into performance-sensitive, low-level territory that previously required dropping into genuinely unsafe constructs — narrowing the gap between \"safe\" and \"fast\" Swift code.",
      },
    ],
  },
];

export function getAllQuizzes(): Quiz[] {
  return quizzes;
}

export function getQuiz(id: string): Quiz | undefined {
  return quizzes.find((quiz) => quiz.id === id);
}
